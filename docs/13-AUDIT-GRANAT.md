# Аудит granat-kmv.ru — 08.08.2026

Повторный аудит, `VD999-2026-08-08-GRANAT-KMV`. Сырой отчёт целиком:
`docs/granat-kmv-audit/2026-08-08.json`. Здесь — только то, что нужно, чтобы начать
работу и не наступить на противоречие в исходных данных.

Сайт клиентский, не этот. Правки делать в репозитории granat-kmv.ru.

## Что показал аудит

Общая оценка 76 против 68 четыре дня назад. Обе критичные находки прошлого раза
закрыты: сайт перестал быть одностраничником (появились `/pechat`, `/cifra`,
`/raboty`, `/kak-rabotaem`, `/kontakty`), H1 и title получили услугу и гео.

Взамен две новые критичные находки, обе про доставку результата, а не про качество
работы:

| ID | Что | Почему срочно |
|---|---|---|
| `T-09` | `sitemap.xml` отдаётся бинарными данными вместо XML | Пять новых страниц могут не дойти до робота. Обесценивает работу недели |
| `L-03` | Трансграничная передача есть, в Политике её нет | ч. 8 ст. 13.11 КоАП: для ИП до 300 000 ₽ при первом нарушении |

Ещё десять находок со статусом «важно» и «потом» — в JSON, поле `findings`.

## Противоречие, которое надо снять до правок

Два документа от одной даты расходятся в том, где физически живёт сайт.

- **ТЗ по Метрике** (`docs/12-METRIKA-GRANAT.md`): «Стек настоящего сайта: Python +
  Flask + обычный HTML, деплой на **Amvera**», основание — строка `Serving Flask app
  'app'` в логах. Там же прямое предупреждение: репозиторий `C:\granat-kmv` на Next.js
  запушен по ошибке и к сайту отношения не имеет.
- **Аудит** (находка `L-03`): «Сайт размещён на **Vercel (США)** — это следует из
  технических признаков площадки».

Основания неравны. В ТЗ — прямая улика из логов сервера. В аудите — вывод по косвенным
признакам, снятым конвертацией страницы в текст; при этом сам аудитор знал про
Next.js-репозиторий, а Next.js по умолчанию живёт на Vercel, так что вывод мог прийти
не с сайта, а из соседнего документа.

Проверить снаружи из рабочего окружения не вышло: домен закрыт сетевой политикой,
прокси отвечает 403 на CONNECT. Проверять руками — заголовок ответа `Server` и панель
хостинга.

**Что меняется от ответа:**

- Сайт на Amvera → из `L-03` уходит хостинг: раздел про перенос в РФ и половина оценки
  риска. Остаётся Telegram — заявки уходят в мессенджер иностранной компании, значит
  трансграничная передача всё равно есть, раздел в Политике и уведомление в РКН нужны.
  Находка не отменяется, она дешевеет: вместо «2-3 часа + перенос на 1-2 дня» остаются
  документы.
- Сайт на Vercel → `L-03` берётся как написано, целиком.

То же касается `T-09`: в отчёте предложено искать причину в сжатии на Vercel. Если
отдаёт Flask — смотреть надо, каким `Content-Type` и через какой `send_file` уходит
`sitemap.xml`, диагностика другая.

## Порядок работ

Неделя 1 — не потерять сделанное: `T-09` (sitemap), `T-05` (коды ответа для робота),
переобход пяти URL, `T-03` (фавикон), `T-06` (www-зеркало), `A-01` (цели в Метрике).
Параллельно юридический блок: `L-03` и `L-01` одним заходом в РКН.

Дальше по неделям — `roadmap` в JSON: микроразметка и доверие, дочерние страницы услуг,
гео-страницы по КМВ.

`A-01` уже расписан отдельно и делать его заново не надо — это `docs/12-METRIKA-GRANAT.md`
вместе с файлами в `docs/granat-kmv-metrika/`. Аудит просит отдельную цель на форму
каждой страницы; в комплекте формы разводятся параметром, а не разными целями, так что
при сборке выбрать что-то одно и не плодить дубли в интерфейсе Метрики.

## Прайс

В `upsell_strategy.price_list` снят прайс с сайта: полиграфия от 200 ₽, лендинг от
25 000 ₽, лид-бот от 15 000 ₽, полная автоматизация от 70 000 ₽, мини-приложения от
90 000 ₽. Цены стартовые. Отдельного документа с прайсом в базе знаний нет — если КП
собираются руками, его стоит завести, чтобы цифры не расходились между КП и сайтом.
