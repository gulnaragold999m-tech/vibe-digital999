# 01 — Архитектура сайта

Главная — одна длинная страница, прокрутка сверху вниз. С августа 2026 к ней
добавились отдельные посадочные страницы под каждую услугу.

## Карта адресов

| Адрес | Что там | Под какой запрос |
|-------|---------|------------------|
| `/` | Главная: страница-хаб, ведёт вглубь | «разработка сайтов», общий спрос |
| `/uslugi/razrabotka-sajta/` | Разработка сайта, цены, состав работ | заказать лендинг, создание сайта цена |
| `/uslugi/telegram-bot/` | Telegram-бот: что умеет, цена, срок | разработка телеграм бота на заказ |
| `/uslugi/ai-agent/` | ИИ-агент: что это, чем отличается от бота | ии агент для бизнеса, чат бот под ключ |
| `/uslugi/avtomatizaciya/` | Автоматизация процессов и связки сервисов | автоматизация бизнес процессов |
| `/uslugi/seo-audit-152fz/` | Аудит сайта и проверка по 152-ФЗ | seo аудит сайта, почему сайт не в поиске |
| `/portfolio/granat/` | Кейс: дизайн-студия «Гранат» | доказательство, не поисковый трафик |
| `/portfolio/avtoshkola-start/` | Кейс: автошкола «Старт» | то же |

**Зачем разносили.** Пока все услуги жили на одном адресе, страница могла
конкурировать только за общий запрос про разработку сайтов. Человек, который
ищет «разработка телеграм бота на заказ», попадал на длинную главную и искал
свой абзац глазами. Теперь у каждого коммерческого запроса своя страница
с ценой, сроком, составом работ и формой.

**Главную больше не растим.** Она уже страница-хаб: следующий блок на ней
даст меньше, чем отдельная посадочная. Новая услуга — новый адрес.

## Как устроены посадочные

Все семь собираются скриптом из одного файла с текстами:

```bash
node tools/build-pages.js
```

Текст — в `tools/pages-data.js`. Шапка, подвал, форма, разметка для поисковиков
и карта сайта — в `tools/build-pages.js`. Правка собранных `index.html`
пропадёт при следующей сборке, об этом написано в шапке каждого файла.

Порядок блоков на посадочной, менять не надо:

1. Хлебные крошки — Яндекс показывает их в сниппете вместо голого адреса.
2. Заголовок с ключевым запросом и результатом.
3. Строка боли — почему это про читателя.
4. Цена и срок плашками. Человек из поиска первым делом ищет глазами цифру.
5. Одна главная кнопка.
6. Кому подходит → что получите → состав работ.
7. Цена с оговорками.
8. Этапы работы — одни и те же на всех страницах, это один процесс.
9. Похожий кейс.
10. Вопросы: свои для услуги плюс три общих (договор, исходники, география).
11. Форма заявки.
12. Ссылки на соседние услуги — иначе страницы висят в поиске поодиночке.

Стиль посадочных — в `public/assets/site.css`, скрипт — в `public/assets/site.js`.
Главная ими не пользуется: у неё свои стили внутри `index.html`, и трогать их
ради посадочных незачем. Токены цветов продублированы — меняете фирменный цвет,
меняйте в обоих местах.

## Карта экранов

| № | Блок | Задача блока | Статус |
|---|------|--------------|--------|
| 1 | Первый экран (Ядро) | За 3 секунды объяснить, кто мы и что даём | собран |
| 2 | Услуги | Показать 6 направлений | собран |
| 3 | Портфолио | Доказать, что мы умеем | собран |
| 4 | Прайс | Снять вопрос цены до разговора | собран |
| 5 | Заявка | Собрать контакт | собран |
| 6 | Мультикнопка связи | Дать написать в один клик из любого места страницы | собран |
| 7 | Как работаем | Снять страх «а вдруг долго и непонятно» | планируется |
| 8 | Вопросы | Закрыть возражения до формы | планируется |
| 9 | Подвал | Контакты, политика обработки данных | планируется |

## 1. Первый экран

Экран обязан за три секунды ответить на четыре вопроса: что мы делаем,
для кого, какой результат, что нажать. Порядок блоков и есть ответ,
менять его нельзя.

| Что | Зачем |
|-----|-------|
| Маркер специализации со значком логотипа | что мы делаем и для кого |
| H1 «Сайты и Telegram-боты, которые приводят заявки» | какой результат |
| Строка боли: «Сайт есть, а заявок нет» | почему это про читателя |
| Подзаголовок с ценой и сроком | как и за сколько |
| Одна главная кнопка «Получить расчёт проекта» | что нажать |
| Вторая ссылка «Посмотреть кейсы» | для тех, кому рано нажимать |
| Три строки доверия: ИП и договор, исходники, срок ответа | почему не страшно нажать |
| Демо «заявка в Telegram» справа | как выглядит результат |
| Строка географии | закрывает вопрос «а вы только по КМВ?» |

Фон: тёмный космос + мерцающие частицы на Canvas.

**Чего на первом экране быть не должно.** Декоративных счётчиков без
пользы для клиента: до августа 2026 здесь стояли «4 ниши в работе» и
«0 конструкторов», их заменили строки доверия. Каждая строка доверия
обязана быть проверяемой — не можете подтвердить её делом, не ставьте.
Второй равнозначной кнопки рядом с главной. Спорных абсолютов вроде
«ничего не ломается».

**Раскладка.** От 1000px — две колонки: текст слева, демо справа.
Уже — одна колонка, демо уходит под кнопки. На низких экранах
(до 700px по высоте) демо скрывается, строки доверия остаются.

**Правый край на телефоне.** Панель связи закреплена справа и занимает
нижнюю половину экрана. Всё, что ниже подзаголовка, отступает от правого
края на 2.7rem, иначе иконки ложатся на главную кнопку. Плашка cookie
на телефоне уезжает вниз и проходит под кнопкой — проверяется на 375×667.

## 2. Услуги

Три карточки в ряд, на телефоне — в столбик.

1. **Сайты** — лендинги, визитки, магазины.
2. **AI-агенты** — боты, которые отвечают клиентам и собирают заявки.
3. **Автоматизация** — связки между сервисами, чтобы рутина шла сама.

В каждой карточке: название, 2–3 строки описания, цена «от», ссылка на форму.

## 3. Как работаем

Четыре шага, горизонтальная линия на десктопе, вертикальная на телефоне.

1. Разговор — 20 минут, разбираем задачу.
2. Смета — фиксируем цену и срок.
3. Сборка — показываем промежуточные версии.
4. Запуск — отдаём сайт и учим им пользоваться.

## 4. Работы

Сетка карточек. В каждой: превью, название, ниша, что сделали, результат одной строкой.
Пока проектов мало — показываем 1–3 и не растягиваем блок пустотой.

## 5. Обо мне

Фото, короткий текст от первого лица, 3 факта. Без биографии на страницу.

## 6. Вопросы

Аккордеон, 5–7 вопросов. Открывается по клику и с клавиатуры.

## 7. Заявка

Форма из трёх полей: имя, телефон или телеграм, задача.
Кнопка одна. После отправки — понятное сообщение «получили, ответим за N часов».
Заявка уходит в Telegram владельцу.

## 8. Подвал

Телефон, email, соцсети, ссылка на политику обработки данных, год.

## Технические требования

- Вес страницы без картинок — до 300 КБ.
- Шрифты грузятся с `display=swap`.
- Картинки в WebP, с `loading="lazy"`.
- Анимации отключаются при `prefers-reduced-motion: reduce`.
- Работает без JS: текст и контакты видны, форма отправляется обычным POST.
