---
name: threejs-spravochnik
description: "Справочник по Three.js, положенный в проект целиком: точные сигнатуры конструкторов, сцены и камеры, геометрия, материалы и текстуры, свет и тени, загрузка моделей GLTF, анимация, шейдеры, постобработка, наведение и клики по объектам. Открывай этот навык всегда, когда пишешь код на Three.js или WebGL: нужна сцена, модель, освещение, текстура, шейдер, райкастинг — чтобы брать API из справочника, а не по памяти: имена и сигнатуры меняются от релиза к релизу. Решение «нужен ли здесь 3D вообще» принимается раньше и в навыке 3d-na-sajte."
---

# Three.js: справочник в проекте

Набор из десяти файлов по разделам Three.js, положенный к нам целиком.
Нужен по одной причине: **API Three.js меняется от релиза к релизу**,
и код, написанный по памяти или по статье трёхлетней давности,
не запускается — а ошибка выглядит как «не работает 3D».

**Порядок работы: сначала `3d-na-sajte`, потом этот навык.**
Первый отвечает на вопрос, нужен ли здесь 3D вообще (для лендинга
местного бизнеса почти всегда нет), задаёт бюджет по весу, запасную
картинку и отключение на слабых телефонах. Этот — как написать сцену,
когда решение уже принято.

## Что где лежит

| Файл | Про что |
|---|---|
| `references/threejs-fundamentals.md` | сцена, камера, рендерер, цикл отрисовки |
| `references/threejs-geometry.md` | геометрия: примитивы, буферы, слияние |
| `references/threejs-materials.md` | материалы, PBR, прозрачность |
| `references/threejs-textures.md` | текстуры, развёртки, сжатие |
| `references/threejs-lighting.md` | свет и тени |
| `references/threejs-loaders.md` | загрузка моделей: GLTF, Draco, окружение |
| `references/threejs-animation.md` | анимация: миксер, клипы, морфинг |
| `references/threejs-shaders.md` | шейдеры и собственные материалы |
| `references/threejs-postprocessing.md` | постобработка: свечение, глубина резкости |
| `references/threejs-interaction.md` | наведение, клики, райкастинг, управление камерой |

## Наши поправки к их советам

Справочник исходит из того, что проект собирается сборщиком и ставит
Three.js через npm. У нас статические файлы, поэтому:

1. **Подключение — через `importmap` с локальными файлами**
   в `public/assets/js/vendor/`, не через npm и не с CDN. Готовый
   скелет со всеми проверками — `../3d-na-sajte/references/minimalnaya-scena.md`.
2. **Постобработка и тени по умолчанию выключены.** В справочнике
   это отдельные разделы, у нас — первое, что снимается ради телефона.
3. **Плотность пикселей ограничена двойкой**, размер холста —
   не на весь экран. Это даёт больше, чем все остальные оптимизации
   вместе взятые.
4. **Сцена обязана уметь не запускаться**: нет WebGL, включено
   «уменьшить движение», экономия трафика, слабое устройство —
   показывается картинка.

## Источник

`CloudAI-X/threejs-skills`, лицензия MIT (объявлена в их README;
отдельного файла лицензии в репозитории нет). Взято 18.08.2026,
версия `b1c6230` от 20.01.2026. Файлы в `references/` — их, слово
в слово; переименованы только имена файлов.

**Справочник не заменяет документацию.** Он про то, как обычно пишут;
точные сигнатуры под конкретную версию — на `threejs.org/docs`.
