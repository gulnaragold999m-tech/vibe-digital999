# Сайт делается КЛИЕНТУ — что меняется

Ниши будут разные: автошкола, парикмахерская, шашлычная, стоматология,
гостевой дом. **Устройство сайта у них одинаковое, меняется наполнение.**
Поэтому здесь ничего про конкретную нишу.

---

## Новый клиент — новый репозиторий. Всё остальное растёт из него

**Главное правило, и оно решается до первого коммита.**

Репозиторий заводится не на сайт, не на бота и не на приложение.
**Репозиторий — это клиент.** Он корень, из которого растут ноги: сайт,
приложение, бот, агент, лендинг под акцию, интеграция с записью. Всё,
что мы делаем для этого клиента, живёт внутри.

```
klient-nazvanie/          <- репозиторий назван по клиенту
  CLAUDE.md      правила работы по этому клиенту
  STATUS.md      где что лежит, что выкачено, чем управляется
  TASKS.md       журнал: что делали, где остановились
  .claude/skills/  навыки, скопированные из студийного репозитория
  docs/          знание о клиенте: филиалы, цены, расписание, договор
  site/          сайт
  app/           приложение
  bot/           боты
  agent/         агент
```

Соблазн — заводить хранилище под каждый кусок: репозиторий на сайт,
репозиторий на приложение, репозиторий на бота. **Так делать не надо.**
Проверено: у первого же клиента приложение и бот оказались в одном
месте, знание о клиенте — в другом, а искать пришлось по беседам.

Почему именно так:

- **Знание о клиенте одно.** Цены, адреса филиалов, часы работы, тон
  общения нужны и сайту, и боту, и приложению. Разложенные по трём
  репозиториям, они разойдутся — не по небрежности, а сами собой.
- **Навыки копируются один раз**, а не в каждый репозиторий отдельно.
- **Журнал один.** Иначе история работы по клиенту рассыпана по трём
  местам, и через месяц непонятно, где что решали.
- **Новая беседа начинает со знанием.** Она видит `CLAUDE.md` и навыки
  сразу, без объяснений от человека.

**Разные способы выкатки этому не мешают:** сайт, бот и приложение
уезжают каждый на своё, просто из разных папок одного репозитория.

**Если часть уже живёт отдельным репозиторием** — не переносить сразу.
Перенос истории отдельная работа, и она может подождать. Достаточно
записать в `STATUS.md`, где эта часть лежит, и поставить ссылку.
Главное, чтобы **знание о клиенте** переехало в общий репозиторий сразу.

---

## Из чего собирается сайт бизнеса с адресами

Порядок — по тому, в каком порядке про это спрашивают у администратора
по телефону. Это и есть правильный порядок блоков на странице.

1. **Главное расписание бизнеса, по каждой точке.** То, ради чего звонят
   чаще всего.
2. **Цены и что в них входит.** Со всеми доплатами. Скрытая доплата —
   первая причина плохих отзывов в любой нише.
3. **Что именно предлагают, списком.** Каждая позиция — отдельный запрос
   в поиске.
4. **Запись или заказ без звонка.** Форма с выбором точки. Заявка уходит
   администратору сразу, а не остаётся в пропущенных вызовах.
5. **Люди и обстановка.** Фотографии **реальные**. Сгенерированные
   нейросетью залы, блюда и сотрудники недопустимы: человек приедет
   и увидит другое.
6. **Документы.** Лицензия, если ниша лицензируемая, реквизиты, ИНН.
   Это проверяют перед оплатой.
7. **Отзывы** — ссылкой на карточку в Яндекс Картах, а не текстом на
   странице. Текст без источника не убеждает никого.

Первый пункт в разных нишах:

| Ниша | «Расписание» — это |
|---|---|
| Автошкола | Когда стартует группа, в каком филиале, есть ли места |
| Парикмахерская | Свободные окошки у мастера на ближайшие дни |
| Шашлычная, кафе | Часы работы, зона и время доставки, меню дня |
| Стоматология | Свободное время у врача по специальности |
| Гостевой дом | Свободные даты и что входит в проживание |

Третий пункт так же: у автошколы это категории обучения, у
парикмахерской — услуги с длительностью, у шашлычной — меню,
у гостевого дома — номера.

---

## Метрика ставится КЛИЕНТА, а не своя

Свой счётчик на чужом сайте — это чужие данные в своём кабинете и свои
в чужом. Заводится новый, доступ отдаётся владельцу бизнеса.

Цели, которые имеют смысл почти в любой нише:

| Цель | Как считается |
|---|---|
| Заявка на запись | Отправка формы |
| Нажатие на телефон | Клик по `tel:` |
| Открытие расписания | Просмотр раздела |
| Переход в мессенджер | Клик по WhatsApp или Telegram |

**Зонтичная цель «любое обращение» — обязательно.** Без неё нельзя
сравнить каналы между собой, а автостратегии рекламы учатся на одной
цели: шесть редких не дадут статистики ни по одной.

Смотреть после запуска: страницы входа, глубина просмотра, конверсия по
филиалам. **Если один филиал даёт трафик и не даёт заявок — проблема
в расписании или в цене, а не в сайте.**

---

## Что нельзя обещать клиенту

- **Позиции в поиске к сроку.** Обход занимает недели, а порядок выдачи
  зависит не от нас.
- **Количество заявок.** Мы отвечаем за то, что заявка дойдёт и не
  потеряется, а не за то, сколько их придёт.
- **Слежение за позициями**, если инструмента для этого нет.
- **Бесперебойную работу**, если хостинг выбирает клиент.

**Что обещать можно и нужно:** сроки работы, состав работ, передачу
исходников и доступов после полной оплаты, и то, что заявка не пропадёт.

---

## Граница ответственности

Если студия продаёт аудиты и проверки по 152-ФЗ — **сдать клиенту сайт,
который сама бы отметила в таком аудите красным, нельзя ни за какие
сроки.** Это не про совесть, это про то, что первый же аудит конкурента
покажет клиенту, что ему продали.

Клиент настаивает на своём варианте — **предупредить письменно,
в переписке,** и зафиксировать, что решение принято им. Устного
предупреждения не существует.

---

## Клиентский проект на Amvera — что известно наверняка

Проверено на живых проектах. Сборка у клиента может быть любой — Vite,
Next, свой скрипт, — всё, что ниже, от сборки не зависит.

- **Выкатка — отдельный push в remote Amvera**, а не в GitHub. Push
  в GitHub не публикует ничего. Порядок: сначала `git fetch origin <ветка>`,
  потом `git push --force amvera origin/<ветка>:master`. Без свежего
  `fetch` уедет старая версия, и push при этом отработает без ошибок —
  выглядит как успех.
- **`amvera.yaml` в корне.** Окружение `node`, `run.command`,
  `containerPort`, `persistenceMount: /data`. Amvera читает его сама.
- **Сборка идёт на сервере, а не приносится готовой.** У проекта на Vite
  это `npm run build` → `dist/`, дальше нужен либо сервер, который эту
  папку отдаёт, либо статическое окружение. Есть свой API — окружение
  только `node`.
- **Собранную папку держать в репозитории.** Если сборка повешена
  на `prestart` со значком `|| true`, упавшая сборка проходит МОЛЧА,
  и сервер отдаёт то, что уже лежит. Собранные файлы — единственная
  страховка: без них первая же неудачная сборка оставит клиента
  с пустым сайтом, и в логах не будет ни слова об ошибке.
- **Переменные окружения читаются только при старте.** Завели или
  поменяли — нажать «Перезапустить», иначе приложение их не увидит
  и откажет на ровном месте.
- **IPv6 на Amvera не работает.** В Node первой строкой
  `dns.setDefaultResultOrder('ipv4first')`, иначе исходящие соединения
  висят до таймаута и это выглядит как «сервис не отвечает».
- **Настоящий IP посетителя до приложения не доходит** — прокси
  подставляет свой внутренний `10.x.x.x`. Ни город по IP, ни ограничение
  по адресу не работают. Частоту заявок считать по телефону.
- **Логов два, и панель не обновляется сама** — перечитывать вручную.
  И окно поиска по умолчанию «последние 30 минут»: если выкладка была
  раньше, логи покажутся пустыми, хотя всё в порядке.
- **Выкладка через GitHub Actions: имя приложения — ВНУТРЕННЕЕ,
  маленькими латинскими.** Проверено 12.08.2026 на двух упавших
  запусках подряд. В панели приложение подписано «СТАРТ», а в адресе
  кабинета и в пути стоит `start` — работает только оно. Заглавные
  и кириллица дают ошибку «папка не найдена».
- **Логин для выкладки — от Amvera, а не от GitHub.** Вторая причина
  падения там же. Смотреть в правом верхнем углу кабинета Amvera.
