# Задачи проекта vibe-digital999

Обновлено: 11 августа 2026

**Это журнал проекта.** Он живёт дольше любого чата: чат заканчивается,
память пропадает, а файл остаётся. Каждый чат читает его перед началом
работы и дописывает перед словом «готово» — правило записано в `CLAUDE.md`,
раздел «Перед словом „готово“».

`- [x]` — сделано и видно на сайте. `- [ ]` — висит, напоминание не снимать,
пока не проверено на живом сайте, в Вебмастере или в Яндекс Бизнесе.

---

## Сделано

- [x] Архитектура и структура проекта
- [x] Контент переведён на B2B, полиграфия и маркетплейсы убраны
- [x] Форма заявки переписана: токен ушёл из браузера на сервер
- [x] Исправлены синтаксические ошибки, из-за которых форма не работала
- [x] Мультикнопка связи: телефон, WhatsApp, Telegram, VK
- [x] Фон — живой граф связей с импульсами
- [x] SEO: мета-теги, robots.txt, sitemap.xml
- [x] Яндекс.Метрика, счётчик 110416869, семь целей
- [x] Логотип 2.03 МБ → 62 КБ
- [x] Деплой на Amvera, адрес `vibe-digital-granatgold999.amvera.io`
- [x] Заголовок первого экрана переписан под боль клиента
- [x] Первый экран помещается на ноутбуках 700–800px
- [x] Блок «За счёт чего код остаётся чистым»
- [x] Блок «Как мы работаем», четыре шага
- [x] Блок «Частые вопросы», шесть возражений
- [x] Подвал с реквизитами ИП
- [x] Политика конфиденциальности и согласие в формах
- [x] Квиз-калькулятор с вопросом про прошлых разработчиков
- [x] Тест-драйв скорости, вариант А — честный замер
- [x] Первый экран пересобран под конверсию: боль клиента, одна главная
      кнопка, три строки доверия вместо счётчиков, демо «заявка в Telegram»
      вместо орбиты
- [x] Убраны спорные абсолюты: «перестали платить — он пропал» про
      конструкторы и «чинить обычно нечего» про поломки
- [x] Карточка 02 в блоке «Не верьте на слово» стала ссылкой — до этого
      она была кнопкой и в разметке выглядела как пропущенный пункт
- [x] Панель связи перестала съедать клики по тому, что под ней лежит
- [x] Собраны недостающие иконки: favicon.ico, apple-touch-icon,
      icon-192, icon-512 — все четыре отдавали 404
- [x] Услуги разнесены по отдельным адресам: пять посадочных и два кейса.
      Текст в `tools/pages-data.js`, сборка `node tools/build-pages.js`
- [x] С главной проставлены ссылки на посадочные — из карточек услуг,
      прайса и портфолио. Без них поисковик новые страницы не найдёт
- [x] Карта сайта собирается автоматически, дубли `sitemap.xml` и
      `site.webmanifest` в корне удалены: сервер отдаёт только `public/`

---

## Сделано 9 августа: сайт стал многостраничным

- [x] 20 новых страниц: 7 услуг, 2 кейса, 2 гео, блог и 4 статьи, о студии, контакты, 404
- [x] Главная превращена в хаб: меню и карточки ведут на страницы услуг
- [x] Сборка страниц из `src/` — общие шапка, подвал и форма в одном месте
- [x] sitemap.xml и llms.txt собираются автоматически, разойтись с сайтом не могут
- [x] Разметка: `WebPage`, `BreadcrumbList`, `Service` + `Offer`, `FAQPage`, `BlogPosting`, `Person` (основатель), даты публикации и правки
- [x] Карточка Яндекс Карт связана с сайтом через `sameAs` и `hasMap`
- [x] Меню на телефоне (бургер) — до этого с телефона в разделы попасть было нельзя
- [x] Форма: сообщение «Заявка принята» вынесено наружу формы — раньше оно пряталось вместе с ней, и человек не видел подтверждения
- [x] Плашка о куках опущена под шапку — она перехватывала нажатия по кнопке меню
- [x] Плашка о куках добавлена на внутренние страницы: люди приходят из поиска сразу туда
- [x] `favicon.ico` и `apple-touch-icon.png` давали 404 на каждой странице — заменены на SVG-значок и `logo-mark.png`
- [x] `privacy.html` был без `<!DOCTYPE>`, `<html lang="ru">`, `description` и `canonical` — исправлено
- [x] Логотип первого экрана грузился лениво (это портит показатель отрисовки) — исправлено
- [x] Фото в подсказке получило описание для экранного диктора
- [x] `meta keywords` убран — поиск не учитывает его с 2009 года
- [x] Описание главной сокращено со 187 до 139 символов
- [x] Сторож в `api/brief.js`: если `LLM_BASE_URL` уводит диалоги за границу, сервер пишет предупреждение — политика заявляет обработку в России

---

## Сделано 10 августа: город в заявке и подсказка региона

- [x] **Найдена и исправлена потеря заявок.** Защита от спама считала
      заявки по IP-адресу, а за прокси Amvera адрес у всех посетителей
      один. Лимит «5 заявок за 10 минут» действовал не на человека,
      а на весь сайт: шестой клиент за десять минут получал отказ
      и уходил. В журнале при этом ничего — заявка просто не приходила.
      Теперь считаем по контакту: один телефон или ник — три заявки
      за десять минут, разные люди друг другу не мешают.

- [x] Поле «Ваш город» в форме: подсказки из `src/cities.js`, необязательное, с подписью зачем
- [x] Подсказка региона по IP — `/api/geo` плюс `public/assets/js/geo.js`
- [x] В заявке видно, вписал город человек или определил сервис: заявка «Краснодар» без подтверждения — догадка, а не адрес клиента
- [x] Регионы отдельной колонкой в подвале: на каждой странице сайта, КМВ первым
- [x] Раздел 10 в политике конфиденциальности — про определение региона

---

## Сделано 10 августа: бриф из чата приведён к уровню заявок с формы

- [x] **Бриф из чата теперь пишется на диск раньше отправки.** Заявки
      с формы получили эту защиту после сбоя Telegram 3 августа, брифы
      из чата — нет. Разговор с человеком, который уже назвал телефон,
      пропадал бесследно, если Telegram и ВК не отвечали одновременно.
- [x] **У брифа появился номер** из того же счётчика, что у заявок
      с формы. Без него подсчёт «сколько пришло за месяц» не видел
      чат вовсе.
- [x] **Вместо сырого разговора приходит свод по полям:** ниша, задача,
      что не так, каналы, город, срок, бюджет, подходящая позиция прайса
      и главный вопрос, который надо задать первым. Диалог прикладывается
      ниже — свод помогает решить, звонить ли сейчас, подробности иногда
      всё равно нужны.
- [x] Свод составляет та же модель отдельным запросом, с железным
      правилом ничего не додумывать: чего не было в разговоре — «не назван».
      Свод с выдуманным сроком и бюджетом хуже отсутствия свода.
- [x] Ожидание свода ограничено восемью секундами: зависшая модель
      не должна задерживать доставку брифа.

---

## Сделано 10 августа: сводка по заявкам

Просьба была ещё в начале: «бот нумерует заявки, регистрирует по городам
и собирает дашборд по городам, или по виду услуг». Нумерация и город
в заявке появились, а сводки не было — выгрузка отдавала просто список.

- [x] `/api/leads?key=…` теперь отдаёт блок `svodka`: по городам, по услугам
      и пакетам, по каналу (форма или чат), по рекламным меткам, по месяцам.
- [x] Город, определённый по IP и не подтверждённый человеком, стоит
      в сводке ОТДЕЛЬНОЙ строкой с пометкой. Иначе догадка сервиса встала
      бы рядом с ответом клиента, и по ней делили бы рекламный бюджет.
- [x] Регистр приводится: «краснодар» и «Краснодар» — один город.
      А «Мин-Воды» и «Минеральные Воды» не сводим: свести их автоматически
      нельзя, не начав угадывать, — человек различит их глазами.
- [ ] Задать `LEADS_KEY` в переменных Amvera, иначе адрес отвечает 404.

---

## Сделано 10 августа: услуга «Интеграция Яндекс Карт»

- [x] Модуль ленивой карты `public/assets/js/ymap.js`: до блока не долистали —
      не грузится ничего, долистали — сначала лёгкая картинка, потом карта.
- [x] Схема карты перекрашивается под палитру сайта, метка — свой DOM-элемент
      в фирменных цветах, а не синяя капля Яндекса. Именно это отличает
      встроенную карту от вставленного виджета.
- [x] Блок на `/kontakty/` как витрина услуги: клиенту проще показать,
      чем объяснить словами.
- [x] Без ключей блок показывает адрес и ссылку на карточку — ровно то,
      что было раньше. Проверено: ни одного запроса к Яндексу.
- [x] Цена «от 15 000 ₽» в прайсе, услугах, `llms.txt` и промпте бота.
- [x] Карты добавлены строкой в каждый пакет, отдельной линейки нет.

### Что нужно от владелицы, чтобы карта заработала

- [ ] Кабинет разработчика `developer.tech.yandex.ru`: подключить
      **JavaScript API и HTTP Геокодер** и **Static API** — это ДВА разных
      ключа, у каждого свои лимиты.
- [ ] **Ограничить каждый ключ доменом** `vibe-digital999.ru`. Ключи публичные,
      они видны в коде страницы; без ограничения ими воспользуется кто угодно,
      а счёт придёт вам.
- [ ] Посмотреть в кабинете реальные лимиты и тариф. Не продавать услугу
      клиентам, пока не увидели цифры своими глазами: продать карту,
      а через месяц узнать, что за неё кто-то должен платить, — худший
      способ закончить проект.
- [ ] Прислать ключи — я впишу их в `src/pages.js`, поле `MAPS`.
- [ ] **Прочитать условия бесплатного использования до продажи услуги.**
      Ключевое, что уже учтено в коде: копирайт, логотип Яндекса и кнопка
      «Открыть в Картах» должны быть видны всегда, на интерактивной карте
      и на статичной. За скрытие отключают ключ без права восстановления.
      Ещё три условия касаются продажи клиентам: бесплатной версией
      нельзя пользоваться в решении, которое разрабатывается на продажу;
      нельзя сохранять данные от API к себе в базу; нельзя следить
      за транспортом в реальном времени. Если делаем карту клиенту
      за деньги — ключ оформляется на клиента и, возможно, платный.
      Это надо выяснить в поддержке Яндекса до первой продажи.
- [ ] **Сверить координаты** офиса с карточкой организации. Сейчас в `MAPS`
      стоит центр Лермонтова: город человек поймёт, но метка встанет
      не на дом. Точные координаты видны в адресной строке при открытии
      карточки на Картах.

---

## 11 августа

**Цель дня:** запустить бота ВКонтакте и прекратить бардак с правилами —
каждый новый чат начинал с нуля и переспрашивал то, что давно решено.

**Сделано:**

- [x] Правило «первым делом найти свежие правила» в `CLAUDE.md`: у проекта
      восемь веток и восемь разных версий правил, в `main` самая старая
      (69 строк). Теперь чат сам ищет, где правила правили последними.
- [x] Правило «конец беседы — запись в журнал»: шаблон записи, правила
      галочки, связка с календарём.
- [x] Образец одного шага в `CLAUDE.md`: где → что → что увидим → что дальше.
- [x] Порядок выкатки записан: сначала `fetch`, потом `push` в `amvera`.
      Имя подключения `amvera` подтверждено, спрашивать больше не нужно.
- [x] Четыре ловушки Amvera записаны: переменные читаются только при старте,
      IPv6 не работает, настоящий IP посетителя не доходит, `>` в PowerShell
      ломает файл.
- [x] Из `TASKS.md` убрана устаревшая команда выкатки `push amvera main:master`.
      Она же исправлена в описании напоминания в календаре.
- [x] Реклама Яндекс Бизнеса метку всё-таки ставит — `utm_referer=geoadv_…`,
      видно в Метрике за 6–9 августа. Заявки теперь её читают. Комментарии
      в коде, утверждавшие обратное, исправлены.
- [x] В сообществе ВК `club240607590` включены «Сообщения сообщества»
      и «Возможности ботов».
- [x] Календарь: напоминание «Выложить страницы услуг на Amvera» закрыто
      галочкой ✔, заведено новое — «Бот ВКонтакте, доделать настройку»,
      повтор ежедневно до 22 августа.

**Остановились на:** настройке бота ВК. Владелица прошла два шага из шести,
следующий — кнопка «Начать» в «Настройки для бота» и «Сохранить». Дальше
по списку в описании напоминания в календаре. Отдельно ждём её решения:
переносить ли правила и журнал в `main`.

**Ждём от Гульнары:**

- [ ] Кнопка «Начать» в настройках бота ВК + «Сохранить».
- [ ] Long Poll API версии 5.199 и тип события «Входящее сообщение».
- [ ] Ключ доступа сообщества с правом «сообщения сообщества».
- [ ] `VK_TOKEN` и `VK_GROUP_ID` в переменные Amvera, потом перезапуск.
- [ ] Написать сообществу с личной страницы — иначе ВК отдаёт ошибку 901.
- [ ] Проверка: написать боту в ВК и получить ответ за несколько секунд.
      Пока не сделано, в логах при старте строка
      `[vk-bot] Не настроен (нет VK_TOKEN или VK_GROUP_ID) — пропускаем`.
- [ ] Разрешение перенести правила и журнал в `main`.

- [x] **Правила, карточка проекта и журнал перенесены в `main`** — с разрешения
      владелицы. Только текстовые файлы: `CLAUDE.md`, `STATUS.md`, `TASKS.md`,
      `README.md`, `docs/01`, `docs/02`. Код не тронут. Теперь чат, начатый
      в `main`, читает актуальные правила сразу.
- [x] `STATUS.md` из `main` сохранён и дополнен: имя remote `amvera`
      подтверждено, указано, что на живом сайте ветка `multipage-website-okje3k`,
      строка `ipv4first` в `server.js` уже стоит.
- [x] Роли трёх файлов памяти разведены в `CLAUDE.md`, чтобы их снова
      не начали дублировать друг в друга.
- [x] `README.md` описывает реальную структуру: `src/` и `build.js`.
      Раньше не упоминал ни того, ни другого.

- [x] **Правило журнала подкреплено двумя хуками** — `.claude/hooks/`.
      Текстовое правило можно не выполнить, и его не выполняли.
      `session-start.sh` печатает памятку в начало каждой беседы независимо
      от ветки. `stop-journal.sh` не даёт закончить беседу, если файлы
      правили, а `TASKS.md` — нет. Напоминает один раз. Проверено: молчит,
      когда журнал дописан, и когда не менялось ничего.
- [x] Записано правило «собранные страницы в `public/` не удалять».
      Причина: `prestart` стоит с `|| true`, сломанная сборка проходит молча,
      и закоммиченные страницы — единственная страховка сайта.

**Согласовано со вторым чатом (ветка `vibe-digital-site-improvements-y7t2sw`):**

Он переносит свою работу на правильную основу. Условия, чтобы не потереть
друг друга:

- [ ] Базу берёт от `claude/multipage-website-okje3k`, но работает **в своей
      ветке**: в живой ветке идёт работа по боту ВК и картам.
- [ ] Удаляет `tools/build-pages.js` и `tools/pages-data.js`.
- [ ] **`public/uslugi/` и `public/portfolio/` НЕ удаляет** — см. правило выше.
      Сборка перезапишет их сама.
- [ ] Переносит в `public/index.html` первый экран, правки честности, четыре
      иконки и снятый перехват кликов панелью связи. Меню и подвал правит
      в двух местах: `src/partials/` и `public/index.html`.
- [ ] Тексты страниц услуг сравнивает с `src/pages/` и берёт лучшее,
      не затирая целиком.
- [ ] Выкатку не делает. Сливаем в живую ветку, потом один выкат.

**Не доделано:**

- [ ] **В `main` лежит код второй системы сборки — папка `tools/`,
      и нет папки `src/`.** Текст поправлен, код нет: разрешения трогать
      код не было. Пока `main` не приведён в порядок — **не выкатывать его
      на сайт**: пропадут блог, гео-страницы, `/pakety/`, `/o-nas/`,
      `/kontakty/` и 404. Решение за владелицей.
- [ ] **Телеграм-бот отложен.** Код `api/tg-bot.js` готов и ждёт
      `TG_BOT_TOKEN`, но Телеграм у владелицы не открывается, а токен
      берётся только у @BotFather внутри Телеграма. Вернуться, когда
      заработает.

---

## 11 августа, вторая половина дня — ветка `claude/vk-bot-setup-vuryya`

Продолжение записи выше: там бот ВК остановился на двух шагах из шести,
здесь он доведён до работающего состояния и проверен живым диалогом.

**Цель дня:** запустить бота ВКонтакте до первой дошедшей заявки
и убедиться, что контакт клиента реально доходит до владелицы.

**Сделано — бот ВКонтакте:**

- [x] **Бот отвечает в сообществе `club240607590`.** Проверка: написали
      боту «сколько стоит сайт с ботом», ответ пришёл за пару секунд,
      цена совпала с прайсом. Второй и третий вопросы («а лендинг»,
      «а многостраничник») он ответил, помня предыдущие, — память диалога
      работает.
- [x] **Путь заявки проверен целиком.** Написали боту «нужен сайт
      для гостиницы, 6 страниц, мой номер +7 999 244 9999» — бриф № 4
      пришёл в ВК и на почту. Это главная проверка дня.
      **Это НАША ПРОВЕРКА, а не клиент:** сообщение отправила сама
      владелица со своего номера. Живых заявок из бота на 11.08 не было
      ни одной. В статистику, в кейсы и в разговоры с клиентами эту
      цифру не ставить — тестовая заявка, посчитанная как настоящая,
      портит все выводы, которые из неё потом делают.
- [x] **Long Poll включён.** В логе при старте строка
      `[vk-bot] Long Poll включён, подписка на входящие сообщения есть`,
      следом `[vk-bot] Подключился к сообществу 240607590`.
- [x] **Long Poll включает сам код** — `ensureLongPollSettings`
      в `api/vk-bot.js`, метод `groups.setLongPollSettings`. Раздел
      «Работа с API» ВК перенёс в интерфейсе, найти его не удалось ни
      по одному пути. Требует у ключа доступа права **«Управление
      сообществом»** — оно у текущего ключа есть, строка в логе это
      подтверждает. При переезде на другое сообщество достаточно
      поменять `VK_TOKEN` и `VK_GROUP_ID`, в интерфейс лезть не нужно.
- [x] **Почта третьим каналом заявок** — `api/mail.js`, SMTP Яндекса,
      данные не уезжают за границу. Проверка: письмо с брифом № 4
      пришло на `gulnaravibecoder999@yandex.ru`.
- [x] **Нумерация заявок сброшена после проверок.** В логе
      `[lead] Нумерация сброшена: следующая заявка получит № 1`.
      Прежний журнал не удалён, а переименован в `leads-do-<дата>.jsonl`.

**Что именно нажималось в ВК** (порядок важен, следующий пункт
не появляется, пока не сделан предыдущий):

1. Управление → Сообщения → включить «Сообщения сообщества».
2. Управление → Сообщения → **Настройки для бота** →
   «Возможности ботов» в положение «Включены» → **«Сохранить»**.
   Без «Сохранить» ВК отвечает ошибкой 912.
3. Там же галочка «Добавить кнопку „Начать“» → снова «Сохранить».
   Галочку «Разрешать добавлять сообщество в чаты» НЕ ставили:
   бот рассчитан на личную переписку.
4. Ключ доступа: Управление → Работа с API → Ключи доступа →
   Создать ключ, отмечены **«Сообщения сообщества»** и **«Управление
   сообществом»**. Ключ показывается один раз.
5. Long Poll руками включить не удалось — раздела в интерфейсе не нашли.
   Включили запросом в браузере:
   `https://api.vk.com/method/groups.setLongPollSettings?v=5.199&group_id=240607590&enabled=1&api_version=5.199&message_new=1&access_token=КЛЮЧ`
   Ответ `{"response":1}`. Дальше это делает код сам.

**Переменные Amvera, добавленные сегодня:**
`VK_TOKEN`, `VK_GROUP_ID=240607590`, `MAIL_TO`, `SMTP_USER`, `SMTP_PASS`
(пароль приложения с id.yandex.ru, обычный пароль Яндекс не принимает),
`RESET_LEADS` — разовая, её пора убрать.

**Сделано — Яндекс.Метрика, счётчик `110416869`:**

- [x] **Удалены шесть дублей целей.** Утром завели второй комплект целей,
      не увидев, что они уже есть, — запись об этом лежала ниже по журналу.
      Удалены по ID: `595620063`, `595623039`, `595629857`, `595634029`,
      `595637213`, `595640693`.
- [x] **Осталось шесть рабочих целей**, тип «Целевое событие · ex
      JS-событие». **Перед заведением новой цели сверяться с этим списком:**

      lead        — 595153462   Любое обращение (зонтичная)
      lead_sent   — 591631859   Заявка отправлена
      click_tel   — 591634819   Клик по телефону
      click_wa    — 595153604   Клик по WhatsApp
      click_tg    — 595153918   Клик по Telegram
      click_vk    — 595154059   Клик по ВКонтакте

- [x] **Записан полный список из 20 событий**, которые сайт реально
      отправляет — сверено по коду, раздел «Очередь», пункт 1. У каждого
      помечено, нужна ли цель. Прежняя таблица была неполной и частично
      не совпадала с кодом: по ней можно было завести цель на имя,
      которого сайт не шлёт, и месяц ждать данных.
- [x] **Записан замер до целей** — раздел «Очередь», пункт 1.1. Снят
      за 6–9 августа, то есть по одностраничной версии сайта.

Фильтры в Метрике сегодня **не трогали** — ни один не заведён и не изменён.

**Сделано — три поломки, которые молча теряли заявки:**

- [x] **Повторное обращение того же человека не создавало заявку никогда.**
      Защита от спама помнила ключ диалога и не забывала. Клиент оставил
      телефон в понедельник, вернулся в пятницу с новым заказом — второй
      бриф не приходил. Теперь помним контакт и время: тот же контакт
      в пределах шести часов — повтор, другой контакт или другой день —
      новая заявка. Проверено четырьмя случаями локально.
- [x] **Пропуск не писался в журнал.** Молчаливый выход неотличим
      от поломки: заявки нет, в логах пусто. На поиске несуществующей
      ошибки в доставке ушёл час. Теперь пишется строка
      `[brief] Повтор, бриф уже отправляли`.
- [ ] **Бот выдумывал каналы связи в брифе.** В брифе № 4 стояло
      «Каналы: Telegram, WhatsApp, сайт», хотя клиент не называл ни одного:
      перечисление примеров в промпте модель приняла за список для
      заполнения. Формулировка исправлена, **но новый бриф после правки
      ещё не приходил** — проверить на следующей живой заявке.
- [ ] **Расшифровка кодов ошибок ВК** (912, 15, 27, 100 про longpoll)
      в логах Amvera. Код выкачен, но ни одна из этих ошибок после
      выкатки не возникала — увидим при следующей поломке.

**Остановились на:** сверке кода с документацией. Читая `docs/12-JARVIS-TELEGRAM-VK.md`
по просьбе владелицы «выполни правила», нашли два невыполненных
предписания этого документа — голосовые в ВК и дата в брифе, оба
описаны ниже в «Не доделано». Правки не начаты, файлы не тронуты.

Следующий шаг для чужого чата: реализовать чтение `transcript`
у голосовых в `api/vk-bot.js` (сейчас `handleMessage` берёт только
`message.text` и на голосовое выходит молча) и добавить дату первой
строкой в бриф `api/brief.js`.

**Ждём от Гульнары:**

- [ ] Убрать переменную `RESET_LEADS` из Amvera — она отработала.
      Если забыть, ничего не сломается: повторно не срабатывает, метка
      лежит на диске. Просто мусор в настройках.
- [ ] Цель `lead` (`595153462`): условие стоит «идентификатор **содержит**»,
      надо «совпадает». Сейчас она срабатывает и на `lead`, и на `lead_sent` —
      в имени второго тоже есть «lead». Конверсии не задваиваются
      (Метрика считает визиты), но отчёт «Достижения целей» завышен вдвое.
- [ ] Фильтр своих визитов в настройках счётчика: за 6–9 августа
      41 просмотр с `?_ym_debug=1` — это отладочные заходы.
- [ ] Вебмастер → Индексирование → Переобход страниц: отправить адреса
      новых страниц, лимит около 20 в день. Sitemap готов, 25 адресов,
      `robots.txt` на него ссылается.
- [ ] Решение по `main`: там лежит вторая система сборки `tools/` и нет
      `src/`. Пока не приведён в порядок — не выкатывать.

**Не доделано в коде:**

- [ ] **Голосовые сообщения в ВК не читаются.** `docs/12` пишет: ВК сам
      расшифровывает голосовое и кладёт текст в `transcript` объекта
      `audio_message` при `transcript_state: "done"` — бесплатно и уже
      готово. В `api/vk-bot.js` этого нет: `handleMessage` берёт только
      `message.text`, а на пустом тексте выходит молча. Человек присылает
      голосовое — бот не отвечает вообще.
- [ ] **В брифе нет даты и времени.** `docs/12`, раздел 5, прямо требует:
      «Ставить дату и время первой строкой» — владелец открывает письмо
      и не понимает, свежая заявка или недельной давности. В `api/brief.js`
      дата пишется только в файл на диске (`at`), в текст брифа не попадает.
      Видно на брифе № 4: заголовок, сайт, контакт — даты нет.
- [ ] **`CLAUDE.md` и `docs/01-ARCHITECTURE.md` разошлись с сайтом.**
      В карте сайта нет четырёх страниц: `/pakety/`, `/kmv/`,
      `/krasnodar-krai/`, `/krasnoyarsk-krai/`. В списке папок, которые
      нельзя править руками, нет `public/pakety/`, `public/kmv/`,
      `public/krasnodar-krai/`, `public/krasnoyarsk-krai/` — то есть ровно
      та ловушка, от которой этот список и защищает. Правки не внесены:
      беседа остановлена на записи журнала.
- [ ] **`api/vk.js` описывает настройку ВК по-старому** — как ручные шаги
      в интерфейсе. С сегодняшнего дня Long Poll включает код.

---

## 11 августа, вечер — три ветки сведены в одну

**Цель дня:** собрать работу трёх чатов в одну ветку и выкатить один раз,
а не три, чтобы ветки перестали затирать друг друга.

**Сделано:**

- [x] В `claude/multipage-website-okje3k` влиты обе рабочие ветки:
      `vk-bot-setup-vuryya` (бот ВК, почта, три починенных потери заявок)
      и `vibe-digital-site-improvements-y7t2sw` (новый первый экран,
      кейс автошколы, иконки, удалённая дубль-сборка `tools/`).
- [x] Конфликты в `STATUS.md` и `TASKS.md` разобраны руками: записи всех
      трёх чатов сохранены, ничья работа не затёрта.
- [x] **Проверено, что работа предыдущих чатов цела**, а не со слов:
      `utm_referer`, `SUMMARY_SYSTEM`, `city_source`, `package_parts`,
      `src/pages/pakety.html`, `public/assets/js/ymap.js`, `api/tg-bot.js` —
      всё на месте после обоих слияний.
- [x] Сборка после слияния прошла: 26 страниц, проверка мета-тегов чистая.
- [x] Сайт поднят локально, десять адресов отвечают 200: главная, пакеты,
      услуги, портфолио с новым кейсом автошколы, контакты, блог,
      гео-страница, 404, `sitemap.xml`, `llms.txt`.
- [x] Проверена новая зависимость `nodemailer` — в `package.json` есть,
      версия 9.0.5 ставится. Без этой проверки Amvera упала бы на установке,
      а `prestart` с `|| true` скрыл бы причину.

- [x] **Хук журнала проверен на живой беседе:** после правки скрипта, но без
      записи в `TASKS.md`, чат не смог закончить — хук перечислил изменённые
      файлы и потребовал запись. Работает не в теории.
- [x] Поправлена памятка в `session-start.sh`: строка про живую ветку
      обрезается по первому предложению. До этого в памятку попадал обрывок
      «…Выкатана» без окончания — фраза в `STATUS.md` идёт на две строки.

- [x] **Исправлен факт в кейсе автошколы: филиалов три, а не два.**
      Лермонтов, Пятигорск и станица Суворовская — теперь названы поимённо
      в кейсе, в портфолио, на главной, в заголовке и описании страницы.
      Названия городов заодно работают на гео-запросы.

- [x] **Второй аудит проверен по коду.** Три из пяти замечаний не подтвердились
      на текущей версии: «92 КБ против 65 КБ» — цифры 92 на сайте нет вовсе,
      65 КБ стоит в двух местах и означает одно и то же, а 70 КБ найдено
      в HTML-комментарии и посетителю не видно; номер `02` в блоке «Не верьте
      на слово» на месте, все четыре идут подряд; цена ИИ-агента везде 60 000 ₽ —
      на главной, в `llms.txt`, в разметке `Offer` и в промпте бота, 40 000 ₽
      не встречается нигде. Разметка `Service` + `Offer` + `FAQPage` на страницах
      услуг тоже уже стоит.
- [x] **Одно замечание было верным и закрыто:** блок «что входит / что считается
      отдельно» был только у ИИ-агента. Добавлен на страницу разработки сайтов —
      там позиции самые дорогие. Всё перечисленное взято из смет и с других
      страниц сайта, ничего не выдумано.

- [x] **В блок «что считается отдельно» добавлены генерация картинок и видео
      нейросетями.** Три причины: подписка и обращения — повторяющийся расход,
      который не кончается со сдачей; условия коммерческого использования
      у каждого сервиса свои; сгенерированное нельзя ставить вместо фотографии
      реального помещения или сотрудников.

- [x] **Выкатано** — `c386aaa`, ответ Amvera `162402d..c386aaa -> master`.
      Права, сопровождение и правка про генерацию картинок на сайте.

- [x] **Написан `docs/13-MNOGOSTRANICHNIK.md`** — всё про многостраничник
      в одном файле: порядок добавления страницы, что сборка проверяет сама,
      требования к странице, почему филиалы одного клиента НЕ делают
      отдельными страницами, а гео-страницы наоборот делают, относительные
      адреса для переобхода в Вебмастере, что смотреть в Метрике и база
      6–9 августа для сравнения. Раньше это лежало в головах и в пяти
      разных документах.

- [x] **Написан `docs/14-KLIENTSKIY-SAYT-FILIALY.md`** — переносимый чек-лист
      для клиентских сайтов. Повод: третий чат делает многостраничник
      автошколе, а `docs/13` про наш репозиторий — там наши пути, наш
      счётчик Метрики и наша база 6–9 августа. В новом файле только то,
      что верно для любого клиента: филиал как переключатель, а не три
      копии сайта; когда отдельная страница филиала всё-таки нужна;
      `LocalBusiness` на каждый адрес; карточка Яндекс Бизнеса на каждый
      филиал со ссылкой на свою страницу; счётчик Метрики ставится
      КЛИЕНТА, не наш; чего клиенту обещать нельзя.

- [x] **`docs/14` дополнен разделом про 152-ФЗ и хранилище.** Повод: сайт
      автошколы строят на Supabase, а его серверы за границей. Персональные
      данные россиян по ст. 18 ч. 5 должны первично записываться в базу
      в России. Записаны три рабочих варианта, что нужно в любом из них
      и правило: клиент настаивает на своём — предупредить ПИСЬМЕННО.
- [x] **`docs/14` переписан под любую нишу, а не под автошколу.** Клиенты
      будут разные — парикмахерская, шашлычная, стоматология, гостевой дом.
      Разделы сайта у них одни и те же, меняется наполнение: таблица
      «что такое расписание в вашей нише» вместо автошкольных категорий.

- [x] **В `docs/14` добавлен раздел про Amvera для клиентских проектов** —
      порядок выкатки, `amvera.yaml`, почему собранную папку держат
      в репозитории, переменные только при старте, IPv6, недоступный IP
      посетителя. От сборки клиента это не зависит.

**Ждём решения владелицы по сайту автошколы:**

- [ ] **Персональные данные уходят в Supabase, за границу — всеми путями.**
      Проверено третьим чатом: форма-заявка, Telegram-бот, бот ВК,
      разбор почты и ручной ввод — всё пишется в `applications`, плюс
      переписка в `vk_sessions`. Первичная запись, а не копия — это ровно
      то, что запрещает ст. 18 ч. 5.
- [ ] Включить `PD_STORE=off` на сайте школы — заявки в мессенджеры
      и на почту, в базу не пишутся. Половина уже построена.
- [ ] Решить про кабинет: ему база нужна, значит Postgres в России.
- [ ] **Отдельно: что делать с данными, которые в Supabase УЖЕ лежат.**
      Выключить запись — не то же самое, что убрать накопленное.

- [x] **Два источника правды сведены в один.** `docs/14` и навык
      `mnogostranichnik-ru` описывали одно и то же — ровно то, что правила
      запрещают. Раздел про Amvera для клиентских проектов перенесён
      в навык, `docs/14` стал указателем: файл не удалён, потому что
      ссылка на него уже ушла в переписку.

**Остановились на:** дне закончили сознательно, наплыва клиентов не ждём —
бот запущен несколько часов назад. Выкатка прошла, но страницу `/pakety/`
глазами на живом домене ещё не смотрели.

**Первое дело следующего чата:** открыть `https://vibe-digital999.ru/pakety/`,
найти раздел «Сопровождение», проверить цены 5 000 / 8 000 / 15 000 ₽
и строку «Круглосуточного дежурства у нас нет». Увидели — поставить здесь
галочку. Не увидели — Amvera не дособрала, повторить выкатку.

**Ждём от Гульнары:**

- [x] Выкатить `claude/multipage-website-okje3k` на Amvera и проверить сайт.
      Сделано 11.08 вечером, коммит `162402d`. Проверено на живом домене:
      кейс автошколы открывается с тремя филиалами.
- [ ] `SMTP`-переменные и `LEADS_KEY` в Amvera — см. записи выше.
- [ ] **Проверить, что в договоре написано то же самое про переход прав:**
      исключительные права переходят заказчику после полной оплаты.
      На сайте это теперь написано в пяти местах. Если в договоре формулировки
      нет или она другая — в споре сошлются именно на расхождение.
- [x] **Цена сопровождения названа владелицей и проставлена везде:**
      «Старт» 5 000 ₽ / 1 час, «Поток» 8 000 ₽ / 2 часа, «Система» 15 000 ₽ /
      3 часа. Стоит в пакетах, в прайсе на главной, в `llms.txt` и в промпте
      бота — расхождений между этими местами быть не должно.
- [ ] **Настроить резервное копирование до первой продажи сопровождения.**
      На странице обещана копия раз в неделю, хранится отдельно от сайта.
      Сейчас этого нет. Обещание без исполнения — худшее, что может быть
      в абонентской услуге: за него платят каждый месяц.
- [ ] **Дописать сопровождение в договор:** состав, часы, что часы не
      переносятся, график будни 9:00–18:00, отсутствие круглосуточного
      дежурства. На сайте это написано, в договоре пока нет.

**Не доделано:**

- [ ] **Плашка о куках на ноутбуке 720px** перекрывает верхние 53px
      заголовка. На телефоне вылечено переносом плашки вниз, на широких
      экранах не тронуто — чужая правка со своей причиной.
- [x] После выкатки обновлена в `STATUS.md` строка про живую ветку и коммит.

---

## Проверить и сделать руками — это не в коде

### 0. Включить определение региона — один шаг

Определение города по IP написано и работает, но выключено: нужен
бесплатный ключ. Пока ключа нет, сайт работает как раньше — просто без
подсказки «похоже, вы из Краснодарского края».

- [x] Регистрация на `dadata.ru`, бесплатный тариф — 10 000 запросов в сутки.
- [x] `DADATA_TOKEN` добавлен в переменные окружения Amvera.
- [x] Исправлено определение адреса за прокси: первая версия брала первую
      запись из `X-Forwarded-For`, и на Amvera это давало «локальный адрес»
      каждому живому человеку. Теперь перебираются все известные заголовки
      и берётся первый не внутренний адрес, плюс `app.set('trust proxy')`.
- [x] Проверено на живом сайте: **не работает, и не по нашей вине.**
      В журнале Amvera:

      ```
      x-forwarded-for[0]: 10.*.*.* — внутренний, отброшен
      x-real-ip:          10.*.*.* — внутренний, отброшен
      req.ip:             10.*.*.* — внутренний, отброшен
      ```

      Прокси Amvera затирает адрес посетителя своим внутренним вместо
      того, чтобы дописать его в цепочку X-Forwarded-For. Настоящий адрес
      до приложения не доходит, и определить регион по нему невозможно
      никаким кодом на нашей стороне.

      Код при этом рабочий и остаётся в проекте: заработает сам, без
      единой правки, как только заголовок начнёт приходить. Сайт от этого
      не страдает — подсказки «похоже, вы из Краснодарского края» просто
      нет, а выбор региона руками на главной работает как работал.

- [ ] **Написать в поддержку Amvera** (чат поддержки в левом меню кабинета):
      «Приложение vibe-digital за вашим прокси получает в X-Forwarded-For
      и X-Real-IP внутренний адрес 10.x.x.x вместо адреса посетителя.
      Можно ли включить передачу настоящего IP клиента?»
      Это обычная настройка ingress, у большинства хостингов она есть.

- [ ] Если поддержка не сможет — запасной путь: спрашивать DaData прямо
      из браузера. Тогда сервис видит адрес посетителя сам, и прокси
      не мешает. Цена вопроса: ключ окажется в коде страницы, поэтому
      его придётся ограничить доменом в кабинете DaData
      («Настройки» → ограничение по домену). Данные всё так же остаются
      в России. Делать только после ответа поддержки — сначала простой
      путь, потом сложный.

Почему DaData, а не зарубежный сервис: IP-адрес посетителя — персональные
данные, и отправлять его за границу нельзя без отдельного основания.
DaData российская, данные остаются в России.

### 0.5. Метрика — сделано 10 августа

- [x] Счётчик 110416869 переименован и переведён с адреса Amvera
      на `vibe-digital999.ru`: отчёты и связка с Яндекс Бизнесом
      смотрели не на тот сайт.
- [x] Шесть целей типа «Целевое событие»: `lead_sent`, `lead`, `click_tel`,
      `click_wa`, `click_tg`, `click_vk`. Проверено на живом коде —
      события уходят и с главной, и с внутренних страниц.
- [x] Зонтичная цель `lead` срабатывает на каждое обращение: заявку,
      звонок, переход в любой мессенджер. Автостратегии Директа и Яндекс
      Бизнеса учатся на ОДНОЙ цели — шесть редких не дадут статистики
      ни по одной, поэтому настраивать рекламу нужно на `lead`.
- [ ] Через неделю посмотреть отчёт «Конверсии»: если `lead` показывает
      ноль при идущей рекламе, проблема не в целях, а в том, что люди
      уходят со страницы, — тогда смотреть Вебвизор.

Автоцели Яндекса (отправка формы, переход в мессенджер, клик по номеру,
цели Яндекс Бизнеса) созданы платформой сами. Их не трогаем: считают
то же самое своим способом и служат перекрёстной проверкой.

### 1. Карточка в Яндекс Картах — самый быстрый канал

`https://yandex.ru/maps/org/vibe_dizhital_999/172151749064/`

Карточка не ждёт индексации: она может дать заявки в этом месяце, пока
новые страницы только заходят в поиск.

- [x] Название карточки решено оставить «Вайб Дижитал 999» — решение владелицы:
      написание отличает студию от других, домен искали долго. Вместо
      переименования карточки под сайт сайт подстроен под карточку:
      в разметке главной оба русских написания перечислены в `alternateName`,
      поэтому связь «организация ↔ сайт» не рвётся.
      Ключевые слова в название по-прежнему не добавлять — за это блокируют.
- [x] Ссылка в карточке исправлена с `http://` на `https://vibe-digital999.ru/`:
      реклама идёт с карточки, и каждый переход по http — лишнее
      перенаправление, на котором теряется часть кликов.
- [ ] Заполнить «Товары и услуги» с ценами: это отдельный источник показов,
      не зависящий от сайта. Цены брать из `/uslugi/`.
- [ ] Загрузить 10–15 настоящих фотографий: рабочее место, экраны работ, вы сами.
- [ ] Добавить категории: «Разработка ПО», «Веб-студия», «Реклама в интернете».
- [ ] Попросить отзывы у тех, кому уже сдали работу. Цель на месяц — 10.
      Не покупать и не публиковать однотипные: для молодой карточки
      естественная разнородность важнее быстрого числа. Отвечать на все.
- [ ] 2ГИС: проверить и заполнить карточку. В регионах им пользуются часто.

### 2. Отправить новые адреса на переобход

- [x] Права в Яндекс Вебмастере подтверждены 10 августа, мета-тегом.
      Код лежит в `src/pages.js`, поле `VERIFY.yandex`. Удалять его нельзя:
      Яндекс перепроверяет права и снимает их, если тег пропал.
- [x] `sitemap.xml` отдан Вебмастеру, обработка занимает 1–2 недели.
- [x] Назначен переезд на https: сайт был добавлен как `http://`,
      а Яндекс считает http и https разными сайтами.
- [x] Все 25 адресов отправлены на переобход. Адреса пришлось давать
      относительными (`/pakety/`, а не полными): пока переезд на https
      не применился, Вебмастер принимает только адреса своего протокола.
- [ ] Google Search Console: подтвердить права и отправить sitemap.
      Место для кода готово — `src/pages.js`, поле `VERIFY.google`.

### 3. Проверить ИИ-брифер и политику

- [ ] Посмотреть в кабинете Amvera, задана ли переменная `LLM_BASE_URL`.
      Пустая — диалоги идут на инференс Amvera в России, политика верна.
      Если там зарубежный адрес — либо вернуть российский, либо дополнить
      раздел 5 политики и подать уведомление о трансграничной передаче
      в РКН (оно подаётся отдельно от общего, ст. 12 152-ФЗ).
      При чужом адресе сервер пишет предупреждение в журнал при запуске.

### 4. Домен vibe-digital.ru

- [ ] Не давать его в переписке и на визитках: сейчас он не открывается,
      человек упрётся в ошибку и решит, что студии нет.
- [ ] Если домен ваш — поднять с переадресацией 301 на `vibe-digital999.ru`
      (не наоборот: весь индекс и Вебмастер привязаны к 999).

### 5. Дополнить кейсы и гео цифрами

- [ ] В кейсах нет цифр результата — мы не выдумываем их без доступа
      к аналитике заказчика. Появятся подтверждённые данные — вписать.
- [ ] На `/pyatigorsk/` и `/essentuki/` честно написано, что кейсов из этих
      городов пока нет. Появится первый — заменить абзац на кейс.
- [ ] Скриншоты работ в `public/assets/`, с осмысленным `alt`
      («Лендинг для типографии Гранат, Лермонтов»), в WebP.

### 6. Внешние публикации

- [ ] 2–3 статьи на VC.ru или Habr с цифрами из кейсов. Для ИИ-поиска это
      важнее любой правки на сайте: модель проверяет, пишет ли о вас кто-то,
      кроме вас самих. Сейчас упоминания только на своём же сайте и в Картах.

---

## В работе

### Привязать домен vibe-digital999.ru

DNS-записи у reg.ru прописаны верно:

| Тип | Хост | Значение |
|-----|------|----------|
| A | @ | 158.160.116.199 |
| A | www | 158.160.116.199 |
| TXT | @ | granatgold999-vibe-digital |

Осталось дождаться обновления кэша DNS и нажать в Amvera
«Создать доменное имя» → «Свой домен». Проверка записей:

```bash
curl -s "https://dns.google/resolve?name=vibe-digital999.ru&type=TXT" | grep -o '"data":"[^"]*"'
```

---

## Очередь

### 1. Цели в Яндекс.Метрике — шесть основных заведены

Счётчик `110416869`, адрес исправлен на `vibe-digital999.ru`. Какие цели
живые и с какими ID — в записи за 11 августа, вторая половина дня.

**Перед заведением новой цели проверить, нет ли такой же.** 11 августа
шесть целей завели вторым комплектом, не увидев, что они уже есть. Две
цели с одним идентификатором засчитывают каждое событие дважды: отчёт
задваивается, а автостратегии Директа учатся на мусоре.

Тип у всех целей один — **«Целевое событие · ex JS-событие»**, раздел
«Пользовательские события». Не «Клик по кнопке» и не «Отправка формы»
из «Конверсионных действий»: то автоцели, Метрика угадывает их сама,
и они дублируют наши.

Полный список событий, которые сайт действительно отправляет — сверено
по коду 11 августа. Заводить цель на имя, которого здесь нет, бесполезно:
она навсегда останется с нулём.

| Идентификатор | Когда срабатывает | Цель заведена |
|---------------|-------------------|---------------|
| `lead` | зонтичная: любое обращение из списка ниже | да |
| `lead_sent` | заявка успешно отправлена | да |
| `click_tel` | клик по телефону | да |
| `click_wa` | клик по WhatsApp | да |
| `click_tg` | клик по Telegram | да |
| `click_vk` | клик по ВКонтакте | да |
| `click_email` | клик по почте | нет, стоит завести |
| `quiz_done` | дошёл до результата квиза | нет, стоит завести |
| `quiz_start` | открыл квиз | только вместе с `quiz_done` |
| `open_chat_strip` | открыл чат из полоски | нет |
| `open_chat_max` | развернул чат | нет |
| `click_order` | клик по кнопке «Заказать» | нет |
| `calc_152` | посчитал штраф по 152-ФЗ | не нужна |
| `speed_test` | нажал «Замерить» | не нужна |
| `case_granat` | открыл кейс Гранат | не нужна |
| `try_chat` | попробовал чат с демо-кнопки | не нужна |
| `pay_click` | клик по оплате | не нужна |
| `nudge_show` | показ подсказки | **не заводить** |
| `nudge_click` | клик по подсказке | не нужна |
| `nudge_close` | закрыл подсказку | не нужна |

`nudge_show` — это показ, а не действие человека. Цель на него станет
самой «конверсионной» на сайте и самой бесполезной.

Зонтичная `lead` отправляется автоматически вместе с любым из шести
событий обращения — список в `public/assets/js/site.js`, `LEAD_GOALS`.
Автостратегии Директа и Яндекс Бизнеса учатся на ОДНОЙ цели, поэтому
рекламу настраивать на `lead`, а не на шесть редких по отдельности.

### 1.1. Замер до целей — точка отсчёта

Снято 11 августа за период **6–9 августа**, то есть **по одностраничной
версии сайта**: многостраничник выкачен 10 августа, и глубина просмотра
за этот период к нему отношения не имеет.

| Показатель | Значение |
|---|---|
| Визиты | 441 |
| Посетители | 407 (401 новых, 34 вернувшихся) |
| Просмотры | 488 |
| Время на сайте | 37 с |
| Глубина просмотра | 1,11 |
| Отказы | 66,44% |

Источники: реклама Яндекс Бизнеса (`geoadv_yab`) — 376, ссылки на сайтах — 32,
прямые заходы — 31, мессенджеры — 1, **поиск — 1**.

Что это значит: сайт живёт на платном трафике, органики нет. Для страниц,
выкаченных 10 августа, это нормально — в поиск они попадут через одну-две
недели. Отдельно: 41 просмотр с `?_ym_debug=1` — собственные отладочные
заходы, их стоит отсечь фильтром в настройках счётчика.

Через неделю снять тот же замер и добавить конверсии по `lead`. Если `lead`
покажет ноль при идущей рекламе — дело не в целях, а в том, что люди уходят
с первого экрана; тогда смотреть Вебвизор, а не переделывать вслепую.

### 1.2. Решено не делать: доставка заявок в WhatsApp

Отклонено 11 августа владелицей. Не предлагать снова без её запроса.

- напрямую отправить нельзя, протокол закрытый — нужен посредник;
- официальный путь Meta WhatsApp Business API уводит заявку с именем
  и телефоном за границу: уведомление в РКН о трансграничной передаче,
  правка раздела 5 политики, верификация бизнес-аккаунта из России;
- российские шлюзы вроде Green API оставляют данные в РФ, но WhatsApp
  блокирует номера за автоматическую отправку — а номер
  `+7 999 244 9999` стоит на сайте и в рекламе;
- надёжности это не добавляет: Telegram и WhatsApp лежат в одном кармане.
  Это второе уведомление на тот же телефон, а не запасной канал.

Заявка уходит в Telegram, ВК, на почту и на диск. Этого достаточно.

### 2. Скриншоты работ в портфолио

Сейчас карточки текстовые. Нужно по каждому проекту:
ссылка на живой сайт, один-два скриншота, город клиента,
одна строка результата. Файлы класть в `public/assets/`.

Проекты: сайт Гранат, автошкола «Старт» (сайт и приложение),
Telegram-боты, этот сайт.

Отдельно уточнить, что умеет приложение для автошколы —
сейчас про него в карточке ни слова, а это самая сильная позиция.

### 3. ОГРНИП в подвал

На сайте granat-kmv.ru его нет, только ИНН. Для B2B полезен:
корпоративный клиент проверяет подрядчика перед оплатой.

### 4. Тест-драйв скорости, вариант Б — через Telegram

Идея: кнопка открывает `t.me/vibedigital999_bot?start=speedtest`,
человек нажимает Start, бот мгновенно отвечает с замером времени.

**Почему не сделали сразу:** бот не может написать первым тому, кто
не начинал с ним диалог. По номеру телефона или по нику Telegram
отправлять запрещает. Единственный рабочий путь — deep-link,
где человек сам нажимает Start.

**Что нужно:** бот сейчас умеет только отправлять. Для этого варианта
нужен обработчик входящих команд — отдельный сервис на Node.js
с вебхуком. Оценка: день-полтора.

**Когда делать:** после запуска рекламы, когда пойдёт трафик.
До первых посетителей тратить полтора дня рано.

### 5. QR-код для оплаты

Есть файл `IP Melkonyan GR - статический TQR-код.PDF`.

На лендинг ставить не стоит: услуги от 15 000 ₽ и «по брифу»,
цена считается под задачу. Публичный QR предлагает заплатить
непонятно сколько до разговора и сметы.

Варианты: отправлять в переписке после согласования сметы,
либо отдельной страницей `/oplata` без ссылок из меню.

Решение за владельцем.

### 6. Переадресация с vibe-digital999.online

Второй домен куплен. После привязки основного настроить
перенаправление, чтобы человек попадал на сайт с любого варианта.

---

## 11 августа, вечер: две системы сборки сведены в одну

**Цель дня:** убрать второй генератор страниц и перенести в рабочую
систему то живое, что успел сделать чат в ветке
`claude/vibe-digital-site-improvements-y7t2sw`.

**Сделано в ветке `vibe-digital-site-improvements-y7t2sw`.** Галочки не
закрыты намеренно: код написан, но на сайте этого ещё нет — ветка не
выкатана. Закрывать после выката и проверки на домене.

- [ ] Живая ветка `multipage-website-okje3k` влита в рабочую. Собранные
      страницы, карта сайта и README взяты из неё: `build.js` + `src/` —
      правильная система, второй генератор был ошибкой.
- [ ] Удалены `tools/build-pages.js` и `tools/pages-data.js` — весь второй
      генератор. Папка `public/uslugi/` и `public/portfolio/` не тронуты:
      это страховка на случай молчаливого падения сборки.
- [ ] Удалён `public/portfolio/granat/` — дубль кейса «Гранат», который
      `build.js` не перезаписывает: он не значился в `src/pages.js`.
      Две страницы про один кейс делят между собой поисковый вес.
      Настоящий кейс живёт по адресу `/portfolio/sayt-dlya-tipografii-granat/`.
      Вместе с ним ушли `public/assets/site.css` и `site.js` — стили того
      же генератора, на них больше никто не ссылался.
- [ ] Первый экран главной пересобран под заявку: маркер специализации,
      H1 «Сайты и Telegram-боты, которые приводят заявки», строка боли,
      цена и срок, одна главная кнопка, три строки доверия вместо
      счётчиков, демо «заявка в Telegram» вместо вращающейся орбиты.
      Крючки геоподмены `data-geo-*` сохранены, главная кнопка осталась
      первой в `.hero-actions` — скрипт региональной подмены ищет её так.
- [ ] Убраны два спорных абсолюта: «перестали платить — страница
      закрывается» про конструкторы и «чинить обычно нечего» про поломки.
      Правки внесены и в текст, и в разметку FAQ для поисковиков.
- [ ] Карточка 02 в блоке «Не верьте на слово» стала ссылкой. Была
      кнопкой: в разметке между 01 и 03 зияла дыра, и разбор страницы
      по ссылкам её не видел.
- [ ] Контейнер `#chat-widget` перестал перехватывать клики пустой
      областью. Он шире своих кнопок, и слева от кружков была невидимая
      полоса, которая съедала нажатия по тому, что под ней.
- [ ] Кнопка чата на телефоне уменьшена с 64 до 52px: на экране 375px
      она накрывала правый нижний угол блока доверия.
- [ ] Плашка cookie на телефоне укорочена и опущена вниз. Под шапкой она
      закрывала заголовок первого экрана целиком: шапка 80px, плашка
      215px, заголовок со 115px. Причина, по которой её убрали с самого
      верха — чтобы не перехватывала бургер, — внизу не действует.
- [ ] Добавлены `favicon.ico`, `apple-touch-icon.png`, `icon-192.png`,
      `icon-512.png`. Последние два прописаны в `site.webmanifest`
      и отдавали 404 на каждой странице.
- [ ] Новая страница `/portfolio/avtoshkola-start/` — кейс автошколы
      на два филиала. Карточка в портфолио была, разбора не было.
- [ ] Шесть вопросов дописаны в существующие страницы услуг, целиком
      ничего не переписывалось: «А если результат не понравится?»,
      «Нужен ли для бота сайт?», «А персональные данные клиентов? 152-ФЗ»,
      «С какими CRM работаете?», «Вы гарантируете позиции в Яндексе?»,
      «Чем это отличается от бесплатной проверки в сервисах?»,
      «Сколько занял проект?» в кейсе «Гранат».
- [ ] Раздел про первый экран дописан в `docs/01-ARCHITECTURE.md`.

**Поправка к записям выше по файлу.** В списке «Сделано» за 11 августа
часть строк помечена `[x]`, хотя на живом сайте их нет — ветка не
выкатана. Правило про галочку было записано в `CLAUDE.md` позже, чем
сделаны те записи. Историю не переписываю, отмечаю здесь: строки про
первый экран, спорные формулировки, карточку 02, панель связи, иконки
и разнесение услуг по адресам считать незакрытыми до выката.

**Остановились на:** всё сведено, собрано и проверено локально, ветка
запушена в GitHub. Выката НЕ было — по договорённости сливаем в живую
ветку `claude/multipage-website-okje3k` и выкатываем один раз оттуда.

**Ждём от Гульнары:**
- [ ] Слить ветку в живую и выкатить одной командой, порядок в `CLAUDE.md`.
- [ ] После выката проверить в браузере: `/portfolio/avtoshkola-start/`
      открывается, на главной с телефона виден заголовок «Сайты и
      Telegram-боты», под кнопкой три строки про ИП, исходники и ответ
      в день обращения.

**Не доделано в коде:**
- [ ] На ноутбуке с невысоким окном (720px) плашка cookie перекрывает
      верхние 53px заголовка: шапка отдаёт первый экран под неё, а первый
      экран на такой высоте поджимается вверх. На телефоне это вылечено
      переносом плашки вниз, на широких экранах решение живой ветки не
      трогал — это чужая правка со своей причиной. Обсудить и починить
      одним из двух способов: опустить плашку вниз и на широких экранах
      или добавить первому экрану верхний отступ, пока плашка на виду.
- [ ] Скриншотов работ в портфолио по-прежнему нет, карточки текстовые.

---

## 11 августа, второй чат (ветка `claude/stories-yandex-metrica-tracking-h7aing`)

**Цель дня:** разобраться, почему сторис не видно в Метрике, и убрать
прямые заходы из общей кучи.

**Сделано:**
- [x] Короткие адреса `/wa`, `/tg`, `/vk` — маршруты в `server.js`, блок
      `STORY_SOURCES`. Отвечают 302 на главную с метками. Зарегистрированы
      до `express.static`: у статики включено `extensions: ['html']`, иначе
      запрос `/wa` ушёл бы искать `public/wa.html` и провалился в 404.
      302, а не 301 — постоянную переадресацию браузер кэширует навсегда.
      Проверено локально в браузере, есть и в `main`, и в живой ветке.
- [x] `docs/11-STORIES-METKI.md` — зачем короткие адреса и где смотреть отчёт.
- [x] `docs/15-KARTY-PRICE.md` — прайс на 60 позиций для карточки в Яндекс
      Картах. Названия позиций взяты из `docs/05-KEYWORDS.md`, то есть из
      запросов клиентов, а не из наших слов. Цены только те, что стоят
      в блоке «Прайс на услуги»; шестнадцать позиций помечены «уточнить» —
      на автоматизацию, приложения и интеграции цен на сайте нет.
      В начале файла таблица замера «до», без неё через месяц не с чем
      сравнивать.

**Остановились на:** прайс написан, но в Яндекс Бизнес не заведён —
это делается руками в кабинете. Замер «до» не снят.

**Ждём от Гульнары:**
- [ ] Проставить цены в шестнадцати позициях, помеченных «уточнить».
- [ ] Снять цифры «до» из статистики Яндекс Бизнеса и вписать в таблицу
      в начале `docs/15-KARTY-PRICE.md`.
- [ ] Завести прайс в Яндекс Бизнесе по одной категории за раз.
- [ ] Открыть Вебмастер → Диагностика → Безопасность и нарушения по
      granat-kmv.ru и сказать, что там написано.
- [ ] Проверить в Яндекс Бизнесе баланс и списания с 6 августа: если ни
      рубля не списалось, реклама не откручивалась и 136 переходов
      с меткой `geoadv` пришли из бесплатной карточки в Картах.

**Не доделано в коде:**
- [ ] Соседний проект `granat-kmv`: главная страница отдаёт 404. Файлы
      `layout.tsx`, `page.tsx` и `globals.css` лежат в корне репозитория,
      а Next.js ищет их в `app/`. Проверено сборкой: маршрута `/` нет
      в списке, `/portfolio`, `/o-nas`, `/privacy`, `/consent` тоже 404.
      Вместе с корневым `layout.tsx` не отдаётся метка подтверждения
      Яндекс.Вебмастера. Правка — перенести три файла в `app/`, проверено,
      что после этого `/` появляется. Ветки с правкой нет: репозиторий
      подключён только на чтение.
- [ ] Там же: `app/api/lead/route.ts` пишет заявку в `console.log` и
      отвечает «успешно». Ни в Telegram, ни в базу заявка не уходит,
      а посетителю показывается «свяжемся в течение часа».

---

## 11 августа: макеты для рекламы

**Цель дня:** свои макеты для ВК и 2ГИС вместо нейросетевых — те не
годились для размещения.

**Что было не так с прежними.** На двух из четырёх стоял знак **BDO** —
это чужой действующий товарный знак, международная аудиторская сеть.
Нейросеть подставила его сама, но отвечает за размещение владелец.
Плюс на четырёх макетах было четыре разных знака (BDO, «V» в круге,
неоновый леттеринг, а на сайте вензель «G») — узнаваемость на таком
наборе не набирается вовсе. Ни на одном не было цены и кнопки, зато
были «Node.js», «Архитектура» и «60 FPS» — язык разработчика, а не
клиента. На первом стояло «для IT-студий», хотя клиенты — малый бизнес.

**Сделано:**
- [ ] Четыре макета 1080×1350 в `marketing/out`: лендинг, бот, ИИ-агент,
      аудит. Рисуются браузером из `marketing/creatives.js`, шрифты и
      цвета — те же файлы, что отдаёт сайт.
- [ ] Правила в `marketing/README.md`: зона превью 540px, один знак,
      обязательная цена, одно действие, язык клиента, свой цвет каждой
      услуге, в центре — результат, а не абстракция.
- [ ] Шрифты вшиваются вместе с `unicode-range` из `fonts.css`. Без
      него кириллица уезжала в латинский файл и заголовок молча
      набирался системным шрифтом — поймано измерением ширины строки,
      на глаз не видно.

**Остановились на:** макеты одобрены и лежат в репозитории, ими можно
пользоваться прямо сейчас — качать из `marketing/out`.

**Ждём от Гульнары:**
- [ ] Решить, какой знак у студии один: на сайте сейчас вензель «G»
      (наследство «Граната»), по имени и домену логичнее «V». Решение
      нужно один раз — потом поставлю его и на макеты, и на сайт.
- [ ] Переписать карточку в 2ГИС под услугу, а не под название: в
      справочнике ищут по рубрике «разработка сайтов», а не по имени
      компании. Текст карточки я готов написать.

**Не доделано в коде:**
- [ ] Фиолетовый цвет макета про ИИ-агента взят из прежних картинок,
      в палитре сайта его нет. Либо добавить в палитру, либо заменить
      на фирменный синий — но тогда бот и агент станут похожи.

---

## 11 августа: цены на рекламе разошлись с сайтом

**Цель дня:** товары выложены в Яндекс Бизнес — значит, цены теперь живут
в трёх местах сразу, и они обязаны совпадать.

**Что нашли.** На рекламных макетах стояли цены из старого прайса:

| Услуга | На сайте | Было на макете |
|---|---|---|
| Лид-бот | от 15 000 ₽ | от 25 000 ₽ |
| ИИ-агент | от 60 000 ₽ | от 40 000 ₽ |

Лендинг и аудит совпадали. Ошибка моя: цены брались на глаз из прайса
на главной, а он к тому моменту уже обновился. Поймано случайно, когда
цены уже уходили в карточку Яндекс Бизнеса.

Расхождение сайта, рекламы и карточки клиент читает не как опечатку,
а как обман: пришёл по рекламе на 40 000, увидел 60 000 и ушёл.

**Сделано:**
- [ ] Цены на макетах исправлены, картинки пересобраны.
- [ ] В сборку макетов встроена сверка со `src/pages.js` — там источник
      правды, оттуда цены попадают и на сайт, и в разметку для
      поисковиков. Расходится — сборка падает с указанием, где именно.
      Проверено подстановкой неверной цены: падает, код выхода 1.

**Ждём от Гульнары:**
- [ ] Сверить карточку в Яндекс Бизнесе с сайтом. Правильные цены:
      разработка сайта от 35 000 ₽, Telegram- и VK-бот от 15 000 ₽,
      ИИ-агент от 60 000 ₽, автоматизация от 30 000 ₽, вайбкодинг
      от 65 000 ₽, приложения от 150 000 ₽, SEO-аудит с 152-ФЗ 15 000 ₽.
- [ ] Если в карточке уже стоят 25 000 за бота или 40 000 за агента —
      поправить там, а не на сайте.
- [ ] Проверить, что в карточке указан сайт `vibe-digital999.ru`:
      в разметке сайта ссылка на карточку уже стоит, нужна обратная.

**Не доделано в коде:**
- [ ] Витрина товаров в Яндекс Бизнесе показывает картинки ближе
      к квадрату, а макеты сделаны 1080×1350 под ленту ВК. Там они
      обрежутся сверху и снизу — как раз по заголовку и по кнопке.
      Нужна квадратная версия 1080×1080.

---

## 12 августа: бот ВК проверен живым диалогом

**Цель дня:** убедиться, что ассистент в сообществе отвечает клиентам,
и починить то, что видно в переписке.

**Проверка.** Гульнара прошла диалог целиком под видом владельца кафе.
Бот отработал сценарий: поздоровался, спросил нишу, назвал типичную
боль кафе (меню в блокноте, бронь по телефону в час пик), уточнил
объём, предложил состав под бюджет 50 000 ₽ и назвал цены — **65 000,
35 000 и 15 000, все по действующему прайсу**. Это же и снимает
вчерашний спор о ценах: бот берёт их правильно.

**Найдено и исправлено:**
- [ ] **В сообщениях были видны звёздочки markdown.** Клиент читал
      «\*\*Где сейчас чаще всего приходят клиенты…\*\*» вместо вопроса.
      ВКонтакте разметку не понимает и показывает как есть. Чистка
      добавлена в `sendVk` — на выходе, а не запретом в промпте:
      промпт модель забудет через десять реплик, код не забудет.
- [ ] **Бот задавал по два вопроса в одном сообщении.** «Сколько человек
      в команде и сколько обращений», «Сколько зон и есть ли фото».
      Человек отвечает на первый, второй теряется. В промпт добавлено
      правило: один вопрос в сообщении, второй — следующей репликой.
- [ ] **Бот додумывал за клиента.** На вопрос «через WhatsApp, звонок
      или соцсети?» получил «Да» и сочинил, что клиенты приходят всеми
      путями сразу. Придуманный факт ушёл бы в бриф владельцу как слова
      клиента. Добавлено правило: не подходит ответ — переспроси.

**Ждём от Гульнары:**
- [ ] После выката пройти диалог ещё раз и проверить, что звёздочек
      нет и вопросы идут по одному.

**Не доделано в коде:**
- [ ] Бот не отличает ответ на вопрос от ответа не по теме шире, чем
      разобранный случай. Правило в промпте помогает, но гарантии не
      даёт — надёжнее было бы проверять кодом, как сделано со звёздочками.

---

## Как обновлять сайт

Порядок выкатки, ловушки Amvera и почему сначала `fetch` — в `CLAUDE.md`,
раздел «Как сайт попадает в интернет». Здесь дубля нет намеренно: две
инструкции в двух файлах разъезжаются, и потом непонятно, какая верная.

Тут раньше стояла команда `git push amvera main:master`. Она устарела:
работа давно идёт в ветках `claude/...`, а не в `main`, и без `fetch`
перед пушем на сайт уезжает старая версия.

---

## 12 августа — личный агент Hermes на сервере

**Цель дня:** разобрать присланный гайд про облачных AI-агентов и решить,
заводим ли своего. Решение владелицы: заводим.

**Сделано:**

- [x] Разобран гайд EdgeLab Space про Hermes. Сама страница из беседы
      не открывается — сеть чата пускает не на все сайты; текст владелица
      вставила в чат руками. Следующему чату: пробовать `WebFetch` по
      `blog.edgelab.space` бесполезно, просить текст сразу.
- [x] **Написан `docs/16-HERMES-AGENT-VPS.md`** — весь порядок настройки
      в одном файле: что решается до аренды сервера, какой сервер брать,
      что отвечать мастеру `hermes setup`, как проверить автозапуск,
      пять ловушек и чего этот агент НЕ заменяет.
- [x] Записано главное разграничение: **Hermes — личный инструмент
      владелицы, а не часть сайта.** Сайт и бот ВК остаются на Amvera,
      второй сервер их не заменяет и не дублирует. Без этой строчки через
      месяц начнут платить за одно и то же дважды.
- [x] Найдено противоречие внутри самой статьи: в тексте сказано, что
      в Hermes можно войти по существующей подписке через OAuth, а в FAQ
      той же страницы — что прямого входа по потребительской подписке нет.
      **Подписку ради Hermes не оплачивать, пока не подтверждено
      официальной документацией.**
- [x] Календарь: заведено напоминание «Hermes: восстановить Telegram»,
      повтор через день. Снимать только после того, как Telegram реально
      откроется, — не по словам.

**Сделано во второй половине дня — два блокера из трёх сняты:**

- [x] **Telegram у владелицы работает.** Взяла новый VPN, поставила
      приложение, вошла в свой аккаунт. Первый шаг закрыт.
      **Побочное следствие: снят блокер, висевший с 11.08** — токен
      у `@BotFather` теперь получить можно, а значит `api/tg-bot.js`
      можно доводить до конца. Это отдельная задача по сайту, с Hermes
      её не смешивать.
- [x] **Закрыт главный вопрос по модели.** Сверено по официальному
      репозиторию `NousResearch/hermes-agent` (`providers.md`,
      `cli-config.yaml.example`): **Hermes работает с любым
      OpenAI-совместимым адресом** — провайдер `custom` и `base_url`
      в `~/.hermes/config.yaml`.
- [x] **Инференс Amvera подходит под это описание.** Сверено по коду
      живой ветки, `api/brief.js`: `https://inference.waw0.amvera.ru/v1`,
      формат `openai`, `/chat/completions`, модель `gpt-5`, ключ
      `LLM_API_KEY` из раздела «LLM» в кабинете. Подключение к Hermes —
      три строки в конфиге, отдельная подписка не нужна.
- [x] **Разрешено противоречие статьи про вход по подписке.** Права
      скорее её же FAQ: OAuth у Anthropic есть, но требует план Max
      и дополнительные кредиты. Обычной подписки мало. Подписку ради
      Hermes не оплачивать.
- [x] **Найден вопрос, который к Hermes отношения не имеет, но важнее
      его:** хост `inference.waw0.amvera.ru` записан в коде как
      российский (`RU_INFERENCE_HOSTS`), и на этом держится обещание
      политики конфиденциальности про обработку в России. При этом `waw`
      — обычно код Варшавы. Записано в `docs/16`, раздел 3.2, и в список
      к Гульнаре ниже.
- [x] **Записана денежная ловушка с сервером.** Раз Telegram у владелицы
      работает только через VPN, российский сервер может не достучаться
      до `api.telegram.org`. Проверка одной командой сразу после
      создания сервера, хостера брать с почасовой оплатой — `docs/16`,
      раздел 4.

- [x] **Отвечено, почему Hermes не поставить на Amvera** — вопрос владелицы,
      и его зададут снова. Четыре причины записаны в `docs/16`, раздел 4;
      решающая — `hermes setup` это интерактивный мастер, а внутрь
      приложения Amvera терминала нет, туда попадают только пушем.
      Плюс `run.command` один на приложение (сейчас `npm start`, то есть
      сайт), окружение `node` против нужного Python 3.11, и отдельный
      проект Amvera дешевле VPS не выходит. Инференс Amvera при этом
      берём — мозг оттуда, а жить агент будет на своём сервере.
- [x] **Сверена команда установки с `README.md` официального репозитория:**
      `curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash`.
      Владелице попался чужой разбор (похоже, от другой нейросети) с
      адресом `raw.githubusercontent.com/.../scripts/install.sh`, которого
      в README нет, и с оборванной строкой `git clone https://github.com`
      без имени репозитория. Записано правило: команды берём из README,
      а не из пересказов — тем более что эта команда скачивает скрипт
      и сразу его исполняет.

- [x] **Отвечено на главный вопрос владелицы: «как наделить его памятью,
      чтобы подсказывал».** Записано в `docs/16`, раздел 12. Коротко:
      память — не кнопка, а файлы, которые агент читает; у Hermes это
      `MEMORY.md` и `USER.md`, и они **намеренно крошечные** — 2200 и 1375
      знаков по умолчанию, то есть блокнот на полстраницы, а не архив.
      Настоящее знание живёт в навыках. «Подсказывает» не чат, а cron:
      с ключом `--no-agent` он гоняет обычный скрипт и присылает результат,
      **не тратя токенов вообще**.
- [x] **Составлен список, чем занять агента в этом проекте** — пять задач
      от простой к сложной, `docs/16` раздел 12. Первая уже почти готова:
      утренняя сводка по заявкам собирается из `/api/leads`, блок `svodka`
      написан 10.08, не хватает только переменной `LEADS_KEY` в Amvera —
      той самой, что висит незакрытой с 10 августа. То есть первая польза
      от агента упирается не в агента, а в одну переменную.

- [x] **Владелица ответила «все три бота»** на вопрос, кто должен умнеть:
      личный агент, бот студии и связка, где первый учит второго. Принято
      целиком, но разложено по порядку — **написан `docs/17-BOT-KOTORYY-UMNEET.md`**.
- [x] **Найдена по коду причина, почему бот студии не умнеет.** Не
      «плохой промпт», а устройство: знание вшито прямо в промпт
      `api/brief.js` (прайс в строках ~119–136), а память живёт в
      оперативке — `hits`, `notified`, `DIALOG_TTL_MS` в `api/vk-bot.js`.
      **Любой перезапуск стирает её начисто**, а он бывает при каждой
      выкатке и при каждой смене переменной в Amvera. Место под
      постоянную память при этом уже есть и простаивает:
      `persistenceMount: /data` в `amvera.yaml`.
- [x] Три уровня расписаны: знание в файле → память на диске → разбор
      диалогов раз в неделю с утверждением правок человеком. Записано
      железное правило: **агент не правит базу знаний сам**, он предлагает.
- [x] Записана граница по 152-ФЗ: диалоги обезличиваются ДО разбора,
      независимо от того, где стоит сервер.

### Скриншоты кабинета Amvera — три находки, одна серьёзная

Гульнара прислала снимки раздела «LLM» вечером 12.08. Разобрано:

- [x] **`waw0` — это Варшава. Подтвердилось.** В кабинете висит
      предупреждение самой Amvera: «Доступ Telegram-ботов в регионе
      **Москва_0** к API Telegram нестабилен, используйте регион
      **Варшава_0**». Значит регионы у площадки называются именно так,
      и `inference.waw0.amvera.ru` — это Варшава_0.

      **Отсюда серьёзное:** в `api/brief.js` этот хост записан в список
      российских (`RU_INFERENCE_HOSTS`), и на нём держится обещание
      политики конфиденциальности, что диалоги обрабатываются в России.
      **Похоже, на сайте написана неправда** — а это не мелочь: раздел
      про обработку данных читают и клиенты, и проверяющие.

      Сторож в коде тут не помог и не мог: он ругается на «чужой» адрес,
      а этот записан своим. Ошибка не в коде, а в предположении, из
      которого код писали.

      **Чинится, возможно, одной переменной.** Спросить поддержку не
      «в какой стране серверы» (ответ уже виден), а **есть ли инференс
      в регионе Москва_0 и какой у него адрес**. Есть — меняем
      `LLM_BASE_URL`, и всё. Нет — правим политику и подаём уведомление
      о трансграничной передаче в РКН (ст. 12 152-ФЗ, подаётся отдельно).

- [x] **Подтвердилась ловушка с Telegram, и опять не нашими руками.**
      Amvera прямым текстом пишет, что из Москвы до API Telegram не
      достучаться надёжно. Причина общая для российских площадок,
      не только для Amvera. Следствия два:

      1. Российский VPS для агента, скорее всего, не подойдёт. Значит
         зарубежный, значит правило «клиентских заявок агенту не давать»
         становится обязательным.
      2. **Заранее найденная ловушка для бота сайта.** `api/tg-bot.js`
         написан и ждёт запуска. Если приложение стоит в регионе
         Москва_0, он будет работать с перебоями, и виноват будет
         не код. Посмотреть регион приложения до запуска.

- [x] **Разобран список моделей.** Подключена ровно одна —
      `Qwen3-30b-Instruct`, остальные в списке неактивны. Для агента она
      не годится: описание самой Amvera — «для лёгких помощников
      бизнеса». Для Hermes по описаниям площадки подходят GPT-5,
      GPT-5.5, GLM-5.1. **Начинать с GPT-5:** у GPT-5.5 Amvera прямо
      предупреждает про цену. Баланс на счету 1598 ₽.

- [ ] **Проверить переменную `LLM_MODEL` у сайта.** В коде по умолчанию
      `gpt-5`, а он в кабинете НЕ подключён. Если переменная не задана,
      бот обращается к неактивной модели. Бот ВК 11.08 отвечал, значит
      переменная скорее всего стоит на Qwen — но это предположение,
      а не проверка.

### Требование: агент должен уметь SEO и гео

Сформулировано владелицей 12.08: сканировать наши сайты и видеть, где
дыра; консультировать по аудиту чужих компаний; база знаний обновляется
и запоминает. Разобрано, записано в `docs/16`, раздел 13.

- [x] **Навык SEO писать заново не нужно — он уже есть.** Сверено по
      живой ветке: `.claude/skills/seo-audit-ru/` — **38 файлов**. Там
      `SKILL.md` с порядком аудита, **рабочий скрипт автопроверки**
      `scripts/audit.mjs`, чек-лист, замеры-ориентиры, семантика, шаблон
      отчёта клиенту, отдельный файл про новый сайт и библиотека `deep/`
      из 29 файлов. Плюс второй навык `texty-uslug` со своим скриптом.
      Плюс `docs/05`, `06`, `07`, `09` — ещё 883 строки.
- [x] **Оба смысла слова «гео» покрыты.** Региональное продвижение —
      `references/geo-local.md`. Продвижение в ИИ-поисках (GEO) —
      `references/deep/knowledge/guide_ai_seo.md`, и под него на сайте
      уже собирается `public/llms.txt`.
- [x] **Найдено, что ложится идеально:** `audit.mjs` не требует
      зависимостей, только Node 18+. Значит «сканировать наши сайты»
      ставится на расписание **без обращения к модели, то есть бесплатно**.
      Модель нужна только чтобы объяснить результат словами.
- [x] **Найдены три дыры, и их надо знать до обещаний клиентам:**
      1. **Нет истории замеров.** Скрипт делает снимок «сейчас».
         «Где дыра» — это не снимок, а сравнение: было и стало. Без
         истории агент не скажет «описание главной пропало вчера».
      2. **Скрипт смотрит сайт, а не выдачу.** Позиций он не знает.
         Позиции — это API Вебмастера, отдельный ключ, отдельная задача.
         **Не обещать клиентам слежение за позициями, пока этого нет.**
      3. **Знание о правилах поиска устаревает,** а навык сам себя не
         обновит. Агент может приносить кандидатов на правку, но
         переписывать навык сам не должен — то же правило, что в `docs/17`.
- [x] Записана граница по чужим аудитам: **агент готовит черновик, отчёт
      клиенту подписывает человек.** В отчёте стоят сроки и обещания,
      за них отвечает ИП. И чужой сайт агент видит только снаружи —
      отличить догадку от факта должен человек.

### Пример «делаем новый сайт» — и найденное расхождение в ценах

Владелица привела пример того, что агент должен понимать сразу: чем
многостраничник отличается от одностраничника и как двигать лендинг
через прайс на 60 позиций. Разобрано, записано в `docs/16`, раздел 13.

- [x] **Про многостраничник агент уже знает.** В `SKILL.md`:
      «одностраничник с идеальной техничкой физически не может
      ранжироваться по десяти коммерческим запросам». В
      `references/new-site.md`: «по странице на интент, шесть услуг —
      шесть страниц, а не шесть якорей». Дописывать нечего.
- [x] **Формулировка про 60 позиций уточнена.** Прайс двигает **карточку
      в Яндекс Картах**, а не лендинг. Для клиента с одностраничником это
      обходной путь — показы оттуда, откуда сайт их дать не может.
      **Не замена структуре, а способ получать заявки, пока её нет.**
      Без этого уточнения агент начнёт советовать прайс вместо структуры.
- [ ] **«В метриках лучшие» пока не доказано.** В `docs/15` первым делом
      стоит требование записать цифры «до»: показы, переходы, звонки,
      маршруты. **Таблица пустая, замер не снят.** Верим по устройству
      Яндекса, но цифрами не подтвердили — значит клиенту подаём как
      ожидание, а не как результат.

**Найдено попутно — два расхождения, и они доказывают правоту владелицы
лучше любых объяснений:**

- [ ] **`docs/15-KARTY-PRICE.md` отсутствует в живой ветке.** Лежит
      только в `stories-yandex-metrica-tracking-h7aing` и в этой. Навык,
      собранный из живой ветки, про 60 позиций не узнает никогда.
      Файл надо влить в живую.
- [ ] **Цена ИИ-агента расходится.** В `docs/15` — «от 40 000 ₽».
      По разбору 11.08 везде 60 000 ₽: на главной, в `llms.txt`,
      в разметке `Offer` и в промпте бота, причём тот разбор прямо
      утверждал, что 40 000 ₽ не встречается нигде. Один из источников
      врёт, и именно `docs/15` пойдёт в карточку Яндекс Карт, которую
      видят клиенты. **Спросить владелицу, какая цена верная, и
      привести к одной.**

Это и есть довод за уровень 1 из `docs/17`: пока одно знание живёт
в четырёх местах, оно расходится само собой, без чьей-либо небрежности.

### Абсурд, который вскрыла владелица — и почему он случился

12.08 она сказала: вчера начала делать автошколе многостраничный сайт
и **пришлось идти в другую беседу, чтобы дать инструкцию, как его
делать** — при том, что всё давно записано. Разобрано, причина найдена.

- [x] **Причина найдена и она в цифрах.** Навык `seo-audit-ru` лежит
      **во всех девяти ветках**. `docs/13-MNOGOSTRANICHNIK.md` и
      `docs/14-KLIENTSKIY-SAYT-FILIALY.md` — **только в четырёх**.
      `docs/15` — вообще в двух. Разница не случайна: **навык
      самодостаточная папка, её копируют целиком и подхватывают
      автоматически, а документ надо помнить и открыть.**
      Чат, открытый в одной из пяти «бедных» веток, про многостраничник
      не знал ничего — и правильно себя вёл, знания у него не было.
- [x] **Написан навык `.claude/skills/mnogostranichnik-ru/`** — пять
      файлов, собран из `docs/13`, `docs/14` и `references/new-site.md`:
      `SKILL.md` (лендинг или многостраничник, порядок работ, требования
      к странице), `filialy-i-geo.md` (филиал — переключатель, гео —
      наоборот, карточки и прайс), `klientskiy-proekt.md` (состав сайта
      по нишам, счётчик клиента, чего нельзя обещать),
      `dannye-152fz.md` (три варианта хранилища, и модель тоже может
      быть за границей), `vebmaster-metrika.md` (переобход, цели на
      событиях, замер «до»).
- [x] **Навык сделан переносимым.** Путей и цифр этого репозитория
      внутри нет — копируется папкой в любой клиентский проект,
      и следующий чат там подхватит его сам. Механика нашей сборки
      осталась в `docs/13`, ссылка на неё в конце `SKILL.md`.
- [x] **Проверено, что система его видит:** навык появился в списке
      доступных сразу после создания, без перезапуска.
- [x] **Правило записано в `CLAUDE.md`:** пригодится в другом проекте —
      значит навык, а не документ в `docs/`.

- [ ] **Скопировать навык в проект автошколы.** Это делается там, где
      лежит её сайт: папку `.claude/skills/mnogostranichnik-ru/` целиком
      в репозиторий проекта. Пока не сделано — чат автошколы по-прежнему
      не знает того, что мы записали.
- [ ] **Влить навык и `docs/15` в живую ветку** — иначе они останутся
      только здесь и повторится ровно та же история.

### Репозитории на GitHub — что есть и две находки

Проверено 12.08 по GitHub API. У владелицы **три** репозитория:

| Репозиторий | Что это | Доступ | Ветка по умолчанию |
|---|---|---|---|
| `vibe-digital999` | наш сайт | **открытый** | `claude/stories-yandex-metrica-tracking-h7aing` |
| `granat-kmv` | сайт Гранат | открытый | `main` |
| `autoschool-insight` | приложение автошколы, TypeScript | закрытый | `main` |

**Сайта автошколы на GitHub нет** — подтверждено, есть только приложение.

- [x] **Найдено: ветка по умолчанию у `vibe-digital999` — не `main`,
      а `claude/stories-yandex-metrica-tracking-h7aing`.** Это многое
      объясняет. Новая беседа отводит свою ветку от ветки по умолчанию,
      а та документационная: в ней есть `docs/13`, `docs/14`, `docs/15`,
      но **нет папки `src/`** и лежит удалённая дубль-сборка `tools/`.
      Отсюда и эта ветка такая же. **Ветку по умолчанию надо переставить
      на живую** `claude/multipage-website-okje3k` — тогда новые чаты
      будут начинать с правильного кода. Делается в настройках
      репозитория, это её действие.
- [x] **Найдено: `vibe-digital999` открытый.** Токенов в нём нет, они
      в переменных Amvera, — но `TASKS.md` с ценами, планами, замерами
      Метрики и разбором клиентов читает кто угодно. Решение владелицы,
      но знать об этом она должна. Приложение автошколы при этом
      закрытое, и это правильно.
- [x] **Решение владелицы: репозиторий один на клиента — экосистема.**
      Не отдельно сайт, отдельно бот, отдельно приложение, а один
      репозиторий `autoschool-start`, внутри `site/`, `bot/`, `app/`,
      плюс `CLAUDE.md`, `STATUS.md`, `TASKS.md`, `docs/` и `.claude/skills/`.
      Решение верное и лучше первоначального предложения: знание
      о клиенте — цены, филиалы, расписание, тон — нужно и сайту,
      и боту, и приложению, а разложенное по трём репозиториям оно
      разойдётся само. **Правило записано в навык**,
      `references/klientskiy-proekt.md`, раздел «Один репозиторий
      на клиента» — оно пригодится с каждым следующим клиентом.
- [x] **Найдено: у владелицы ДВА аккаунта GitHub, и это опасно.**
      Выяснилось со скриншота её телефона.

      | Аккаунт | Что в нём | Состояние |
      |---|---|---|
      | `gulnaragold999m-tech` | `vibe-digital999`, `granat-kmv`, `autoschool-insight` | **рабочий**, обновлён 11.08 |
      | `9rpkzh42qm-web` | `my-vibecoding-agenci` (июнь), **копия** `vibe-digital999` | старый, на телефоне открыт он |

      Ассистент работает от имени `gulnaragold999m-tech` — проверено
      через `get_me`. **В репозиторий, созданный во втором аккаунте,
      он писать не сможет.**

- [ ] **Опасность: вторая копия `vibe-digital999`** лежит на
      `9rpkzh42qm-web`, создана 07.08 13:22 и **с тех пор ни разу
      не тронута** (`updated_at` совпадает с `created_at`). Настоящий
      сайт живёт в `gulnaragold999m-tech`. Однажды туда зайдёт человек
      или чат и начнёт работать в версии от 7 августа, потеряв всё
      после. **Заархивировать её** (Settings → Archive this repository)
      — архив не удаляет, но делает репозиторий только для чтения
      и снимает вопрос навсегда. Решение владелицы; удалять не
      предлагаем — архива достаточно.

- [x] **Репозиторий `gulnaragold999m-tech/autoschool-start` создан**
      владелицей с телефона, закрытый, ветка `main`. Создать его
      ассистентом было нельзя — 403, у встроенного доступа нет права
      заводить репозитории. Имя сначала получилось с лишним дефисом
      на конце, переименовано.
- [x] **Попутно выяснилось: у владелицы два аккаунта GitHub**, и на
      телефоне был открыт не тот. Разобрано выше. Репозиторий создан
      в правильном, `gulnaragold999m-tech`.
- [ ] **Наполнить репозиторий не удалось — упёрлись в границу сессии.**
      Беседа привязана к `vibe-digital999`, писать в чужой репозиторий
      она не может. Подключение (`add_repo`) требует подтверждения,
      а владелица была с телефона, где кнопки подтверждения не видно.
      Попытки: `add_repo` дважды, `git clone` с токеном из окружения,
      `push_files` через API — все отклонены.
- [x] **Работа не пропала: комплект готов** и лежит в
      `templates/autoschool-start/` — `CLAUDE.md`, `STATUS.md`,
      `TASKS.md` со списком того, что нужно от клиента, `gitignore.txt`
      и `README-KAK-POLOZHIT.md` с порядком переноса.
- [ ] **Перенести комплект в `autoschool-start`.** Нужна беседа, у
      которой в доступе оба репозитория, — тогда это копирование файлов
      и один коммит. Вместе с файлами скопировать **папку целиком**
      `.claude/skills/mnogostranichnik-ru/`, а заодно `seo-audit-ru`
      и `texty-uslug`.
- [ ] **После переноса удалить `templates/autoschool-start/` отсюда**
      и отметить это здесь. Две копии одних и тех же правил — ровно та
      болезнь, от которой мы лечимся.
- [ ] **Приложение `autoschool-insight` пока НЕ переносить.** Перенос
      истории — отдельная работа, и она может подождать. Достаточно
      записать в `STATUS.md` нового репозитория, где оно лежит,
      и поставить ссылку. Знание о клиенте переезжает сразу, код —
      когда дойдут руки.
- [ ] **После создания — положить туда навыки.** `mnogostranichnik-ru`
      обязательно, `seo-audit-ru` и `texty-uslug` заодно, плюс стартовый
      `CLAUDE.md`. Тогда чат по автошколе начнёт со знанием, а не
      с объяснений владелицы. Это и есть то, ради чего навык писался.

### Поиск работ по автошколе — бота в репозитории НЕТ

Владелица попросила собрать в новый репозиторий работы по автошколе:
бота («вчера писала много») и приложение. Проверено по всем девяти
веткам `vibe-digital999`.

- [x] **Бота автошколы в этом репозитории нет.** Ни кода, ни промпта,
      ни настроек. Есть только кейс — `src/pages/case-avtoshkola.html`
      и собранная `public/portfolio/avtoshkola-start/`. Это рассказ
      о работе, а не сама работа.
- [x] **Приложение `autoschool-insight` обновлялось 11.08 в 19:43** —
      то есть вчера вечером туда что-то положили. Прочитать его из этой
      беседы нельзя: сессия видит только `vibe-digital999`.
- [ ] **Выяснить у владелицы, где писался бот автошколы.** Три
      возможности, и они требуют разных действий:
      1. В `autoschool-insight` — тогда просто переносим.
      2. В беседе, привязанной к другому репозиторию — найти и перенести.
      3. **В беседе без репозитория — тогда работа существует только
         в той вкладке чата.** Это срочно: закроется — пропадёт. Достать
         оттуда текст и положить в репозиторий, пока чат жив.
- [ ] Собрать в `autoschool-start` всё по клиенту: бота, приложение,
      кейс. Упирается в то же подключение репозитория.

### Бот автошколы НАЙДЕН — и план меняется

Владелица показала беседу: бот писался в репозитории **`autoschool-insight`**,
ветка **`claude/driving-school-vk-bot-b5ezmb`**, диff **+5565 −231**.
Ничего не потеряно, всё запушено.

- [x] **Работа цела.** Бот ВК для автошколы — не набросок, а готовый код
      в ветке `claude/driving-school-vk-bot-b5ezmb` репозитория
      `autoschool-insight`. Тот чат оставил три нерешённых вопроса
      (см. ниже).
- [x] **План переноса пересмотрен, и это важно.** Код бота и приложения
      УЖЕ лежит в `autoschool-insight`. Переносить 5565 строк вместе
      с историей в пустой `autoschool-start` — риск без выгоды.
      **Правильно наоборот: положить ЗНАНИЕ туда, где уже лежит РАБОТА.**
      Принцип «один репозиторий на клиента» от этого не страдает —
      меняется только, какой из двух станет единым.
- [x] **Решено и сделано 12.08: корень автошколы — `autoschool-insight`.**
      Код бота и приложения уже там, двигать нечего. Пустой
      `autoschool-start` **удалён владелицей** — в нём был только README,
      ничего не потеряно. Проверено после удаления: у аккаунта осталось
      три репозитория — `vibe-digital999`, `granat-kmv`,
      `autoschool-insight`. Один клиент — один корень, как и
      сформулировано правилом.
      Имя `autoschool-insight` историческое; переименовать в
      `autoschool-start` можно когда угодно, GitHub держит
      перенаправление.

**Три вопроса, оставленные тем чатом, и ответы на два из них
у нас уже есть — сегодня как раз это разбирали:**

- [ ] **1. База.** Тот чат предлагал переезд на российский хостинг
      (меняются три переменные, код не переписывается) либо запуск
      с `PD_STORE=off`. Ответ — в навыке,
      `.claude/skills/mnogostranichnik-ru/references/dannye-152fz.md`:
      три варианта, и **выбирать самый простой из подходящих.**
      Для бота записи это вариант 1 — ничего не хранить, заявка уходит
      в мессенджер администратору. То есть `PD_STORE=off` — не времянка,
      а вполне возможное постоянное решение.
- [ ] **2. Модель.** Вариант «Claude с маскированием» для КЛИЕНТСКОГО
      бота плохой: маскирование — это наше обещание, а не гарантия,
      и отвечать за него перед клиентом придётся нам. Чистые варианты —
      **YandexGPT** (`llm.api.cloud.yandex.net`) или **GigaChat**
      (`gigachat.devices.sberbank.ru`): оба российские, оба уже стоят
      в списке `RU_INFERENCE_HOSTS` в нашем `api/brief.js`.
      **Инференс Amvera для клиентского бота НЕ годится** — сегодня
      выяснили, что `inference.waw0` это Варшава.
- [ ] **3. Мелочи.** Реквизиты оплаты, контакт Сергея Дмитриевича
      в справочник, номера страниц ВК для `VK_STAFF` — это данные
      от клиента, добавлены в список ожидания.

### Правило владелицы: репозиторий — это клиент

Сформулировано ею 12.08: «правило создавать репозиторий на нового
клиента, а уже всё, что мы создаём для этого клиента — агента, или
приложение, или бота — всё должно быть в корне, от которого растут
ноги».

Формулировка точнее прежней. Было «один репозиторий на клиента» —
стало «репозиторий и ЕСТЬ клиент, остальное растёт из него».

- [x] Записано в `CLAUDE.md` этого репозитория, раздел «Новый клиент —
      новый репозиторий, и он корень».
- [x] Записано в навык, `references/klientskiy-proekt.md` — то есть
      поедет в каждый клиентский проект вместе с папкой навыка.
      Добавлена папка `agent/` к `site/`, `app/`, `bot/`.

### GEPA — проверен пересказ, технология настоящая

Владелице попался пересказ видео про «движок GeP от Nous Research».
Разобрано, записано в `docs/17`, раздел 5.1.

- [x] **Технология настоящая и нам подходит.** GEPA (Genetic-Pareto),
      статья «GEPA: Reflective Prompt Evolution Can Outperform
      Reinforcement Learning», arXiv 2507.19457, **принята на ICLR 2026
      устным докладом** — это верхние проценты отбора.
- [x] **Пересказ переврал четыре вещи**, все проверены:
      1. **Не Nous Research.** Авторы — Agrawal и другие. Репозитория
         `NousResearch/GeP` не существует, поиск дал ноль.
      2. Название **GEPA**, не «GeP» и не «General Evolution of Prompts».
      3. Цифры приукрашены: не «на 20% производительнее», а «в среднем
         на 6%, максимум до 20%»; не «в 35 раз меньше данных», а «до
         35 раз меньше прогонов».
      4. **Команды выдуманы целиком** — включая ту же оборванную строку
         `git clone https://github.com`, что и в пересказе про Hermes.
- [x] **Правило подтверждено дважды за день и записано:** команды
      из пересказов не выполнять, брать из README первоисточника.
      Оба пересказа предлагали скачать скрипт и сразу исполнить.
- [x] **Записано, почему GEPA — это уровень 3, а не первый шаг.**
      Ему нужны evals: набор задач с заранее известным правильным
      ответом. Нет набора — нечем измерить «стало лучше», и оптимизатор
      честно выдаст «идеальный промпт», который хуже исходного.

- [ ] **Собрать evals для бота студии** — 20–30 разговоров с известным
      правильным ответом: «сколько стоит лендинг» → 35 000 ₽ от 7 дней;
      «приедете к нам?» → нет, работаем удалённо; «а 152-ФЗ?» → ответ
      из промпта. Работа на несколько часов. **Делается после уровня 1.**
- [ ] **Проверить evals нарочной поломкой промпта** — если оценка
      не упала, evals бесполезны. Этот шаг не пропускать.

- [x] **Вывод владелицы записан правилом в `CLAUDE.md`:** «в видео есть
      доля правды, а дальше надо изучать, знания надо добывать».
      Формализовано как закономерность: пересказ сохраняет идею и теряет
      подробности, поэтому **направление берём из пересказа, детали —
      из первоисточника**. Опознавательный признак плохого пересказа —
      оборванная строка `git clone https://github.com`, она встретилась
      дважды за ночь.

### Третий пересказ за ночь — и он поправил МЕНЯ

Пересказ видео про Hermes Desktop. Проверено, записано в `docs/16`
раздел 4.5 и `docs/17` раздел 5.1.

- [x] **Поправка к моему же вчерашнему разбору GEPA.** Я написал «не Nous
      Research, точка» — это было слишком категорично. Саму GEPA
      придумали не там, но **у Nous Research есть официальный репозиторий,
      который её применяет:** `NousResearch/hermes-agent-self-evolution`,
      «optimize skills, prompts, and code using DSPy + GEPA», почти
      5000 звёзд. То есть связка Hermes + GEPA существует и поддерживается
      авторами агента, прикручивать руками ничего не надо. Пересказ ошибся
      в том, ЧЬЯ это разработка, а не в том, что связь есть.
- [x] **Найден `NousResearch/hermes-telegram-business`** — плагин
      «бот-секретарь: каждый черновик ответа требует одобрения владельца,
      прежде чем уйдёт клиенту». Это готовая реализация нашего железного
      правила из `docs/17` раздела 5. **Посмотреть до того, как писать
      своё.**
- [x] **Hermes Desktop — проверено частично, честный итог в `docs/16`
      разделе 4.5.** В шапке README он упомянут, значит существует.
      Но установщиков под Windows подтвердить не удалось: страница
      релизов не отдала список файлов, отдельного репозитория
      `hermes-desktop` в организации нет.
- [x] **«Nous Portal бесплатно, DeepSeek V4 Flash по акции» — не
      подтверждено.** README описывает Portal как подписку: «300+ models
      under one subscription». Про бесплатный тариф ни слова. И «по
      акции» само по себе значит «временно».

- [ ] **Проверить страницу релизов Hermes** — есть ли файл под Windows.
      Минута работы, а решает, нужен ли сервер вообще на первом шаге.
      `github.com/NousResearch/hermes-agent/releases`
- [ ] **Если Desktop есть — сначала неделя на своём компьютере,
      бесплатно, и только потом решение про сервер.** Но помнить
      ограничение: ноутбук не работает круглосуточно, брифинг в 7:00
      не придёт на выключенном компьютере.
- [ ] **Заполнить `USER.md` и `MEMORY.md` руками сразу** после установки:
      как работаем, каким тоном отвечать, какой стек, чего делать нельзя.
      Совет из пересказа, и он верный — совпадает с `docs/16` разделом 12.

### ВКонтакте и Hermes — ответ получен

- [x] **Hermes официально ВК не поддерживает.** Список платформ из
      README: Telegram, Discord, Slack, WhatsApp, Signal, Email,
      Home Assistant, командная строка. ВК нет.
- [x] **Найдено три сторонних плагина для ВК** — `hermes-vk-connector`
      (1 звезда), `hermes-vk-platform` (0), `hermes-vk-plugin` (0).
      Все от частных лиц, все свежие, у всех околонулевая популярность.
      **Клиентскому боту такой плагин отдавать нельзя:** ему нужен ключ
      сообщества, то есть право писать от имени бизнеса всем подписчикам.
- [x] **Вывод: переносить ВК на Hermes не нужно.** Он у нас уже закрыт
      своим кодом — бот студии `api/vk-bot.js` работает и проверен,
      бот автошколы написан. Разделение записано в `docs/16` разделе 4.6:
      Hermes отвечает за Telegram и личные задачи, ВК остаётся на своих
      ботах.
- [x] **Четвёртый пересказ за ночь.** Прислан подробный план установки
      Hermes Desktop с описанием вкладок интерфейса («Memory»,
      «Integrations → Telegram»). **Утверждает как факт ровно то, что
      проверить не удалось** — наличие `.exe` и `.dmg`. Описание чужого
      интерфейса по памяти — самая выдумываемая часть таких текстов.
      Проверка прежняя и она одна: открыть страницу релизов.

### Поправка: сайт granat-kmv.ru РАБОТАЕТ

- [x] **Запись от 11.08 про 404 на главной устарела.** Проверено
      12.08 скриншотом с телефона владелицы: `granat-kmv.ru` открывается,
      первый экран на месте — логотип, кнопка «ЗАЯВКА», заголовок
      «Типография и дизайн-студия в Лермонтове и на КМВ». Значит,
      перенос файлов в `app/` уже сделан. **Следующему чату на эту
      запись не опираться.**
- [ ] **Вторая находка того же разбора НЕ проверена и она важнее:**
      `app/api/lead/route.ts` пишет заявку в `console.log` и отвечает
      «успешно», ничего никуда не отправляя, а посетителю показывается
      «свяжемся в течение часа». Проверяется за минуту: заполнить форму
      на себя и посмотреть, пришло ли что-нибудь.
- [ ] **Наблюдение по первому экрану, требует проверки на всей странице.**
      Заголовок «Типография и дизайн-студия», а подсказка сбоку продаёт
      веб-разработку: «сайт живой? это не магия, это код». Человек,
      которому нужны наклейки или баннер, попадает на страницу про код.
      Два разных бизнеса на одной витрине, и печать — та, что может дать
      выручку быстрее, — стоит второй.
- [ ] **Подключить `granat-kmv` к беседе не удалось** — подтверждение
      не всплывает, как и с `autoschool-start`. Три попытки за ночь.
      Путь — новая беседа, открытая на этом репозитории.

**Контекст, важный для приоритетов** (в открытый репозиторий подробности
не пишем): типография — собственный бизнес владелицы, автошкола — место
работы по найму. Значит, `granat-kmv` по деньгам приоритетнее
`vibe-digital999`.

### Кабинет Amvera: все приложения в Москве, но приложений семь

Проверено 12.08 по скриншоту кабинета.

- [x] **Все приложения в регионе `Moscow-0`.** Видно подписью под именем
      каждого проекта и в адресе кабинета (`.../applications/msk0/...`).
      **База учёта автошколы — ФИО, телефоны и платежи курсантов —
      физически в России.** Утверждение из `DEPLOY-AMVERA.md`
      подтвердилось, 152-ФЗ по ней соблюдён.
- [x] **Записано различие, из-за которого мы чуть не успокоились рано:**
      регион ПРИЛОЖЕНИЯ и регион ИНФЕРЕНСА — разные вещи. Приложение
      `vibe-digital` в Москве, а диалоги с ботом уходят на
      `inference.waw0`, то есть в Варшаву. **Вопрос к поддержке
      остаётся**, но стал точным: есть ли инференс в Москва_0.
      `docs/16`, раздел 3.2.
- [x] **Заранее объяснена будущая поломка Telegram-бота.** Amvera пишет,
      что из Москва_0 доступ к API Telegram нестабилен. Все приложения
      в Москве. Значит `api/tg-bot.js` при запуске может барахлить —
      **это ограничение площадки, а не ошибка кода.** Записано в `docs/16`,
      чтобы не искали ошибку не там.

- [ ] **Приложений на Amvera СЕМЬ, а мы знали про два.** В списке видны
      `start-uchet`, `СТАРТ`, `start-avto`, `vibe-digital`, `autopost-bot`
      и ещё два ниже. **Пройти по каждому и проверить, есть ли его код
      в git.** `start-uchet` показал, что бывает иначе: код жил только
      на Amvera, в одном экземпляре, и его пришлось выгружать вручную.
      Всё, чего нет в git, — это работа, которую нечем восстановить.

### Карта проектов заведена — `docs/18-KARTA-PROEKTOV.md`

Повод: владелица 12.08 — «этот бардак должен закончиться сегодня».
Порядок наступает не когда всё починено, а когда перестало быть
неизвестным, где что лежит.

- [x] **Заведена `docs/18-KARTA-PROEKTOV.md`** — карта: семь приложений
      Amvera, три репозитория GitHub, что куда деплоится. Заполняется
      по одному приложению за раз, со слов владелицы: кабинет Amvera
      из беседы не открывается.
- [x] **Найдено, где последние изменения автошколы:**
      `autoschool-insight`, ветка `main`, семь коммитов, последние
      три утром 12.08. Ветка бота `claude/driving-school-vk-bot-b5ezmb`
      **уже влита в `main`** 12.08 в 06:41.
- [x] **Владелица поправила формулировку, и поправка верная.** Я назвал
      это путаницей — «два приложения, работа шла не в том». Она:
      «приложение старт учёт — это приложение, корневая папка — автошкола
      Старт; это как заходишь в компанию, у неё есть ещё склады, филиалы,
      которые тоже ей принадлежат».
      **Несколько приложений у клиента — норма.** Ошибка не в их числе,
      а в том, что они лежали без общего корня. Это ровно правило
      «репозиторий — это клиент».
- [x] **Записана структура корня** в `docs/18`: `app/`, `uchet/`, `bot/`,
      `site/` плюс файлы памяти, `docs/` и навыки.
- [ ] **Две базы в разных странах — это остаётся проблемой.**
      `start-uchet` в Москве, приложение с ботом на Supabase за границей,
      а курсанты одни и те же. Несколько приложений — норма, данные
      в двух юрисдикциях — нет.
- [x] Записано, что беседа по автошколе сделала правильно: YandexGPT
      вместо Claude, режим `PD_STORE=off`, файлы памяти, перенос нашего
      чек-листа по ВК. И нашла тихую ошибку с кириллицей в `\b`,
      из-за которой ФИО уходили в модель незамаскированными.

- [x] **Корень автошколы переименован в `autoschool-start`** — сделано
      владелицей 12.08 в 10:50. Проверено на экране: закрытый, ветка
      `main`, 69 коммитов, 14 веток. Перенаправление со старого имени
      работает, беседа по автошколе не сломалась.
- [x] **Найдено, откуда взялся Supabase: приложение собрано в Lovable.**
      В корне лежит `.lovable` с пометкой `template:
      tanstack_start_ts_current`. Значит заграничная база пришла
      **по умолчанию вместе с конструктором**, а не была выбрана.
      Переезд от этого проще: не «переделать решение», а «поправить
      умолчание». По записи в коммите от 11.08 переписывать приложение
      не придётся — меняются три переменные.
- [x] **Подтверждено, что выкладка на Amvera падает.** В репозитории
      есть `.github/workflows` — «Выкладка на Amvera через GitHub
      Actions», и рядом с последним коммитом красный крестик.
      Значит код в GitHub и код на сервере расходятся молча.

- [ ] **Починить выкладку** — в GitHub не заданы доступы к Amvera.
      До этого любое «я выложил» ничего не значит.
- [ ] **Разобрать 14 веток** в `autoschool-start`. Не срочно, но
      с таким числом снова начнётся разъезд.
- [ ] **Заказать загрузку Excel в приложении учёта.** Сейчас посещения
      импортируются скриптом только с компьютера, и потому нет июля
      и августа — два месяца. Владелице нужно заливать данные самой,
      из любого места. Текст задания готов, отдан 12.08.
- [ ] **Скачать копию базы `migration.db`** до любых дальнейших правок:
      кабинет Amvera → `start-uchet` → «Репозиторий» → Data.
- [ ] **Заполнить карту по всем семи приложениям** — репозиторий и ветка
      у каждого. Где репозитория нет, код живёт в одном экземпляре.
- [ ] **Решить, что делать с двумя базами.** Держать один бизнес
      в двух системах, одна из которых за границей, — дороже и опаснее,
      чем свести к одной.

### Архив «Старт учёт» передан в беседу по автошколе

- [x] **Владелица выгрузила код `start-uchet` с Amvera архивом**
      и передала его в беседу, открытую на `autoschool-start`,
      с заданием положить в папку `uchet/`. Задание включает: только
      положить как страховку, приложение на Amvera не трогать,
      записать часть в `STATUS.md` и отметить незакрытым, что нет
      июля и августа.
- [x] **Файлы доехали.** Проверено по коммитам `main` 12.08:
      `uchet/` положен в 11:13 мск, коммит `9a968cc`. Заодно там же
      записано, что программ у школы две и адрес
      `start-uchet-granatgold999.amvera.io` принадлежит учёту —
      источник всей дневной путаницы с «кабинетом».
      Папка выведена из eslint и prettier, чтобы форматирование
      не переписало чужой рабочий код.

### Дыра в доступе к данным автошколы — найдена и закрыта 12.08

Нашла беседа по автошколе, проверено и закрыто владелицей при участии
этой беседы. **Серьёзнее всего остального за день.**

**Что было.** На странице входа в приложение-дашборд стояла кнопка
«Регистрация»: любой человек мог сам завести аккаунт. А правила доступа
в базе написаны как «вошёл — можно всё»: читать, менять и удалять
заявки, оплаты, переписку бота. То есть публикуемый ключ был тут
ни при чём — **дверь была открыта изнутри.**

- [x] **Регистрация выключена.** Supabase → Authentication →
      Sign In / Providers → «Allow new users to sign up» → выключено,
      подтверждение «Successfully updated settings» на экране.
      **Проверено глазами, а не по словам.**
- [x] **Смягчающее обстоятельство:** «Confirm email» был включён, то есть
      зарегистрироваться и сразу войти было нельзя — требовалось
      подтверждение почты. Дверь была прикрыта, а не распахнута.

**Что выяснилось про базу автошколы попутно:**

- [x] **Проект Supabase один**, и он **в Ирландии**: `AWS eu-west-1`,
      подпись «West EU (Ireland)». Данные заявок и оплат физически
      за границей — теперь это не догадка, а название региона.
- [x] **Проект висит на СТАРОМ аккаунте владелицы** —
      `9rpkzh42qm-web's Org`. Значит Lovable подключался через него.
      Потеряется тот аккаунт — уйдёт и база.
- [x] **Главное успокаивающее: 1372 курсанта НЕ в Ирландии.** Они
      в «Старт учёте», файл `migration.db` на Amvera, регион Moscow-0.
      В Ирландии — база дашборда: заявки, оплаты, переписка бота.
      Размер базы 26 МБ, то есть примерно как у пустой: столько занимают
      системные таблицы Supabase.
- [x] **«No migrations» в панели проекта.** База собиралась не файлами
      миграций, а напрямую из Lovable. Значит **проверять RLS надо
      в живой базе, а не в файлах** — файлы показывают замысел.
- [x] **«No backups»** — резервных копий нет вовсе. Бесплатный тариф.

- [ ] **Переписать правила доступа** с «всех вошедших» на поимённый
      список сотрудников. Работа беседы по автошколе.
- [ ] **Убрать кнопку «Регистрация»** со страницы входа.
- [ ] **Сверить RLS в живой базе** — Database → Tables, значок у каждой
      таблицы. Отдельно проверить `documents`: там медсправки, а это
      сведения о здоровье.
- [ ] **Посмотреть, сколько строк в таблицах.** Если почти пусто —
      переезд базы в Россию делается спокойно и без потерь.
- [ ] **Разобраться с «10 users (estimated)»** в списке пользователей
      при одной видимой записи. Скорее всего это неточная оценка
      Postgres, но проверить.

### Автошкола: три правки безопасности выкачены в main

Проверено по коммитам 12.08. Всё в основной ветке:

| Время мск | Коммит | Что |
|---|---|---|
| 11:13 | `9a968cc` | «Старт учёт» положен в `uchet/` |
| 11:20 | `aa7f302` | `.env` убран из репозитория, вписан в `.gitignore` |
| 11:22 | `676eab9` | Проверка RLS: анонимный доступ закрыт, регистрация открыта |
| 11:59 | `51aa7a6` | Переменные в рантайме, вход только для сотрудников |

- [x] **Подтверждено по коду, а не по словам:** RLS включена на всех семи
      таблицах, политик для роли `anon` нет ни одной. Публичный ключ
      из страницы не открывает ничего. **Дыра была в другом:** политики
      выданы роли `authenticated` с условием «всегда истина», а на
      странице входа стояла регистрация. Любой заведённый аккаунт видел
      всех курсантов, оплаты и переписку.
- [x] **Найдена ловушка приоритета переменных.** При правке выяснилось:
      значения из сборки перебивали переменные хостинга. То есть при
      старом `.env` рядом со сборкой приложение **молча ходило бы
      в чужую базу**, а панель Amvera показывала бы правильный адрес.
      Порядок исправлен. Такую ошибку по логам не видно.
- [x] **Три переменные заданы в панели Amvera у `СТАРТ`** владелицей:
      `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`,
      `SUPABASE_SERVICE_ROLE_KEY`, все с этапом «Запуск».
      **В значении адреса была лишняя закрывающая скобка** — замечено
      на скриншоте, убрано. С ней приложение базу бы не нашло.

- [x] **Выкладка ПОЧИНЕНА и прошла** — запуск №10, 12.08 в 13:57,
      17 секунд, зелёная галочка. Сама она не заработала: два запуска
      подряд упали, и обе причины стоит запомнить.
      **Первая:** в `AMVERA_PROJECT` стояло `START` заглавными, а
      работает только **внутреннее имя маленькими латинскими** — `start`.
      В панели приложение подписано «СТАРТ», но это подпись для глаз;
      в пути кабинета стоит `start`.
      **Вторая:** в `AMVERA_USER` был логин от GitHub, а нужен от Amvera
      — `granatgold999`.
      **Записано в навык**, `references/klientskiy-proekt.md`, раздел про
      Amvera — пригодится на каждом следующем клиенте.
- [x] Заодно записано: окно поиска в логах Amvera по умолчанию
      «последние 30 минут». Выкладка была раньше — логи покажутся
      пустыми, хотя всё в порядке. На этом сегодня потеряли время.
- [ ] **Накатить три миграции в Supabase** — без последней
      (`staff_only`) правила останутся старыми и список сотрудников
      работать не начнёт.

### 152-ФЗ у автошколы: разобрано, но не решено

- [x] **Проверено по коду, куда уходят персональные данные.** Ответ:
      всё пишется в Supabase, то есть в Ирландию. Форма-заявка,
      телеграм-бот, бот ВК, разбор почты, ручной ввод в кабинете —
      все пять путей ведут в таблицу `applications`. Ни одного пути
      с первичной записью в российскую базу нет.
- [x] **Смягчающее:** публичной формы на сайте студии сейчас нет,
      страница `/vibe` данные не собирает. То есть с улицы никто ничего
      не отправляет — данные попадают через ботов и руками.
      Нарушение от этого не исчезает.
- [x] **Для САЙТА школы решение есть и оно простое** — вариант 1
      из навыка: ничего не хранить, заявка уходит в мессенджер и
      на почту. Половина уже построена: есть режим `PD_STORE=off`
      и отправка брифов. Сайт школы можно делать сразу правильно,
      не дожидаясь переезда базы.
- [ ] **Для КАБИНЕТА решения нет** — ему база нужна, значит вариант 3:
      Postgres в России. **Решение владелицы.** По записям той беседы
      переписывать приложение не придётся, Supabase открытый и ставится
      на российском сервере.

### ПЛАН СБОРКИ — «всё это надо собрать, и тогда будет порядок»

Слова владелицы 12.08. Здесь они превращены в список, иначе останутся
намерением. Порядок важен: каждый следующий пункт опирается на предыдущий.

**Шаг 1. Наш репозиторий — свести ветки**

- [x] **Живая ветка влита в рабочую.** Сделано 12.08. Пришло 102 файла:
      `src/`, собранные страницы, блог, гео; ушла дубль-сборка `tools/`.
      Конфликтов было четыре, все текстовые, ничего не потеряно.
      Проверено сборкой: 26 страниц, мета-теги чисто.
- [x] **Рабочая ветка влита в живую** `claude/multipage-website-okje3k`
      12.08 с разрешения владелицы. Слияние оказалось простым переносом
      (fast-forward), конфликтов не было — живую ветку влили в рабочую
      заранее, поэтому расхождений не осталось. Ответ GitHub:
      `14a598d..04f2331`.
      Перед отправкой проверено сборкой: 26 страниц, мета-теги чисто.
      Проверено поимённо, что доехало: `docs/15`, `docs/16`, `docs/17`,
      навык `mnogostranichnik-ru` (5 файлов), заготовка автошколы,
      правило про репозиторий клиента в `CLAUDE.md`.
      **Обе ветки после этого синхронизированы**, чтобы не разошлись снова.
- [x] **Выкатка сейчас НЕ нужна — проверено.** С последней выкатки
      (`c386aaa`, 11.08 вечером) папки `src/` и `public/` не менялись
      ни разу: `git diff c386aaa..HEAD -- src public` пуст. Всё, что
      сделано 12.08, — документы, навыки, заготовки и журнал. На сайте
      они не видны, и трогать живой сайт незачем.
      **Выкатывать в следующий раз, когда поменяется что-то на страницах.**
      Порядок тот же: `git fetch origin <ветка>`, потом
      `git push --force amvera origin/<ветка>:master`.
- [x] **Ветка по умолчанию переставлена на живую** —
      `claude/multipage-website-okje3k`. Сделано владелицей 12.08 в
      настройках репозитория, проверено на экране. До этого стояла
      `claude/stories-yandex-metrica-tracking-h7aing` без папки `src/`,
      и новые беседы начинали со старого кода. **Ловушка закрыта.**
      Попутно: она сначала нажала карандаш вместо значка `⇄` и попала
      в переименование ветки — GitHub не дал, потому что ветка с таким
      именем уже была. Значок переключения — правый из двух, `⇄`.
- [ ] **Заархивировать вторую копию `vibe-digital999`** на аккаунте
      `9rpkzh42qm-web` — она от 7 августа и ни разу не тронута.
      **Действие владелицы.** Архив, не удаление.
- [ ] **Решить, что делать с `main`.** Там дубль-сборка `tools/` и нет
      `src/`. Либо привести в порядок, либо оставить и не выкатывать.

**Шаг 2. Автошкола — собрать в один корень**

- [ ] **Решить, какой репозиторий корень.** Рекомендация —
      `autoschool-insight`: код бота и приложения уже там, двигать нечего.
      **Решение владелицы.**
- [ ] **Влить ветку бота** `claude/driving-school-vk-bot-b5ezmb`
      (+5565 строк) в главную ветку корня.
- [ ] **Положить в корень комплект** из `templates/autoschool-start/`:
      `CLAUDE.md`, `STATUS.md`, `TASKS.md`, `.gitignore`.
- [ ] **Скопировать туда навыки папками целиком:** `mnogostranichnik-ru`,
      `seo-audit-ru`, `texty-uslug`.
- [ ] **Разложить по папкам** `site/`, `app/`, `bot/`, `agent/`.
- [ ] **Удалить лишний пустой репозиторий** (в нём один README).
- [ ] **После переноса удалить `templates/autoschool-start/` отсюда.**

**Шаг 3. Знание — убрать расхождения**

- [ ] **Цена ИИ-агента: 40 000 или 60 000 ₽.** В `docs/15` одно,
      в четырёх других местах другое. **Решение владелицы**, потом
      привожу к одной везде.
- [ ] **Проверить, что ещё разъехалось** между `docs/15`, `llms.txt`,
      промптом бота и страницами: цены и сроки.

**Шаг 4. То, что висит на владелице (не про сборку, но мешает всему)**

- [ ] Amvera: ответ поддержки про инференс в регионе Москва_0.
- [ ] Amvera: что стоит в `LLM_MODEL` и в каком регионе приложение.
- [ ] `LEADS_KEY` и `SMTP`-переменные в Amvera.
- [ ] Метрика: цель `lead` — «совпадает» вместо «содержит».
- [ ] Метрика: фильтр своих визитов.
- [ ] Вебмастер: переобход новых адресов.

**Когда будет порядок.** Признак простой: **новая беседа по любому
проекту открывается и сразу знает, что делать, ни о чём не спрашивая.**
Пока это не так — сборка не закончена.

**Остановились на:** втором шаге из трёх. Порядок жёсткий и соблюдается:
Telegram → модель → сервер. Первый закрыт, второй упирается в один
взгляд в кабинет Amvera, третий сознательно не начат — сервер тратит
деньги с первого дня аренды.

**Следующий шаг для чужого чата:** попросить владелицу открыть кабинет
Amvera, раздел «LLM», и сказать три вещи — есть ли ключ, какие модели
доступны и как считаются деньги. Если ключ есть, дальше сразу сервер:
порядок в `docs/16`, разделы 4–7. Второй раз объяснять про `base_url`
не нужно, вопрос закрыт и записан.

**Ждём от Гульнары:**

- [x] Telegram. Сделано 12.08 — VPN, приложение, вход в свой аккаунт.
- [ ] Открыть кабинет Amvera → раздел «LLM» и сказать: есть ли ключ,
      какие модели в списке, как списываются деньги.
- [ ] **Спросить в поддержке Amvera, в какой стране стоят серверы
      инференса** (`inference.waw0.amvera.ru`). Это не про Hermes:
      на ответе держится обещание политики конфиденциальности сайта
      про обработку диалогов в России. Ответ «не в России» — править
      политику и подавать уведомление в РКН, независимо от агента.
- [ ] Не оплачивать ни подписку, ни сервер, пока не пройден пункт про
      кабинет Amvera. Подписку Codex или ChatGPT ради Hermes — не
      оплачивать вовсе, обычной подписки Hermes не хватает.

**Не доделано:**

- [ ] Сервер не арендован, Hermes не установлен — по плану, а не забыто.
- [ ] Не проверено, достучится ли российский сервер до `api.telegram.org`.
      Проверяется одной командой после создания сервера, `docs/16` раздел 4.
      Хостера брать с почасовой оплатой, чтобы неудачный сервер стоил
      копейки.
- [ ] Не проверено, тянет ли модель Amvera работу агента с инструментами.
      Выяснится только опытом, на задаче из `docs/16`, раздел 9.

**Отдельно, не про Hermes:** раз Telegram заработал, снят блокер, висевший
с 11.08 — токен у `@BotFather` теперь получить можно, и `api/tg-bot.js`
можно доводить. Владелице пока не предлагали: одна задача за раз.

**Внимание следующему чату про эту ветку.** `claude/vps-ai-agent-setup-oljp1o`
отведена от старой основы: здесь нет папки `src/`, зато лежит удалённая
в живой ветке дубль-сборка `tools/`. **На Amvera её не выкатывать** —
снесёт с сайта блог, гео-страницы и `/pakety/`. В ветке только текст:
`docs/16`, записи в `TASKS.md` и строка в `CLAUDE.md`. Их надо влить
в живую ветку `claude/multipage-website-okje3k`, а код отсюда не брать.

---

## 12 августа — итог дня

Сводка поверх подробных записей выше. Читать её первой, детали — там.

**Цель дня менялась дважды.** Начали с личного агента Hermes. К утру
выяснилось, что мешает не отсутствие агента, а бардак: три проекта
подряд оказались с расхождением между живым и репозиторием. Слова
владелицы: «этот бардак должен закончиться сегодня». Дальше день ушёл
на сборку.

**Сделано:**

- [x] **Ветка по умолчанию у `vibe-digital999` переставлена на живую.**
      Была документационная, без `src/`, — новые беседы начинали
      со старого кода. Ловушка закрыта.
- [x] **Живая и рабочая ветки сведены**, обе на одном коммите.
      Проверено сборкой: 26 страниц, мета-теги чисто.
- [x] **Выкатка не нужна** — с `c386aaa` папки `src/` и `public/`
      не менялись. Проверено diff, записано, чтобы не выкатывали зря.
- [x] **Написан навык `mnogostranichnik-ru`** — 5 файлов, переносится
      в клиентский проект копированием папки.
- [x] **Правило владелицы: репозиторий — это КЛИЕНТ**, остальное растёт
      из него. В `CLAUDE.md` и в навыке.
- [x] **Корень автошколы назван по компании** — `autoschool-start`,
      лишний пустой репозиторий удалён.
- [x] **Код «Старт учёта» спасён.** Жил только на Amvera, выгружен
      архивом, положен в `uchet/` рабочей ветки.
- [x] **Написаны `docs/16`, `docs/17`, `docs/18`** — агент, бот который
      умнеет, карта проектов.
- [x] **Три пересказа из интернета проверены**, во всех найдены ошибки.
      Правило записано в `CLAUDE.md`.

**Остановились на:** последней дыре в карте. У автошколы три приложения
на Amvera — `start-uchet`, `СТАРТ` (`start-granatgold999.amvera.io`)
и `start-avto` (`start-avto-granatgold999.amvera.io`). Известно, что
`start-uchet` — учёт курсантов. Какое из двух других то, над которым
работала беседа по автошколе, — не выяснено. Признак: пункты «Оплаты»
и «Бот ВКонтакте» в меню.

**Следующий шаг для чужого чата:** попросить владелицу открыть эти два
адреса и сказать, что в меню. После этого закрыть карту и только потом
браться за выкладку.

**Ждём от Гульнары:**

- [ ] Открыть `start-granatgold999.amvera.io` и `start-avto-granatgold999.amvera.io`,
      сказать, что в меню.
- [ ] Написать в поддержку Amvera: есть ли инференс в регионе Москва_0.
- [ ] Заархивировать вторую копию `vibe-digital999` на аккаунте
      `9rpkzh42qm-web`.
- [ ] Сказать, какая цена ИИ-агента верная — 40 000 или 60 000 ₽.
- [ ] `LEADS_KEY` и `SMTP` в Amvera, цель `lead` на «совпадает»,
      фильтр своих визитов, переобход в Вебмастере.

**Не доделано:**

- [x] **Карта автошколы закрыта.** `СТАРТ` — это дашборд заявок,
      целевое приложение репозитория. На сервере лежит сборка от 5.08:
      есть заявки, дашборд, телеграм-бот; нет «Оплат», бота ВК и
      псевдонимов. **Работа 11–12.08 не доехала до сервера.**
      Именно поэтому владелица не нашла «Оплаты» в меню.
- [x] **Опасение по выкладке снято.** В `.github/workflows/amvera.yml`
      указано целевое приложение `START` — значит выкладка не заденет
      `start-uchet`. Чинить можно.
- [x] **RLS в Supabase включена** — проверено по миграциям: четыре
      таблицы, 25 политик. Тревога про открытый ключ снята.
- [x] **`.env` разобран:** только адрес Supabase и публикуемый ключ,
      секретов нет. Но файл не вписан в `.gitignore`, поэтому и попал
      в git.

- [ ] **Вписать три доступа в секреты GitHub** — `AMVERA_USER`,
      `AMVERA_PASSWORD`, `AMVERA_PROJECT=START`. Это чинит выкладку
      и доставляет на сервер недельную работу.
- [ ] **Проверить RLS на таблице `documents`** — там медсправки,
      то есть сведения о здоровье. Из архива не видно.
- [ ] **Вписать `.env` в `.gitignore`.**
- [ ] **Проверить RLS в Supabase.** Открытый ключ безопасен только
      при включённой защите строк, а приложение собрано в Lovable,
      где она часто выключена. Без RLS ключ из кода страницы даёт
      доступ ко всей базе курсантов.
- [ ] **Загрузка Excel в учёт.** Сейчас импорт только скриптом
      с компьютера, поэтому нет июля и августа.
- [ ] **Данные автошколы в двух юрисдикциях:** учёт в Москве, второе
      приложение на Supabase за границей. Одни и те же люди.
- [x] **Поправка: сайт автошколы ЕСТЬ и работает.** `start-avto` на
      Amvera, семь страниц, статус «Запущено». Раньше здесь было
      записано «сайта нет» — неверно.
- [ ] **Положить код сайта в репозиторий.** Он тоже жил только на
      Amvera, репозиторий не подключён. Архив выгружен 12.08, ждёт
      переноса в `site/`.
- [ ] **У сайта автошколы нет домена.** Живёт на техническом адресе
      `start-avto-granatgold999.amvera.io`, в `nginx-site.conf` стоит
      `server_name _;`. Клиенту такой адрес не дашь — нужен свой домен.
- [ ] 14 веток в `autoschool-start`, снова начнётся разъезд.
- [ ] Форма заявки на `granat-kmv.ru` — проверить, доходят ли заявки.
- [ ] `main` в `vibe-digital999` не приведён в порядок.

---

## 12 августа, вечер — журнал разъехался по веткам, проверка перенесена в хук

**Цель дня:** ответить на вопрос владелицы «последняя правка сайта 11-го
числа, а в журнале не записано» — и убрать причину, а не только ответить.

**Что оказалось на самом деле.** Правки за 11 августа в журнале записаны:
`c386aaa` (права и сопровождение), сопровождение с ценами, правка про
генерацию картинок. Последняя правка сайта — `cec8e2a`, 11.08 в 17:59.
За 12 августа правок в `src/` и `public/` не было вовсе.

Соседний чат, который доложил «в журнале запись только за 10-е», читал
**свой** `TASKS.md` — из своей ветки. Журнал не один, их восемь, по одному
в каждой ветке, и они расходятся:

| Ветка | Строк в журнале | Докуда доведён |
|---|---|---|
| `vibe-digital-site-improvements-y7t2sw` | 2216 | 12 августа, 18:11 |
| `multipage-website-okje3k` (живая) | 2090 | 12 августа |
| `vps-ai-agent-setup-oljp1o` (эта) | 2090 | 12 августа |
| `main` | 2021 | 12 августа |
| `stories-yandex-metrica-tracking-h7aing` | 1024 | 11 августа |
| `vk-bot-setup-vuryya` | 769 | 11 августа |
| `granat-kmv-multipage-site-6h04uz` | 580 | **10 августа** |
| `new-session-s96ybp`, `site-verification-tag-manager-kjety3` | 137, 147 | дат нет вовсе |

Внизу списка — журнал, кончающийся 10 августа. Это его и прочитали.

**Сделано:**
- [x] `8b20fa1` **Подтянута ветка `vibe-digital-site-improvements-y7t2sw`**
      (слияние перемоткой, своего коммита нет) —
      она ушла вперёд на семь коммитов, до 18:16. Пришли: выкладка через
      GitHub Actions (`.github/workflows/deploy-amvera.yml`), рекламные
      макеты своей отрисовки (`marketing/`), три правки бота ВК и разбор
      расхождения цен на макетах с сайтом.
- [x] `3616588` **Хук считает свежесть памяти сам.** В `session-start.sh` добавлена
      функция `svezhee`: сравнивает `CLAUDE.md`, `STATUS.md` и `TASKS.md`
      в этой ветке со всеми ветками `origin` **по содержимому**, а не по
      дате. Печатает либо «все три файла у тебя самые свежие», либо
      «В ТВОЕЙ ВЕТКЕ УСТАРЕВШАЯ ПАМЯТЬ» с именем ветки, временем правки
      и готовой командой `git show <ветка>:<файл>`. Обе ветви проверены
      подстановкой старого журнала — работают.
- [x] `3616588` **В `CLAUDE.md` просьба заменена на факт.** Раздел «Первым делом
      в новом чате» больше не просит выполнить команду проверки: её
      выполняет хук. Записана и причина замены.
- [x] `3616588` **Правило хэшей в журнале.** У каждой строчки «сделано» теперь
      стоит короткий хэш коммита, и шаблон записи в `CLAUDE.md` тоже.
      Это делает сверку журнала с историей механической.

**Чем это лечится по существу.** Правило, которое выполняется
добровольно, рано или поздно не выполняется — сегодняшний случай третий
за три дня. Всё, что можно проверить программой, уходит в хук:

- `session-start.sh` — устарела ли память в этой ветке;
- `stop-journal.sh` — правили файлы и не дописали журнал.

**Остановились на:** правило хэшей записано, но задним числом к старым
записям хэши не проставлены. Проставлять всю историю не нужно —
достаточно начать со следующей записи. Проверка, что ничего не потеряно:

```
git log --format='%h %s' --since='7 days ago' -- src public
```

Каждый хэш из ответа должен встречаться в `TASKS.md`.

**Ждём от Гульнары:**
- [ ] Ничего по этой задаче. Всё сделано в репозитории.

**Не доделано в коде:**
- [ ] **Хуки живут не во всех ветках.** Новый `session-start.sh` есть
      только здесь и в тех ветках, куда его подтянут. Ветки, застрявшие
      на 10 августа, памятку про устаревшую память не увидят, потому что
      у них старый хук. Лечится слиянием, а по-хорошему — сокращением
      числа веток.
- [ ] **Восемь веток — сама причина.** Пока их восемь, любой файл памяти
      будет расходиться, а хук только сообщает об этом. Свести к двум:
      живая и рабочая.

### Дополнение к тому же дню: ответ «я прочитал журнал, этого не сделано»

**Что сказала владелица.** «Я уже боюсь, когда мне предлагают сделать
правки. Я прошу прочитай журнал, чтобы подстраховаться, а он говорит:
я почитал, этого не сделано. Так можно переделывать то, что вчера
сделала».

Это опаснее разъехавшихся журналов. Разъехавшийся журнал просто неполон.
А ответ «я прочитал, этого нет» звучит как выполненная проверка, хотя
проверки не было — и по нему принимается решение переделывать.

**Сделано:**
- [x] `e0acb9a` **Запрет в `CLAUDE.md`** — раздел «"Я прочитал журнал, этого
      не сделано" — так отвечать запрещено». Три причины, почему записи
      может не быть при сделанной работе, и порядок из трёх проверок
      до ответа: памятка хука → история всех веток → сам код живой ветки.
      Требуемая форма ответа: не «не сделано», а «в журнале не нашёл,
      проверил по истории — вот результат».
- [x] `e0acb9a` **Пункт 7 в памятке хука.** Печатается в начале каждой беседы
      независимо от ветки, вместе с командой проверки. Запрет в правилах
      живёт только в свежих ветках, а памятка — везде.

**Правило одной строкой:** живой сайт и история git старше любого
журнала. Журнал говорит, что мы ДУМАЛИ, что сделали; код говорит, что
сделано.

**Не доделано в коде:**
- [ ] Проверить эту привычку на деле: в следующий раз, когда журнал
      промолчит, а история покажет правку, — записать случай сюда.
      Пока правило записано, но ни разу не сработало на живом вопросе.

---

## 13 августа — девять веток сведены к двум

**Цель дня:** убрать причину, из-за которой память проекта разъезжалась.
Хук научился сообщать о расхождении 12-го числа, но сообщать — не лечить.
Пока веток девять, файлы памяти будут расходиться снова.

**Как было.** Девять веток, и в каждой свой сайт, свои правила и свой
журнал. Журналы отличались вдвое: 2216 строк в самой полной и 137 в самой
старой. Один чат читал память своей ветки и не знал про соседнюю.

**Порядок работы был такой: сначала опись, потом спасение, потом
удаление.** Не наоборот — иначе теряется то, чего никто не хватится
сегодня.

**Сделано:**

- [x] `d5c092e` **Опись: что есть только в одной ветке.** Посчитаны
      «свои» коммиты каждой ветки и файлы, которых нет в живой. Нашлось
      три места с настоящей потерей и ни одного очевидного заранее.
- [x] `d5c092e` **Спасено 1817 строк по Гранату** в `docs/granat-kmv/`:
      семь документов из `site-verification-tag-manager-kjety3` (права
      в Вебмастере, гео-план, услуга продвижения, оптимизация, разбор
      двух сайтов, цены, Директ) и два из `new-session-s96ybp` (аудит
      08.08 и комплект целей Метрики со скриптом). **Номера в именах
      убраны:** файлов `docs/12-…` было три разных, и склеить их
      в одну папку с номерами было нельзя. Таблица соответствия старых
      и новых имён — в `docs/granat-kmv/README.md`.
- [x] `d5c092e` **Чек-лист бота ВК стал навыком** `.claude/skills/vk-bot-ru/`,
      322 строки. В самом файле стояла оговорка «номер не ставлю, потому
      что номера разъехались» — диагноз верный, лечение неполное: документ
      в одной ветке в другие проекты всё равно не попадает. Теперь папка,
      которая копируется целиком.
- [x] `d5c092e` **Сборка проверена после переноса:** 26 страниц,
      мета-теги чисто.
- [x] `add86dc` **История шести веток вшита в живую** слиянием стратегией
      `ours`. Дерево не изменилось ни на байт — проверено сравнением хэша
      дерева до и после. Смысл: ветки удаляются, а их коммиты остаются
      достижимы, и `git show <хэш>` откроет любой из них через год.
      Метки `arhiv/*` сделать не вышло: GitHub запретил этой сессии
      пуш тегов, ответ 403.
- [x] `add86dc` **Живая ветка и `main` обновлены до одного коммита.**
      Впервые `main` не опасен: вторая система сборки `tools/` из него
      ушла, страниц 26 вместо девяти.
- [x] `6aa3607` **Карта веток в `STATUS.md` переписана** под новое состояние,
      с таблицей «что было только в этой ветке и куда переехало»
      и хэшами всех удаляемых веток.
- [x] `6aa3607` **Хук перестал ругаться на собственную свежую работу.**
      Если правка из другой ветки уже входит в нашу историю, тревоги
      нет: наша версия новее, просто отличается. Поймано на себе.
- [x] `6aa3607` **Из памятки убрано «main на сайт не выкатывать»** — неверно
      с сегодняшнего дня.

**Остановились на:** удалить шесть веток на GitHub не получилось —
у этой сессии нет права удалять ветки, ответ 403 на каждую. Всё
остальное сделано: содержимое спасено, история вшита, живая и `main`
совпадают. Ветки удаляются в одно нажатие на GitHub и уже помечены там
как влитые.

**Ждём от Гульнары:**
- [ ] **Удалить шесть веток на GitHub.** Открыть
      `https://github.com/gulnaragold999m-tech/vibe-digital999/branches`,
      нажать корзину напротив каждой:
      `claude/granat-kmv-multipage-site-6h04uz`,
      `claude/new-session-s96ybp`,
      `claude/site-verification-tag-manager-kjety3`,
      `claude/stories-yandex-metrica-tracking-h7aing`,
      `claude/vibe-digital-site-improvements-y7t2sw`,
      `claude/vk-bot-setup-vuryya`.
      Ничего не потеряется: всё уже в живой ветке и в `main`.
      **Не трогать** `claude/multipage-website-okje3k` и `main`.

**Не доделано в коде:**
- [ ] **Ветка этой беседы `claude/vps-ai-agent-setup-oljp1o`** совпадает
      с живой и удаляется вместе с остальными, когда беседа закончится.
- [ ] **`docs/granat-kmv/` лежит не на своём месте.** По правилу
      «репозиторий — это клиент» знание о Гранате должно жить
      в репозитории Граната. Спасали в спешке — раскладывать потом.
- [ ] **Ветки будут появляться снова:** каждая новая беседа заводит свою.
      Это устройство Claude Code, а не поломка. Правило записано
      в `STATUS.md`: ветка живёт, пока идёт работа, и вливается в живую
      в конце. Накопилось больше трёх — свести, как сегодня.
- [ ] **Цены в `docs/granat-kmv/` — снимок на 08.08** и с тех пор
      менялись. Сверять с сайтом, прежде чем показывать кому-нибудь.

### 13 августа, вторая половина — три организации, три ствола

**Что сказала владелица.** «Создали репозиторий на организацию — что
у этой организации есть? Сайт, бот, может много ботов. Как есть ствол
и есть корни. На сегодня у меня три проекта, три организации: Вайб
Дижитал, Гранат и автошкола. Все со своими корнями. И все беседы здесь
удалить, уже мусорная свалка какая-то. **Журналы не помогают.**»

**Последняя фраза — самая важная за два дня.** Журнал разросся до 2300
строк, и это не поломка, а его назначение: он написан для беседы, которая
начинается с нуля. Владелице он не помогает и не должен. Ей нужна была
страница на один экран, а её не было ни в одном репозитории.

**Проверено, а не предположено:** три репозитория уже существуют и ровно
совпадают с тремя организациями — `vibe-digital999` (07.08, открытый),
`granat-kmv` (08.08, открытый), `autoschool-start` (05.08, закрытый).
Стволы стояли, корней не было.

**Сделано:**
- [x] `6a0b32a` **`README.md` переписан как карточка организации** — для
      владелицы, а не для чата. Один экран: восемь корней с ответом
      «работает или нет», шесть поломок по порядку убытка, выкатка
      с телефона, состояние веток. Прежний `README.md` был про папки
      и `npm install` — то есть для программиста.
- [x] `6a0b32a` **Правило в `CLAUDE.md` подняли с клиента до организации.**
      Было «репозиторий — это КЛИЕНТ», стало «репозиторий — это
      ОРГАНИЗАЦИЯ, он ствол». Таблица трёх организаций с репозиториями
      и одинаковые имена корней: `site/`, `app/`, `bot/`, `agent/`,
      `docs/`.
- [x] `6a0b32a` **Записано, почему у этого репозитория корни названы иначе.**
      Сайт студии лежит в `src/`+`public/`+`server.js`, боты в `api/`.
      Переезд в `site/` — это правка `amvera.yaml`, путей сборки
      и проверка на живом сайте. Отдельная работа, походя её делать
      нельзя. В новых репозиториях сразу называть как в таблице.
- [x] `6a0b32a` **Заведена папка `korni/`** — то, что переносится в другую
      организацию: `shablony/` с четырьмя файлами памяти и готовая фраза,
      которую владелица вставляет в беседу, открытую в чужом репозитории.
      Фраза работает, потому что `vibe-digital999` открытый: беседа
      скачивает его и делает по инструкции. `templates/autoschool-start/`
      переехала сюда же — двух мест для одного не держим.
- [x] `6a0b32a` **Снято устаревшее** предупреждение про `main` в `STATUS.md`
      (осталось с утра, до сведения веток).

**Остановились на:** доступа к `granat-kmv` и `autoschool-start` эта
беседа не получила — оба вызова вернули «требуется подтверждение».
Поэтому корни там раскладываются не мной, а беседой, открытой в том
репозитории, по фразе из `korni/README.md`.

**Ждём от Гульнары:**
- [ ] **Удалить шесть веток на GitHub** (см. запись выше за 13 августа).
- [ ] **Открыть беседу в `granat-kmv`** и вставить фразу из
      `korni/README.md`. Потом то же для `autoschool-start`.
- [ ] **Удалить старые беседы** — их удаляет только владелица,
      на `claude.ai/code`. Удалять стало безопасно: всё, что беседы
      наработали, лежит в репозиториях, а не в переписке.

**Не доделано в коде:**
- [ ] **`docs/granat-kmv/` лежит не в своей организации.** Переедет
      в `granat-kmv`, когда там разложат корни.
- [ ] **Корни этого репозитория не переименованы** в `site/`, `bot/`.
      Только когда будет время проверить выкатку на живом сайте.

### 13 августа, вечер — цены на сайте выровнены по боту

**Что попросила владелица:** «Выровняй цены на сайте. Я знаю, что
у бота цены правильные в ВК. И на сайте они должны быть правильные».
Прайс бота принят за источник правды.

**Сначала сверил всё со всем.** Прайс бота (`api/brief.js`) против
страниц услуг, главной, `src/pages.js` и рекламных макетов.
**Двенадцать позиций из тринадцати совпали** — лид-бот, бот записи,
ИИ-агент, оба лендинга, сайт 5–10 страниц, приложение, автоматизация,
аудит, карты, сопровождение и три пакета.

**Разошёлся квиз-калькулятор на главной** — и разошёлся молча, потому
что это отдельная копия цен внутри скрипта:

| В квизе было | В прайсе и у бота |
|---|---|
| Лендинг от 25 000 ₽ | от 35 000 ₽ |
| Лендинг до 60 000 ₽ | сайт 5–10 страниц 90 000 ₽ |
| ИИ-агент от 45 000 ₽ | от 60 000 ₽ |
| Лендинг «от 3 дней до полутора недель» | срок от 7 дней |
| Лид-бот «от 2 до 4 дней» | от 7 дней |

Человек проходил квиз и получал цифру, которой нет ни на странице,
ни у бота. Над самим объектом стоял комментарий «взяты из прайса
на странице, чтобы цифры не расходились» — то есть расхождение
предвидели, но проверить было нечем.

**Сроки разъехались шире цен.** «От 3 дней» стояло в четырёх местах:
описание сайта для поисковика, первый экран и ответ в блоке вопросов
(дважды — в тексте и в разметке для поисковика). При этом в прайсе
на той же странице — «Срок от 7 дней». Это уже не цифра, а обещание,
которое нечем закрыть.

**Сделано:**
- [ ] `e516fa8` **Квиз-калькулятор выровнен** (в коде; на сайте появится после выкатки)**:** лендинг 35 000 – 90 000
      «от 7 дней до трёх недель», лид-бот «от 7 дней», ИИ-агент
      от 60 000 «от 14 дней». Над объектом записано, почему цифры
      обязаны совпадать с двумя другими местами.
- [ ] `e516fa8` **Убраны обещания «от 3 дней»** (в коде, ждёт выкатки) из описания для поисковика,
      с первого экрана и из ответа про сроки — везде «от 7 дней»,
      с добавленной строкой про сайт 5–10 страниц «от трёх недель».
      «3 дня» осталось там, где верно: SEO-аудит и Яндекс Карты.
- [x] `13371c7` **Рекламный макет аудита по 152-ФЗ:** штрафы были 300 000
      и 60 000, на сайте у тех же нарушений — 700 000 и 100 000.
      Выровнено по сайту. В рекламе аудита по 152-ФЗ разойтись
      с собственным сайтом — то, на что клиент укажет первым.
- [x] `e516fa8` **Сборка проверяет цены сама.** В `build.js` добавлена
      `validatePrices()`: берёт все суммы из прайса бота и сверяет
      с нижними границами калькулятора. Печатает «Проверка цен:
      калькулятор совпадает с прайсом бота» либо список расхождений.
      **Проверено подстановкой поломки** — ловит.
- [x] `e516fa8` **Правило в `CLAUDE.md`:** «Цена живёт в трёх местах».
      Где именно, что прайс бота — источник правды, что сроки
      проверяются так же, что макеты в `marketing/` тоже сверяются.

**Остановились на:** правки лежат в репозитории, **на живом сайте их
ещё нет**. Главная страница собрана вручную, сборка её не трогает,
поэтому увидеть изменения можно только после выкатки.

**Ждём от Гульнары:**
- [ ] **Выкатить сайт** — GitHub → Actions → «Выкатить сайт на Amvera»
      → Run workflow → ветка `claude/multipage-website-okje3k`.
- [ ] **Проверить после выкатки:** открыть `vibe-digital999.ru`, нажать
      кнопку квиза, выбрать «Сайт или лендинг» и дойти до конца.
      Должно показать «от 35 000 ₽ до 90 000 ₽», а не «от 25 000 ₽».

**Не доделано в коде:**
- [ ] **Проверяются только нижние границы калькулятора.** Верхняя
      (120 000 у агента) ни с чем не сверяется — это конец вилки,
      отдельной цены за ним нет.
- [ ] **Страницы услуг проверка не трогает.** Если цена разойдётся
      в `src/pages/`, сборка промолчит. Пока сверялось руками.

**Поправка к записи выше, в тот же вечер.** Галочки `[x]` были
проставлены сразу после правки кода — то есть с нарушением
собственного правила: `[x]` ставится, когда результат ВИДЕН, а сайт
ещё не выкачен. Исправлено, две строки вернулись в `[ ]`.

И заодно нашлось то, что правкой файла не чинится:

- [x] `13371c7` **Картинка макета перерисована.** Правка `creatives.js`
      сама по себе ничего не меняет: в рекламу идут готовые `.png`
      из `marketing/out/`. Перерисован `audit.png` — остальные три
      совпали побайтово, значит вчерашнюю правку цен до картинок
      довели. Рисуется так: `npm install --no-save playwright`,
      потом `node marketing/creatives.js`.
- [x] `13371c7` **Таблица в `marketing/README.md` отстала от макетов:**
      бот значился за 25 000 ₽, агент за 40 000 ₽ — старые цены,
      хотя на самих картинках уже 15 000 и 60 000. Выровнено.
- [x] `13371c7` **У макетов своя проверка цен и она работает** — скрипт
      печатает «цены сверены с src/pages.js: совпадают». То есть
      расхождение было не в ценах услуг, а в суммах штрафов,
      которые эта проверка не смотрит.

### 13 августа, поздний вечер — сроки на рекламных макетах

**Что показала владелица:** свой рекламный макет аудита рядом со снимками
живого сайта на телефоне. Одна фраза: «Здесь что».

**Что оказалось.** Три вещи, и все разные.

**1. Её макет — не из этого репозитория.** У него другая вёрстка: коробка
«ПАКЕТ» в перспективе и колонка из четырёх пунктов справа. Ни одна версия
`marketing/creatives.js` такого не рисует — сверено с первой версией
`644f47f` и с сегодняшней. Значит, макет сделан где-то ещё, и наши правки
до него не доходят по определению.

**2. Штрафы на её макете старые** — 300 000 и 60 000 против 700 000
и 100 000 на сайте. В нашем макете это уже исправлено сегодня
(`e516fa8`), в её файле — нет.

**3. Сроки разъехались во всех четырёх макетах, и этого я не заметил
утром.** Цену проверял скрипт, срок не проверял никто:

| Макет | Было на макете | На странице услуги |
|---|---|---|
| Лендинг | запуск от 3 дней | **от 7 дней** |
| Бот | срок 2–3 дня | **от 7 дней** |
| ИИ-агент | запуск от 2 недель | **от 14 дней** |
| Аудит | отчёт за 3–5 дней | **3 дня** |

Плюс та же ошибка внутри нарисованного диалога на макете агента: бот
на картинке отвечал «срок 2–3 дня».

**Сделано:**
- [x] `43dab70` **Сроки на четырёх макетах выровнены** по блоку «Срок»
      на страницах услуг. Поправлен и текст в нарисованном диалоге.
- [x] `43dab70` **Проверка сроков добавлена в генератор макетов.** Берёт срок
      из `src/pages/*.html` и сравнивает ЧИСЛА, а не слова: обёртка
      у макета своя («запуск от», «отчёт за»), и это нормально.
      Печатает «цены и сроки сверены с сайтом: совпадают» либо падает
      с указанием расхождения. **Проверено подстановкой поломки.**
- [x] `43dab70` **Все четыре картинки перерисованы.**

**Вывод, который стоит запомнить:** проверка ловит только то, что ей
велели смотреть. Цены сверялись с 11.08 и держались; сроки не сверялись
и разъехались за неделю. **Заводя проверку, спрашивать себя: что ещё
в этом файле обещано клиенту?**

**Ждём от Гульнары:**
- [ ] **Заменить рекламные макеты** там, где они уже стоят: ВК, 2ГИС,
      объявления. Старый файл с «3–5 дней» и «до 60 000 ₽» — заменить
      на новый.
- [ ] **Сказать, откуда взялся макет с коробкой.** Если его рисует
      другая беседа — там та же ошибка в сроках, и её надо чинить там же.

**Не доделано в коде:**
- [ ] **Сроки на главной проверкой не охвачены** — сверяются только
      макеты и калькулятор. Блок прайса на главной по-прежнему держится
      на внимательности.

### 13 августа, ночь — один прайс на весь проект

**Что попросила владелица:** «Мы можем подправить прайс на сайте
и забыть про бота. Это должно быть автоматическое обновление. Цена
по пакетам, по акциям везде одинаковая — в макетах, в рекламе».

Правильная просьба. До этого дня цену держали проверки, а не устройство:
она была записана руками в четырёх местах, и каждая проверка ловила
расхождение уже после того, как оно случилось.

**Сделано:**
- [x] `fa35ee5` **Заведён `src/prices.js` — единственный источник цен**
      и сроков. Десять услуг, три пакета, сопровождение, акция на аудит.
- [x] `fa35ee5` **Прайс бота собирается из него.** В `api/brief.js` три
      написанных руками блока — услуги, сопровождение, пакеты — заменены
      подстановкой. Просьба «подправить на сайте и забыть про бота»
      выполнена буквально: бот больше не хранит своих цен.
- [x] `fa35ee5` **Макеты берут цену и срок оттуда же.** В `creatives.js`
      у каждого макета осталась только ссылка на услугу и своё слово
      перед сроком — «запуск», «срок», «отчёт за».
- [x] `fa35ee5` **Разметка Offer для поисковиков** — из прайса, а не числом
      в `src/pages.js`.
- [x] `fa35ee5` **Страницы услуг: `{{цена:id}}` и `{{срок:id}}`.** Сборка
      подставляет. Проставлено на пяти страницах — сайт, бот, агент,
      приложения, вайбкодинг.
- [x] `fa35ee5` **Цена пакета не записана числом** — считается как сумма
      состава. Пакет не может разойтись с услугами, из которых собран.
- [x] `fa35ee5` **`npm run ceny`** — печатает весь прайс одной командой.
- [x] `fa35ee5` **Проверка переписана.** Автоматически подставить нельзя
      в одно место — `public/index.html` собран вручную. Его сборка
      и проверяет: разметку Offer и нижние границы калькулятора.
      **Проверено подстановкой поломки** — ловит.

**Проверка сквозная, а не на словах.** Поменял в прайсе ИИ-агента
с 60 000 на 66 000 и пересобрал. Приехало во все четыре места сразу:
страница услуги «От 66 000 ₽», разметка `"price": "66000"`, прайс бота
«ИИ-агент с базой знаний — от 66 000 ₽», макет «от 66 000 ₽». Вернул
обратно.

**Про регионы.** Цены одинаковые во всех регионах, и это записано
как решение, а не как случайность: сайт продаёт по всей России,
а разная цена на гео-страницах читается как обман, если человек
откроет две. Передумаем — менять в `src/prices.js`, а не в тексте
страниц.

**Ждём от Гульнары:**
- [ ] **Выкатить сайт** — GitHub → Actions → «Выкатить сайт на Amvera»
      → Run workflow → ветка `claude/multipage-website-okje3k`.
      Одной выкаткой уедут и утренние правки калькулятора, и это.
- [ ] **Проверить бота после выкатки:** написать ему в ВК «сколько
      стоит ИИ-агент» — должен ответить «от 60 000 ₽, от 14 дней».
- [ ] **Заменить рекламные макеты** в ВК, 2ГИС и объявлениях.

**Не доделано в коде:**
- [ ] **Главная — единственное место, куда цена не подставляется.**
      Собрана вручную, сборка её не трогает. Пока только проверяется.
      Чтобы подставлять и туда, её надо провести через сборку —
      отдельная работа с проверкой на живом сайте.
- [ ] **Плейсхолдеры стоят на пяти страницах из двадцати шести.**
      Остальные упоминания цен — в тексте абзацев, и там подстановка
      потребует переписать фразы. Делать по одной, а не разом.
- [ ] **Гео-страницы** (Пятигорск, Ессентуки, КМВ, два края) цены
      пока держат текстом. Они совпадают, но не подставляются.

### 13 августа — проверка бота в ВК: цена верная, канал выдуман

**Что показала владелица:** снимок переписки с ботом в сообществе.
Ответ про ИИ-агента: «с одной базой знаний и каналом — от 60 000 ₽.
Это базовый запуск: один сценарий, **интеграция с Telegram**, обработка
типовых вопросов».

**Цена верная** — 60 000 ₽, совпадает с прайсом. Но проверка эта
про прайс ничего не доказывает: 60 000 стояло у бота и до сегодняшних
правок. Что единый прайс доехал, покажет только выкатка.

**Найдено другое, и это дороже цены.** Человек пишет **во ВКонтакте**,
а бот обещает интеграцию с **Telegram**. В промпте стояло «запуск
с одним каналом» — без указания, с каким. Бот дописал сам и выбрал
не тот: клиент читает это как «ВКонтакте в базовый запуск не входит»,
хотя входит и работает.

Это повтор ошибки, разобранной 11.08 («бот выдумывал каналы связи
в брифе»). Тогда правило записали только для брифа — строка про каналы
в шаблоне письма. В разговорной части такого правила не было, и дыра
осталась ровно там, где её не закрыли.

**Сделано:**
- [ ] `65b1a5e` **Правило про канал в промпте бота:** «один канал» — это тот,
      где идёт разговор; конкретный мессенджер по своей инициативе
      не называть; Telegram по умолчанию не подставлять; клиент
      не назвал — «один канал на выбор: ВКонтакте, Telegram или сайт».

**Вывод.** Закрывая ошибку, спрашивать: где ещё в этом файле
то же самое? 11.08 закрыли в письме и не закрыли в разговоре —
и через два дня она вышла тем же боком, но перед живым клиентом.

**Ждём от Гульнары:**
- [ ] **Выкатить** — тогда уедут разом: единый прайс, правка калькулятора
      и это правило. До выкатки бот отвечает по-старому.
- [ ] **Переспросить бота после выкатки** тем же вопросом из ВК:
      Telegram по своей инициативе называть больше не должен.

### 13 августа — разбор третьего пересказа: репозиторий agency-agents

**Что принесла владелица:** пересказ ролика про «полноценное AI-агентство
на GitHub: 147 агентов в двенадцати отделах, бесплатно».

**Проверено по первоисточнику, а не по пересказу:**

| Утверждение в ролике | Что на самом деле |
|---|---|
| Репозиторий `msitarzewski/agency-agents` | **Есть.** 145 458 звёзд, 23 517 форков, публичный, MIT, заведён 13.10.2025 |
| 147 агентов в 12 отделах | **230+ агентов в 18 отделах.** Ролик просто старый |
| Ставится в Claude Code и Cursor | **Правда**, плюс ещё десяток инструментов, `./scripts/install.sh` |
| «Ничего не стоит» | **Правда**, лицензия MIT |
| «Сделки от 150 000 по ссылке в шапке» | Воронка курса. К репозиторию отношения не имеет |

**Новое по сравнению с двумя прошлыми разборами.** Раньше пересказы
завышали цифры («на 6%» превращалось в «на 20%»). Здесь наоборот:
цифры занижены, потому что ролик отстал от репозитория на несколько
месяцев. **Вывод тот же, причина другая:** цифру из пересказа проверять
всегда, независимо от того, в какую сторону она врёт.

**Что делает установщик — проверено чтением скрипта:** sudo не просит,
в сеть не ходит, но пишет **в домашнюю папку**: `~/.claude/agents/`
и ещё десяток каталогов других инструментов. То есть агенты появятся
сразу во ВСЕХ проектах — и в автошколе, и в Гранате.

**Решение (не принято, ждёт владелицу):** целиком не ставить. Их агенты
не знают ни ВК, ни Amvera, ни 152-ФЗ, ни нашего прайса — то есть ровно
того, из-за чего сегодня чинили цены. Разумнее взять две-три роли
из отделов, которых у нас нет (Paid Media, Sales, Finance), прочитать
и переписать в наш формат навыка по-русски и со своими цифрами.

**Не доделано:**
- [ ] Решить с владелицей, брать ли роли из agency-agents и какие.

### 13 августа — два новых навыка из agency-agents

**Владелица сказала «Бери».** Взял из открытого репозитория
`msitarzewski/agency-agents` метод трёх ролей и переписал под студию.
Целиком репозиторий не ставили — почему, разобрано записью выше.

**Что взято и во что превратилось:**

| Их роль | Наш навык |
|---|---|
| `sales/sales-proposal-strategist` | `kp-i-cena` |
| `sales/sales-deal-strategist` | `kp-i-cena` |
| `paid-media/paid-media-auditor` | `reklama-audit-ru` |

**Взят метод, а не текст.** Их пороги и метрики про Google Ads
и американский рынок: «улучшение на 15–30%», MEDDPICC, RFP,
procurement. Полезен порядок действий, а не цифры.

**Сделано:**
- [x] `5469f87` **Навык `kp-i-cena`** — заявка → КП → торг → сделка.
      Главное: цена считается от стоимости задачи клиента, а не
      от наших часов, и называется ТРЕТЬЕЙ, после потери и решения.
      Скидку не предлагаем первыми: просьба о скидке значит, что
      ценность не показана. Скидка только в обмен — предоплата,
      портфолио, отзыв, рекомендация. Торг о сроке важнее торга
      о цене. Шесть признаков, что сделки не будет, и правило
      «лучше отказать за неделю, чем потратить месяц».
      Цены берутся из `src/prices.js`, а не выдумываются.
- [x] `5469f87` **Навык `reklama-audit-ru`** — Директ, ВК Реклама, 2ГИС,
      Яндекс Бизнес. Порядок проверки: сначала счёт, потом деньги,
      потом запросы, потом креатив, потом посадочная. Начинать
      с креатива бессмысленно — пока заявка не меряется, сравнивать
      нечем. Внутри свои грабли: счётчик, меривший карточку вместо
      сайта; цель по условию «содержит» вместо «совпадает»;
      зонтичная цель; замер «до» до запуска.
- [x] `5469f87` **Списки навыков обновлены** в `CLAUDE.md` и `README.md`:
      было четыре, стало шесть.

**Честная оговорка в обоих навыках:** их обещание «улучшение
на 15–30%» клиенту не повторять — это их цифра по их рынку,
подтвердить нам её нечем.

**Не доделано:**
- [ ] **Навыки не проверены на живой сделке.** Первая же заявка —
      пройти по `kp-i-cena` и записать, где он врёт.
- [ ] **Отдела Finance не брали.** Их пять ролей — про анализ
      компаний и налоги США, к ценообразованию студии отношения
      не имеют. Ценообразование пока живёт в `docs/granat-kmv/ceny.md`
      и в самом прайсе.

## 15 августа — седьмая роль в комнате: маркетолог

**Цель дня:** чтобы сайт перестал начинаться с вёрстки. Владелица
попросила роль, которая при заявке «сделать сайт» сначала изучает
аудиторию заказчика по сегментам, расписывает потребности, боли,
возражения и страхи, определяет ступень лестницы Ханта, разбирает
рынок и конкурентов и формулирует УТП — и чтобы все эти выводы
обязательно попадали на страницы.

**Оформлено навыком, а не документом в `docs/`** — по правилу
проекта: пригодится в другом проекте, значит навык. Клиентский
репозиторий получит его копированием папки, вместе со скриптом.

**Сделано:**
- [x] `eb6a7db` **Навык `.claude/skills/marketolog-issledovanie/`.**
      Семь шагов: снять факты с заказчика → сегменты → лестница
      Ханта → конкуренты → УТП → карта смыслов → проверка.
      Пять справочных файлов: `anketa.md`, `segmenty.md`,
      `lestnica-hanta.md`, `konkurenty.md`, `uto.md`,
      `shablon-issledovaniya.md`.
- [x] `eb6a7db` **Скрипт `scripts/proverka-smyslov.mjs`.** Читает
      таблицу «Карта смыслов» из исследования и по каждой строке
      смотрит собранный сайт: есть ли страница и встречается ли
      опорная фраза. Печатает, что не доехало, и завершается
      с ошибкой. Проверен на пяти случаях: фраза найдена, фразы
      нет, страницы нет, проверка глазами (`—`), две формулировки
      через « или ».
- [x] `eb6a7db` **Порядок навыков записан в `CLAUDE.md`:**
      маркетолог → многостраничник → тексты → SEO-аудит →
      КП и реклама. Раньше сайт начинался со структуры.
- [x] `eb6a7db` Списки навыков обновлены: `CLAUDE.md`, `README.md`,
      `korni/shablony/CLAUDE.md`. Было шесть, стало семь.

**Что в навыке главное, если читать одну строчку.** Вывод, не
получивший места на странице, — выброшенный вывод. Так гибнет
большинство «анализов ЦА»: тридцать страниц разбора, сайт как был.
Поэтому исследование заканчивается не выводами, а таблицей «смысл →
страница → блок → опорная фраза», и её сверяет скрипт. Правило,
которое выполняется добровольно, рано или поздно не выполняется —
это уже записано в правилах про журнал, здесь то же самое.

**Три вещи, которые навык разводит по разным блокам сайта:**
боль (что болит сейчас), возражение (что скажет вслух, услышав цену)
и страх (что подумает и промолчит). Их обычно валят в один список
«боли ЦА», после чего страница отвечает не на то. Страх текстом
не лечится вообще: «мы надёжные» его усиливает, снимает его только
устройство сделки — оплата по этапам, домен на клиента, показ работы
в процессе.

**Запреты, записанные в навык:** не выдумывать боль (у каждой строки
источник, иначе помечается «гипотеза»), не спрашивать заказчика «кто
ваша целевая аудитория» (ответ будет «все»), не писать «самые лучшие»
и «№1» — ФЗ «О рекламе» ст. 5 ч. 3, Директ не пропустит, ФАС
оштрафует за текст на сайте.

**Остановились на:** навык собран и зарегистрирован, но исследования
по НАШЕМУ сайту ещё нет — его нельзя написать в одиночку, факты
снимаются с владелицы. Следующий шаг: задать ей семь обязательных
вопросов из `references/anketa.md` по одному, начиная с «назовите трёх
последних клиентов: кто, с какой задачей пришёл, сколько заплатил».
Из ответов собрать `docs/marketing/issledovanie.md` и прогнать скрипт
по текущему сайту — он покажет, каких смыслов на страницах нет.

**Ждём от Гульнары:**
- [ ] **Ответы на семь вопросов анкеты** — по одному, в чате. Без них
      исследование по своему сайту не делается: выдуманные боли
      попадут на страницы и не сработают.
- [ ] **Утвердить УТП письменно**, когда оно будет сформулировано.
      Вопрос при утверждении — не «нравится ли», а «что из этого вы
      готовы выполнять каждый день»: обещание, которое компания
      не выполнит, ломает не сайт, а отзывы.

**Не доделано в коде:**
- [ ] **Исследование по своему сайту** — `docs/marketing/issledovanie.md`
      не существует. Пока его нет, тексты сайта опираются на догадки.
- [ ] **Навык не проверен на живой работе.** Первый же заказ на сайт
      пройти по нему целиком и записать сюда, где он оказался неудобен.
      Та же оговорка стоит в самом навыке — заказчику его не выдавать
      как «проверенную методику агентства».
- [ ] **Проверка смыслов не встроена в `build.js`.** Пока запускается
      руками. Встраивать имеет смысл, когда появится первое живое
      исследование, — иначе сборка будет ругаться на отсутствующий файл.

## 15 августа, вторая половина — маркетолог получил инструменты

**Цель дня:** владелица прислала развёрнутое задание на роль
маркетолога с фреймворками. Утренняя версия навыка описывала, ЧТО
разбирать; эта — ЧЕМ разбирать. Разница практическая: «распиши боли»
даёт сочинение, «собери Job Story по формуле» даёт проверяемую строку.

**Сделано:**
- [x] `8a54d35` **`references/jtbd.md`** — Job Story «Когда …, я хочу …,
      чтобы …»; четыре силы (толчок, притяжение, тревога, привычка)
      и карта барьеров Кюблер-Росс: отрицание → сопротивление → торг →
      сомнение → принятие, по каждой стадии свой блок сайта.
- [x] `8a54d35` **`references/struktura-lendinga.md`** — PMPH для
      холодных ступеней Ханта, AIDA для тёплых, таблица порядка блоков
      с источником смысла, правило про интерактив на своём коде.
- [x] `8a54d35` **TOWS в `konkurenty.md`** — матрица «слабость
      конкурента × наша сила = строчка на сайте» и бенчмаркинг
      по слоям: вес страницы, экранов до цены, полей в форме, время
      ответа. Сравниваем путь, а не картинки.
- [x] `8a54d35` **XYZ-формула УТП в `uto.md`** с проверкой «выкинь
      третий блок»: осталось верным для конкурента — отличия нет.
- [x] `8a54d35` **Протокол выдачи** в `SKILL.md` и `anketa.md`: три
      вводных до анализа, дальше порциями, сегменты первыми
      на подтверждение.
- [x] `8a54d35` В `CLAUDE.md` дописан состав фреймворков и правило
      про порции.

**Три вещи в задании, которые пришлось поправить, — и почему:**

1. **Домен.** В задании стояло `vibe-digital.ru`. Наш —
   `vibe-digital999.ru`. В навык записан наш, иначе он уехал бы
   в тексты клиентских сайтов.
2. **Цифра «не теряйте до 50% кликов из-за долгой загрузки».** Чужое
   исследование по чужому рынку, подтвердить нечем. В навыке записано
   правило: чужую цифру не ставим, ставим свой замер — «страница весит
   столько-то, их столько-то, замерено такого-то числа». Он проверяем
   и потому убедителен. Заодно там же: «65 КБ чистого кода» —
   не константа студии, а результат конкретного замера, мерить каждый
   раз заново.
3. **«Офферы для победы в аукционе».** Аукцион Директа выигрывает
   ставка × качество, а не формулировка. Оффер влияет по цепочке:
   выше CTR → выше качество → дешевле клик. Так и записано: обещать
   заказчику победу в аукционе за счёт текста — обещать то, чем
   мы не управляем.

**Остановились на:** навык доработан и запушен, вводных по первому
проекту ещё нет. Следующий шаг не изменился: получить три вводных
(ниша и город, ссылка на текущий сайт и карточки, что говорит сам
заказчик) — и по ним выдать первую порцию, сегменты с Job Story.
Утренние семь вопросов анкеты никуда не делись, они идут после вводных.

**Ждём от Гульнары:**
- [ ] **Вводные по первому проекту** — можно по нашему сайту, можно
      по клиентскому. Без них анализ будет разбором чужого рынка.
- [ ] **Утвердить УТП письменно**, когда оно будет сформулировано.
      Вопрос при утверждении — не «нравится ли», а «что из этого
      вы готовы выполнять каждый день».

**Не доделано в коде:**
- [ ] **Исследования по своему сайту нет** — `docs/marketing/issledovanie.md`
      не существует, тексты сайта пока опираются на догадки.
- [ ] **Ни один фреймворк не проверен на живой сделке.** Первый заказ
      пройти целиком и записать сюда, где формулы оказались неудобны.
- [ ] **Замера веса наших страниц против конкурентов нет.** Это
      и есть будущий главный оффер про скорость: пока цифры нет,
      про скорость на сайте не пишем.

## 15 августа, вечер — разбор своего сайта маркетологом

**Цель дня:** владелица попросила разобрать vibe-digital999.ru.
Первый прогон нового навыка на живом материале.

**Как смотрели.** Живой сайт из беседы не открывается — сеть окружения
отдаёт 403 на домен (`recentRelayFailures` в статусе прокси). Разбирали
по коду: `public/` и `src/` этой ветки совпадают с живой веткой
`claude/multipage-website-okje3k` байт в байт. Непроверенным осталось,
выкачена ли эта версия на Amvera.

**Сделано:**
- [ ] `docs/marketing/audit-svoego-sajta.md` — разбор: замеры, пять
      сильных мест, семь дефектов по цене ошибки, порядок работ.

**Главное, что нашли — своя же цифра не сходится.** На странице обещано
«около 65 КБ в сжатом виде», в карточке портфолио тоже 65 КБ,
в переключателе — 92 КБ, а реальный gzip главной **87 КБ**. И рядом
стоит приглашение проверить в панели разработчика. Единственное место
на сайте, где мы просим клиента нас проверить, — и там расхождение;
проверять пойдёт самый грамотный клиент.

Это ровно та ошибка, от которой навык предостерегает в разделе «чем
студия отстраивается»: замер — не константа, его повторяют и ставят
с датой. Правило написано утром, сайт нарушал его с прошлой недели.

**Ещё три дефекта, которые стоят заявок:**
- `<title>` обещает «от 15 000 ₽» (это лид-бот), первый экран — «лендинг
  от 35 000 ₽». Человек из выдачи по «заказать сайт» читает приманку.
- Три разных обещания времени ответа: «в день обращения» на первом
  экране, «в течение часа» в форме и ещё трёх местах, «за минуту»
  у ассистента. Час ночью не выполняется.
- Шесть разных формулировок кнопки на ~35 кнопок: «Заказать» (17),
  «Обсудить задачу» (7), «Получить расчёт» (4), «Обсудить проект» (4),
  «Открыть чат» (3), «Оставить заявку» (1).

**Что на сайте сильно** (не трогать при правках): четыре проверяемых
инструмента вместо прилагательных, калькулятор штрафа по 152-ФЗ,
дословные ответы на возражения, снятие страхов устройством сделки,
открытый прайс.

**Остановились на:** разбор записан, ни одна правка на сайте
не сделана — сначала нужны два ответа владелицы (замер веса и решение
про время ответа), иначе правка заменит одну неверную цифру другой.

**Ждём от Гульнары:**
- [ ] **Замер веса на живом сайте:** открыть сайт, F12 → «Сеть» →
      строка document, назвать число. Из беседы сайт не открывается.
- [ ] **Решить, какое время ответа обещаем** — и оно станет единственным
      во всех местах сайта, бота и рекламы.
- [ ] **Три-четыре реальных случая:** кто пришёл, с чем, сколько
      заплатил. Нужны для цифры потери на первом экране.

**Не доделано в коде:**
- [ ] Ни один из семи дефектов не исправлен.
- [ ] Исследования аудитории по-прежнему нет: разбор сделан по тому,
      что уже написано на сайте, а не по фактам от клиентов.

## 15 августа, утро — разбор Метрики Граната за 10–13 августа

**Что принесла владелица:** пять снимков дашборда Метрики
по `granat-kmv.ru`, счётчик 110961373, период 10–13 августа.

**Сделано:**
- [ ] `docs/granat-kmv/metrika-10-13-avgusta.md` — разбор цифр,
      четыре вывода и порядок работ.

**Цифры:** визиты 115 (+88,5 %), посетители 109 (+94,6 %), просмотры 140,
время 1 м 5 с, глубина 1,22 (−23,4 %), отказы 57,4 %. Источники: реклама
101, прямые 12, ссылки 1, **поиск 1**. Новых 104, вернувшихся 9.
Заголовок главной — 138 просмотров из 140.

**Отличие от разбора 08.08, и оно важное.** Тогда (`dva-sajta.md`)
отказы 66 % и время 5 секунд оказались поведением НА КАРТОЧКЕ в Картах,
а не на сайте, и мерить их метриками сайта было нельзя. Здесь в отчёте
стоят адреса `granat-kmv.ru/...` с метками `utm_referer=geoadv_yab…` —
это переходы с продвижения Яндекс Бизнеса НА САЙТ. Значит, поведение
относится к сайту и читается честно.

**Главное: конверсий в отчёте нет ни одной.** ТЗ на цели написано 08.08
(`docs/granat-kmv/metrika.md`, комплект в `metrika/`, цели `call_click`,
`whatsapp_click`, `telegram_click`, `email_click`) и выполняется
в репозитории granat-kmv.ru на Flask. **Внедрено оно или нет — из этого
репозитория не видно, утверждать нельзя.** Проверяется в Метрике:
Настройка → Цели.

Для типографии это дороже, чем для нас: главное действие клиента —
звонок, а звонок без цели на клик по номеру в отчётах не существует.

**Второй вывод: органики по-прежнему нет.** Один визит из поиска
за четыре дня. Запись от 08.08 говорила то же самое — за неделю
не изменилось. 88 % трафика покупное: кончится продвижение — останется
3–4 визита в день.

**Чего НЕ стал сравнивать.** В `geo-seo-plan.md` есть строка «реклама
в Картах 136 визитов», но в `dva-sajta.md` та же цифра отнесена
к vibe-digital999.ru. Чьё это число, по документам не разобрать,
поэтому сравнение «было-стало» не делал — вместо ошибочного вывода
лучше пусто.

**Ждём от Гульнары:**
- [ ] **Проверить цели** в Метрике Граната: Настройка → Цели. Есть ли
      там `call_click`, `whatsapp_click`, `telegram_click`, `email_click`.
- [ ] **Спросить у владельца Граната**, сколько было звонков и заявок
      за 10–13 августа. Пока целей нет, это единственная правда
      о конверсии.
- [ ] По своему сайту вопросы прежние: замер веса страницы в F12 → «Сеть»
      и решение про обещанное время ответа.

**Не доделано:**
- [ ] Не проверено, ведёт ли главная Граната в разделы `/pechat`
      и `/cifra`. Если ссылок нет, глубина 1,22 — это вёрстка,
      а не поведение.

### 15 августа, 7:39 — цели Граната: заведены, конверсии почти нулевые

**Владелица прислала список целей Метрики.** Вопрос из утренней записи
закрыт: цели есть и работают. Вчерашняя формулировка «конверсий в отчёте
нет» уточняется — они считаются и почти нулевые.

**В штуках, а не в процентах** (0,87 % от 115 визитов = 1 визит,
2,61 % = 3, сходится до сотых — период тот же):

- Яндекс Бизнес Автоцель «заявка» — 3;
- клик по кнопке, переход в Telegram, «Работы», переход в мессенджер —
  по 1, и это почти наверняка ОДИН человек;
- **клик по телефону — 0**;
- WhatsApp, email, отправка формы, «Заявка», «Заявка через запасной
  путь», контактные данные — пусто.

**Главное — ноль по телефону при 101 рекламном визите.** Три возможных
причины, и они требуют разных действий: номер не кликабелен (`tel:`
нет), цель размечена не на тот элемент, либо звонят прямо с карточки
в Картах, не заходя на сайт. Для трафика `geoadv` третье самое
вероятное. Проверяется за десять секунд: нажать на номер с телефона.

**Дубли:** две цели «Яндекс Бизнес Автоцель: заявка» (589591077
и 589591078), работает одна. Плюс автоцели Яндекса дублируют наши
ручные. Сейчас терпимо, при рекламе с оптимизацией по цели — обучение
по неверному сигналу.

**Сделано:**
- [ ] `docs/granat-kmv/metrika-10-13-avgusta.md` дополнен разделом
      «Цели: проверено 15.08» с переводом процентов в штуки.

**Ждём от Гульнары:**
- [ ] Нажать на номер телефона на сайте Граната с телефона —
      открывается ли звонилка.
- [ ] Открыть цель № 589591077 и посмотреть три визита в Вебвизоре:
      что именно Яндекс засчитал как заявку.
- [ ] Спросить у владельца, сколько было звонков 10–13.08.

**Вывод для правил:** процент конверсии на маленьких числах врёт.
0,87 % звучит как «почти процент», а это один человек за четыре дня.
Всегда переводить в штуки, прежде чем делать вывод.

### 15 августа, 7:50 — по Гранату: номер кликабелен

Владелица проверила с телефона: кнопка с номером на `granat-kmv.ru`
работает, звонилка открывается. Из трёх причин нулевого «Клика
по телефону» первая (номер не ссылка `tel:`) отпадает.

Осталось две, и они разводятся живым тестом: принять баннер cookie,
нажать на номер, через пару минут посмотреть цель «Клик по телефону»
за сегодня. Появилась единица — цель рабочая, значит за 10–13.08
с сайта не звонили ни разу, и обращения идут мимо сайта, прямо
с карточки. Пусто — цель размечена не на тот элемент, чинить
в репозитории Граната.

Про баннер важно: в ТЗ 08.08 (`metrika.md`, шаг 0.3) записана ловушка —
счётчик может стартовать только после согласия. Тест без принятого
баннера ничего не покажет.

**Ждём от Гульнары:**
- [ ] Живой тест цели «Клик по телефону» по порядку выше.
- [ ] Цель № 589591077 в Вебвизоре — что засчиталось за три заявки.
- [ ] Звонки у владельца Граната за 10–13.08.

### 15 августа, 7:50 — Гранат оказался нашим, а не клиентским

**Владелица уточнила:** Гранат — её собственная организация, оба сайта
её, обе организации по одному адресу. В `CLAUDE.md` стояло «типография,
там владелица работает», и из-за этой строки Гранат считали клиентским
проектом. Строка исправлена.

**Сделано:**
- [ ] `CLAUDE.md` — таблица организаций поправлена, дописаны два
      следствия: контакты Оператора и конкуренция реклам.
- [ ] `docs/granat-kmv/metrika-10-13-avgusta.md` — раздел про политику.

**Находка L-03 закрыта.** Владелица прислала снимок раздела
«6. Трансграничная передача» на сайте Граната: ст. 12 152-ФЗ названа,
основание — согласие отметкой в форме, альтернатива дана. Самый дорогой
хвост аудита 08.08 (до 300 000 ₽ по ч. 8 ст. 13.11 КоАП) снят.

**Ложная тревога, которую стоит запомнить.** В разделе стоят контакты
`+7 999 244-99-99` и `gulnaravibecoder999@yandex.ru` — контакты студии.
Я поднял это как дефект «в клиентской политике наши контакты».
Дефекта нет: организация её же. **Ошибка вышла из устаревшей строки
в CLAUDE.md, а не из наблюдения.** Наблюдение было верным, неверной
была карта.

**Что теперь важно по существу.** Две карточки в Яндекс Бизнесе, один
город, один адрес, обе кампании оплачены на полгода вперёд, и у Граната
есть раздел «Сайты и Telegram-боты» — то же, что продаёт студия.
За 10–13.08 этот раздел собрал 7 просмотров. Риск, записанный ещё
08.08 в `dva-sajta.md`, стал ближе: деньги двух кампаний могут уходить
на конкуренцию друг с другом, а Яндекс может склеить или понизить одну
из карточек по одному адресу.

**Ждём от Гульнары:**
- [ ] Одно ли ИП у Граната и студии. Если разные — в политике Граната
      Оператором должно стоять его лицо с его ИНН.
- [ ] Развести ли рубрики карточек: Гранат — полиграфия, Vibe Digital —
      сайты и боты. Решение 08.08 было «структуру не менять», но тогда
      обе карточки ещё не крутились одновременно.

### 15 августа, 8:05 — «Гранат наш» записано во все места, где чат это встретит

Владелица попросила записать так, чтобы знали все чаты. Одного
`CLAUDE.md` мало: беседа читает тот файл, что лежит в её ветке,
и правило можно пропустить. Поэтому запись сделана в пяти местах,
и одно из них печатается само.

**Сделано:**
- [ ] `.claude/hooks/session-start.sh` — **пункт 8 памятки**, печатается
      в начало КАЖДОЙ беседы независимо от ветки. Проверен запуском.
- [ ] `CLAUDE.md` — таблица организаций: «типография, там владелица
      работает» → «тоже наша», плюс два денежных следствия.
- [ ] `STATUS.md` — строка в «Где что живёт» и абзац с уточнением.
- [ ] `README.md` — было «docs/granat-kmv/ ЧУЖОЕ, ждёт переезда»,
      стало «ваша вторая организация, не клиент».
- [ ] `docs/granat-kmv/README.md` — снята шапка «это не про нас».

**Почему в памятку, а не только в правила.** Ровно тот случай, который
разобран в самом `CLAUDE.md`: правило, которое выполняется добровольно,
рано или поздно не выполняется. Устаревшая строка про Гранат прожила
в правилах несколько дней и сегодня утром стоила ложной тревоги —
я поднял её же контакты в её же политике как чужую копипасту.
Наблюдение было верным, устаревшей была карта.

**Не доделано:**
- [ ] **Чат, открытый в репозитории `granat-kmv`, этого не увидит.**
      Файлы памяти там свои. Когда будет беседа в том репозитории —
      перенести туда абзац про владение и ссылку на `docs/granat-kmv/`.
- [ ] Одно ли ИП у Граната и студии — от этого зависит, чьи реквизиты
      должны стоять Оператором в политике Граната.
- [ ] Решение про рубрики двух карточек в Яндекс Бизнесе не принято.

### 15 августа, 8:20 — ответ на вопрос «почему нет заявок»

**Уточнение владелицы:** ИП одно, два направления деятельности внутри
него. Значит Оператор в политике один и контакты на обоих сайтах верные.
Вопрос про реквизиты закрыт полностью, правок не требуется.

**Сделано:**
- [ ] `docs/marketing/pochemu-net-zayavok.md` — разбор на цифрах.

**Вывод, ради которого считали.** Заявки — это произведение:
трафик × доля целевых × конверсия. У Граната за 10–13.08: 29 визитов
в день, конверсия по автоцели 2,61 %. Перемножаем: 0,75 обращения
в день, за четыре дня примерно три — ровно то, что показывает Метрика.

**То есть сайт отрабатывает как положено, мало не заявок — мало входа.**
Это меняет порядок работ: улучшать конверсию на тридцати визитах в день
бессмысленно, спор пойдёт о десятых долях человека.

**Три причины малого входа:** канал ровно один (88 % — карточка
в Яндекс Бизнесе); из поиска не приходит никто (страницы новые, им
2–4 месяца до выдачи); люди из Карт приходят не читать, а звонить
(глубина 1,22).

**Чего не знаем, и это может перевернуть вывод.** Звонки с карточки
до Метрики не доходят вообще. Пока статистика Яндекс Бизнеса
не поднята, говорить «заявок нет» рано — возможно, они есть в виде
звонков.

**Ждём от Гульнары:**
- [ ] Статистика звонков и действий с карточки в Яндекс Бизнесе.
- [ ] Три визита по автоцели № 589591077 в Вебвизоре.
- [ ] Стоимость продвижения ÷ дни — чтобы получить цену обращения.

**Не доделано:**
- [ ] По `vibe-digital999.ru` та же арифметика не посчитана: нет снимка
      сводки Метрики. Прислать такой же — посчитаю тем же способом.

### 15 августа, 8:35 — звонков нет вообще, вывод развернулся

**Владелица уточнила:** звонков ноль, даже на «отксерить, распечатать».
Гипотеза «звонят прямо с карточки, мимо Метрики» отпадает.

**Что это значит.** 115 визитов за четыре дня и ни одного обращения
любым способом — это не слабая конверсия, это трафик, который
не является покупателями. Ксерокопию не выбирают и не обдумывают:
нужно здесь и сейчас, берут ближайшего. Ноль звонков по такой услуге
означает, что до людей с этой потребностью карточка не доходит.

Плюс трезвая цифра: 29 визитов в день на сайт типографии в Лермонтове,
где живёт около 22 тысяч человек. Для настоящего интереса это
подозрительно много.

**Четыре объяснения, по вероятности:** широкие показы и случайные клики
из Навигатора и Такси; карточка не ловит простые запросы, потому что
их нет в прайсе карточки; люди приходят ногами, а не звонят; рядом есть
ближе.

**Сделано:**
- [ ] `docs/marketing/pochemu-net-zayavok.md` — раздел с новым выводом
      и таблицей проверок.

**Ждём от Гульнары — теперь это главное:**
- [ ] **Метрика → Отчёты → География.** Из каких городов визиты.
      Не КМВ — значит канал бьёт мимо, и это ответ на весь вопрос.
- [ ] **Яндекс Бизнес → Статистика.** Показы, нажатия на телефон,
      маршруты и топ запросов, по которым находят карточку.
- [ ] Есть ли в прайсе карточки строки «ксерокопия», «распечатать
      документы», «фото на документы».
- [ ] Сколько человек пришло в офис 10–13.08 — для копицентра это
      и есть конверсия.

**Не доделано:**
- [ ] Правки сайта заморожены до ответов по пунктам 1 и 2: сайт
      не виноват в том, что к нему приводят не тех.

### 15 августа, 8:05 — отчёт «Посетители» объяснил всё

**Владелица прислала список посетителей** за 10–13.08 с фильтром
«приходили по рекламе» и карточку одного визита. Первые данные,
по которым видно поведение каждого человека.

**Что видно:**
- больше половины визитов — **0:00–0:03**, одна страница, один визит
  на человека, почти все Android;
- среднее «1 м 5 с» из сводки держится на трёх-четырёх длинных визитах
  (7:32, 23:32, 44:25) — типичный визит равен нулю;
- карточка визита 13.08 19:14: источник **«AD Яндекс: Директ»**,
  19 секунд, одна страница, достигнута цель «Яндекс Бизнес Автоцель:
  заявка», рядом внешний переход на `t.me/GranatJarvis_bot`.

**Главное: «три заявки» — это три клика по кнопке Telegram**, а не три
обращения. Автоцель Яндекса считает переход в мессенджер за заявку.
Настоящих обращений за четыре дня ноль, и это сходится с тем, что
звонков не было.

**Диагноз:** так ведёт себя не аудитория, а случайный клик. Либо показы
уходят в сети и мобильные приложения (РСЯ), либо это клики из Навигатора
и Карт на ходу. Чинится настройкой размещения, а не сайтом.

**Сделано:**
- [ ] `docs/marketing/pochemu-net-zayavok.md` — раздел «список
      посетителей всё объяснил», диагноз и таблица проверок.

**Ждём от Гульнары:**
- [ ] **Метрика → Отчёты → Источники → Директ, площадки.** Если там
      мобильные приложения и незнакомые сайты — это РСЯ-мусор,
      и он объясняет всё.
- [ ] Метрика → Источники → Директ, сводка: какие кампании.
- [ ] В настройках размещения оставить поиск и Карты, сети отключить.

**Вывод для порядка работ:** сначала чинится источник, потом карточка,
потом сайт. Пока 88 % трафика — случайные клики, улучшения страницы
делятся на ноль.

**Правило, которое стоит записать в навык рекламы:** среднее время
на сайте врёт сильнее процентов. «1 м 5 с» выглядит прилично, а список
визитов показывает нули. Смотреть распределение, а не среднее.

### 15 августа, 8:10 — НАЙДЕНА причина: 94 % рекламы откручивается в сетях

**Отчёт «Директ, площадки» по Гранату за 10–13.08:**

| Тип площадки | Визиты | Отказы | Глубина |
|---|---|---|---|
| Сети (РСЯ) | 84 (94,4 %) | 59,5 % | 1,12 |
| Поиск | 5 (5,6 %) | 20 % | 1,40 |

**Это ответ на вопрос «почему нет обращений».** 84 визита из 89 —
баннеры в приложениях и на чужих сайтах: человек не искал печать,
задел баннер, ушёл. Поиск, где реально набирают «типография Лермонтов»
и «распечатать документы», дал пять визитов за четыре дня.

**Качество поисковых визитов втрое выше:** отказы 20 % против 59,5 %,
глубина 1,40 против 1,12. Живые люди есть, их просто почти нет.

**Про кампанию:** называется `0-22308384 (N-712690544)`, своего кабинета
Директа у владелицы нет и договора с Директом тоже. Это автосозданная
кампания под подписку Яндекс Бизнеса — Директ там движок, а рычаг
управления в Яндекс Бизнесе.

**Сделано:**
- [ ] `docs/marketing/pochemu-net-zayavok.md` — раздел «ОТВЕТ: 94 %
      бюджета уходит в сети», с таблицами и планом.

**Ждём от Гульнары:**
- [ ] Узнать тариф продвижения в Яндекс Бизнесе: есть ли выбор площадок.
- [ ] Написать в поддержку Яндекс Бизнеса с этим отчётом: оставить
      Карты и поиск, сети убрать.
- [ ] Проверить тот же отчёт по `vibe-digital999.ru` (счётчик
      110416869): продвижение оплачено 06.08, схема та же.

**Предупредить заранее:** после отключения сетей трафик упадёт
с 29 визитов в день до 3–5. Это правильный исход, а не поломка.

**Правило для навыка `reklama-audit-ru`:** смотреть не «сколько визитов
с рекламы», а тип площадки. Сети и поиск в отчёте выглядят одинаково,
стоят одинаково, а по качеству различаются в разы. Отчёт: Метрика →
Отчёты → Источники → Директ, площадки, группировка «Тип площадки».

### 15 августа, 8:20 — диагноз сайта Граната целиком

**Владелица попросила диагноз «умирающего сайта».**

**Сделано:**
- [ ] `docs/granat-kmv/diagnoz-sajta.md` — диагноз по трём источникам:
      аудит 08.08 (14 находок со статусами), закрытия на 09.08,
      Метрика 10–13.08.

**Диагноз одной строкой: сайт не умирает — он ещё не начал жить
в поиске.** Умирающий сайт это тот, у кого трафик был и упал. Здесь
органики не было никогда: 1 визит из поиска за четыре дня, а нынешней
структуре семь дней. Ощущение смерти создали два факта рядом:
29 визитов в день и ноль обращений. Но это не остановка сердца,
а стройка, на которую свозят случайных прохожих.

**Разобрано по четырём системам:**
1. **Реклама — сломана** (94 % сети). Единственная система, которая
   прямо сейчас тратит деньги и не отдаёт ничего.
2. **Поиск — не запущен, и это нормально по срокам.** Индексация
   2–4 недели после переобхода, первые НЧ-позиции 1,5–3 месяца,
   заметная органика 4–6 месяцев — сроки из самого аудита. Тормозят
   два дефекта: `S-01` (страницы склеивают интенты) и `S-02` (нет
   гео-посадочных).
3. **Карточка — главный канал и он недокормлен.** `E-01` закрыт
   частично: часы поправлены, услуги с ценами не заполнены.
4. **Сайт — не проверен, потому что проверять было некем.** Судить
   о конверсии по трафику из сетей нельзя.

**Открытые находки аудита:** `S-01`, `S-02`, `T-03` (фавикон), `T-04`
(микроразметка), `C-01` (отзывы, лицо, договор), `L-01` (РКН),
`E-01` (карточка), `T-07`, `T-08` (404).

**План на три горизонта:** сегодня — сети, прайс карточки, первый экран,
сбор отзывов; две недели — разнести `/pechat` на дочерние страницы,
доверие, разметка, переобход; 1–3 месяца — гео-страницы и замер.

**Ждём от Гульнары:** порядок действий тот же, начинать с отключения
сетей и прайса в карточке.

**Не доделано:**
- [ ] Диагноз построен без живого сайта: сеть беседы его не пускает,
      репозиторий Граната не подключён. Часть находок могла быть
      закрыта после 09.08 — при подтверждении снять со списка.

### 15 августа, 8:35 — возраст бизнеса: 40 дней

**Владелица уточнила:** Гранат работает с 06.07.2026, Vibe Digital —
с 06.08.2026. За прошлый месяц у Граната два живых клиента.

**Это переворачивает чтение всех сегодняшних цифр.** «Умирающего сайта»
нет — есть бизнес возрастом сорок дней в городе на 22 тысячи человек.
Два клиента при нулевой узнаваемости — нормальная первая цифра.
Провалом был бы ноль.

**Мои поправки к собственным рекомендациям:**
- Цифра «15–20 отзывов» из аудита 08.08 к этому масштабу неприменима.
  Перенёс её механически — ошибка. Реально: просить у каждого, 5–6
  живых отзывов к декабрю.
- Ждать поиска раньше октября бессмысленно: сайту месяц, страницам
  неделя.
- Кнопку Telegram я поставил слишком высоко в приоритетах: до неё
  доходит один человек из 115. Это страховка доставки заявок,
  а не причина их отсутствия. Владелица возразила справедливо.

**Главный рычаг — не сайт, а узнаваемость в городе:** карточка, 2ГИС,
группы ВК, обход организаций, повторные заказы.

**Ближайшая возможность — конец августа.** Для типографии это сезон:
школы и садики (грамоты, стенды, расписания, бейджи), магазины и ИП
(ценники, визитки, наклейки). В Лермонтове это конечный список,
обходимый за день. Сезон через две недели и не повторится до следующего
года.

**Не доделано:**
- [ ] По `vibe-digital999.ru` отчёт «Директ, площадки» не смотрели.
      Продвижение оплачено 06.08 по той же схеме — почти наверняка
      те же сети.

### 15 августа, 8:45 — 2ГИС оплачен, дан чек-лист заполнения

**Владелица:** 2ГИС оплачен 14.08, не знает, что заполнять.

**Сделано:**
- [ ] `docs/granat-kmv/2gis-zapolnenie.md` — порядок заполнения
      по важности: рубрики → услуги с ценами → фото → часы и
      мессенджеры → описание → проверка → отзывы.

**Главное в чек-листе:** карточку находят по рубрикам и названиям услуг,
а не по описанию. Платное размещение крутится по рубрикам — неверная
рубрика означает, что деньги уходят не тем людям. Ровно та же ошибка,
что сегодня нашли в Яндекс Бизнесе с сетями, только в другом месте.

Названия услуг писать словами клиента: «ксерокопия А4», а не
«оперативная полиграфия». Каждая строка ловит свой запрос.

**Ждём от Гульнары:**
- [ ] Прислать прайс Граната — подготовлю готовые строки услуг
      под копирование сразу для 2ГИС и для Яндекс Бизнеса.
- [ ] Сверить, что название, адрес и телефон в 2ГИС, Яндексе и на сайте
      совпадают символ в символ.

### 15 августа — маркетолог подключён к ветке `claude/connect-marketer-yfwc8q`

**Вопрос владелицы:** «Как подключить маркетолога с прошлого чата?»

**Что оказалось.** Навык `marketolog-issledovanie` никуда не пропал —
он лежал в ветке `claude/marketer-web-projects-fpsmk9`, где его вчера
и собрали. Новый чат открылся в своей ветке, а навыки живут в той ветке,
где лежат, — поэтому в этой беседе его сначала не было.

**Сделано:**
- [x] `9a4c8e1` Ветка маркетолога влита сюда целиком (перемотка, своих
      правок в этой ветке не было). Приехали разом: седьмой навык
      со скриптом проверки смыслов, разбор Граната по Метрике,
      чек-лист 2ГИС, `docs/marketing/`, обновлённая памятка хука.

**Как подключается навык — записать, чтобы не искать снова.** Отдельной
команды «включить» нет. Навык подхватывается сам, если папка
`.claude/skills/<имя>/` есть в той ветке, где открыт чат. Пропал —
значит чат в другой ветке, лечится `git merge origin/<ветка> --no-edit`.

**Остановились на:** маркетолог в этой беседе доступен, но исследование
по нашему сайту по-прежнему не написано — оно делается на фактах
владелицы. Следующий шаг прежний: семь вопросов из
`.claude/skills/marketolog-issledovanie/references/anketa.md`, по одному,
начиная с трёх последних клиентов.

### 15 августа — сезон конца августа: план обхода Лермонтова

**Поправка владелицы принята и была верной.** Гранат работает с 6 июля,
Vibe Digital — с 6 августа. Сорок дней и два клиента при нулевой
узнаваемости — нормальный старт, а не провал. Все прежние выводы про
«умирающий сайт» этим отменяются, диагноз уже поправлен записью
в `diagnoz-sajta.md`.

**Главное из поправки:** через две недели конец августа, для типографии
это сезон — школы, садики, магазины. В Лермонтове это конечный список,
обходимый ногами за день. Сезон стоит дороже любых правок сайта
и не повторится до следующего года.

**Сделано:**
- [x] `dfe478c` **`docs/granat-kmv/sezon-avgust.md`** — план
      обхода: кого (школы, сады, магазины), с кем говорить (завхоз
      и секретарь, а не директор), что положить в папку образцов,
      маршрутный лист, скрипт разговора на тридцать секунд, что
      оставлять, как просить отзывы, что мерить к 1 сентября.

**Список организаций собран поиском и помечен как непроверенный.**
Портал администрации Лермонтова из беседы не открывается — сеть
окружения блокирует `lermontov-r07.gosweb.gosuslugi.ru` и `arxim.ru`.
Поэтому в файле стоят четыре школы и шесть садовских адресов с прямой
пометкой «сверить в 2ГИС». Одно расхождение видно уже сейчас: СОШ № 4
и администрация города числятся по одному адресу (Решетника, 1) —
почти наверняка ошибка справочника.

**Ждём от Гульнары:**
- [ ] **Отключить сети в Яндекс Бизнесе** — оставить поиск и Карты.
      Пока 94 % бюджета уходит в сети, любые улучшения делятся на ноль.
- [ ] **Прислать прайс Граната на печать** — соберу прайс-листовку
      на десять ходовых позиций для папки образцов и строки услуг
      для 2ГИС.
- [ ] **Сверить список организаций в 2ГИС** по рубрикам «школы»,
      «детские сады», «продуктовые магазины».
- [ ] **Попросить отзыв у двух клиентов** — текст готов в файле.

**Остановились на:** план обхода написан, дальше он упирается в цены
на печать. Следующий шаг после них — прайс-листовка одной страницей
и строки услуг для карточек.

### 15 августа — искал прайс Граната: в git его нет

**Владелица:** «прайс по Гранату делали в прошлое воскресенье
и вживляли в бота для VK заявок».

**Искал в обоих репозиториях, по всем веткам** — по словам
«ксерокопия», «ламинирование», «прайс», «₽». Результат:

| Где | Что нашлось |
|---|---|
| `granat-kmv`, `app/pechat/page.tsx` | шесть позиций: визитки 200–500, приглашения 500–2000, сертификаты 300–1500, буклеты 1000–5000, стикеры 100–500, фирменный стиль 5000–15000 |
| `granat-kmv`, последний коммит | **8 августа.** Воскресной работы (9.08) в репозитории нет вообще |
| `vibe-digital999`, `docs/15-KARTY-PRICE.md` | прайс на 60 позиций, но это **прайс студии** — лендинги и боты, не печать |
| Боты `api/vk-bot.js`, `api/brief.js` | прайс студии из `src/prices.js`. Гранат там не упоминается ни разу |

**Причина известна и записана в правилах:** бот Граната — отдельное
приложение `jarvis-bot`, его файлы загружаются в Amvera **вручную**,
мимо GitHub. Прайс, вшитый в бота, живёт только там.

**Отсюда две вещи, и вторая важнее первой:**

1. Прайса печати у нас нет ни в одном файле, и выдумать его нельзя.
   Взять — либо снимком ответа бота в ВК, либо файлом с её компьютера.
2. **Это ровно та болезнь, которую 13.08 вылечили у студии
   через `src/prices.js`.** Цена, лежащая в одном экземпляре внутри
   приложения на Amvera, не может попасть ни на сайт, ни в 2ГИС,
   ни в Яндекс Бизнес, ни в листовку — и разъедется, как только
   её где-то поправят руками.

**Замечено попутно:** на странице печати сайта Граната **нет ни
ксерокопии, ни фото на документы** — а это два самых частых запроса
в маленьком городе. Шесть позиций там про полиграфию под заказ.

**Ждём от Гульнары:**
- [ ] Прислать прайс печати — снимок ответа бота в ВК или файл.
      Дальше положу его в `docs/granat-kmv/price-pechat.md` как
      единственный источник и соберу из него строки для 2ГИС,
      Яндекс Бизнеса и листовку для обхода.

**Проверено по веткам, а не по журналу** (просьба владелицы «посмотри
ветку, где боту прайс приживляли»):

- **15 веток `vibe-digital999` + 2 ветки `granat-kmv`** прочёсаны
  по словам «прайс», «ксерокопия», «ламинирование», «копия», «А3»,
  «фото на документы», «₽».
- **За воскресенье 9 августа во всём репозитории один коммит** —
  `55d3af2 09.08 02:47 Записать статус работ по granat-kmv.ru`.
  Это запись в журнал, ни строчки кода, ни одной цены.
- **В `granat-kmv` файлов бота нет вообще.** Весь репозиторий — 24 файла
  сайта на Next.js, последний коммит 8 августа.

**Вывод: воскресный прайс в GitHub не попадал ни разу.** «Вся
электронная база» у нас неполная, и это не фигура речи: по `docs/18`
на Amvera семь приложений, у трёх мы даже имени не знаем, а кабинет
из беседы не открывается.

**Владелице не нужно ехать на работу и фотографировать бумагу** — прайс
есть в электронном виде в трёх местах, и все три доступны с телефона:
ответ бота в ВК, файлы приложения в кабинете Amvera, история той беседы,
где прайс писали.

### 15 августа — бот Граната отвечает, но упирается в тупик

**Проверка с личной страницы прошла** — версия про сообщество
подтвердилась: из-под группы бот молчал, с личной страницы отвечает
мгновенно. Правило из `STATUS.md` сработало.

**Но вскрылось хуже.** На «Дай мне весь прайс», на «Прайс» и на
«Согласен, продолжим» бот трижды подряд ответил одно и то же:

> Не понял задачу. Выберите направление:

**Кнопок под этим сообщением нет.** Бот предлагает выбрать направление
и не показывает, из чего выбирать. Человек упирается в стену на первом
же сообщении.

**Это видит не только владелица, а каждый клиент.** Слово «прайс» —
первое, что пишут в сообщество типографии. Сейчас на него приходит
«не понял».

| Что известно | Откуда |
|---|---|
| Бот жив, отвечает за секунды | снимок 15.08, 8:44 |
| Слова `прайс` в сценарии нет | три попытки подряд |
| Клавиатура ВК не приходит | на снимке под ответом пусто |
| Развёрнутый ответ он умеет | снимок 14.08, 13:42 — адрес, часы, предоплата |

**Чинить нечем: кода бота в git нет.** Приложение `jarvis-bot`
загружается в Amvera вручную. Пока код не в репозитории, ни увидеть
сценарий, ни поправить слово нельзя.

**Ждём от Гульнары:**
- [ ] Написать боту `Печать` — проверить, есть ли такое направление
      (на сайте два раздела: печать и цифра).
- [ ] Достать код бота в git — иначе каждая правка будет вслепую.

**8:47 — на слово `Печать` тот же ответ.** Три разных сообщения, включая
название раздела с сайта, дают один и тот же fallback. Диагноз меняется:

**Сломан не сценарий, а понимание.** Ключевые слова так себя не ведут:
хоть одно из «прайс», «печать», «привет» совпало бы. Отвечает всегда
одна и та же ветка «не понял» — так выглядит бот, у которого **отвалился
мозг**: ключ модели просрочен, кончились деньги на счету провайдера
или приложение упало в вечный запасной ответ.

**Что это подтверждает:** 14.08 в 13:42 бот выдал развёрнутый ответ
с адресом и часами — значит, сценарий есть и работал. Сломалось между
вчера и сегодня.

**Известная ловушка Amvera рядом:** переменные окружения читаются только
при старте. Перезапуск приложения или правка переменной могли оставить
бота без ключа.

**Проверить самому нечем:** `git.amvera.ru` из беседы закрыт — прокси
отдаёт 403, как и `amvera.io`. Ни логов, ни кода не видно.

**Ждём от Гульнары:**
- [ ] Снимок последних строк логов приложения `jarvis-bot` в Amvera.
      Пять строк покажут причину точно.

**Запасной план, если бота не поднять:** рабочий код бота ВК у нас есть
свой — `api/vk-bot.js` плюс `api/brief.js`. Он живёт в репозитории,
правится и выкатывается. Собрать на нём бота Граната со знанием
типографии — работа на день, зато код перестанет быть невидимым.

**Заготовка карточек сделана, пока владелица на работе.**
`docs/granat-kmv/kartochki-zapolnit.md` — готовый текст для копирования
в 2ГИС и Яндекс Бизнес: название, адрес, телефон, часы без перерыва,
описание на три предложения и **таблица из 22 услуг под заполнение
ценами**. Придёт прайс — останется подставить цифры.

Двадцать две строки против шести на сайте. Сейчас карточка не ловит
ни «ксерокопия рядом», ни «распечатать с флешки», ни «фото на документы» —
а это самые частые запросы в маленьком городе.

### 15 августа — прайс печати найден и расшифрован

**Владелица прислала пять фотографий прайс-листа** и сказала: «Прайс
этот должен быть на сайте и у бота».

**Сделано:**
- [x] `07a2f60` **`docs/granat-kmv/price-pechat.md`** — расшифрованы
      пять страниц: фото на документы, печать фотографий, ксерокопия
      и распечатка, флаеры и листовки, евробуклет, открытки
      и приглашения. С условиями: минимальный заказ 50 ₽ на копирование,
      700 ₽ по разделу открыток, наценки за плотность бумаги, биговка
      от 170 г/м².

**Найден сам документ.** В углу фотографий видно путь:
`C:\Users\Гульнара\Downloads\pricegranat.html`, дата в шапке —
**09.08.2026, 17:47**, внизу нумерация **«2/10», «3/10»**. То есть
прайс это HTML-файл на десять страниц, и он существует в электронном
виде — фотографировать бумагу не требовалось.

**Поправка к сегодняшнему разбору:** дата в шапке прайса — 9 августа,
воскресенье. Владелица сначала сказала «в воскресенье», потом «в
субботу»; права оказалась первая версия. Мой поиск по субботе
поэтому ничего и не дал.

**Расшифровано 5 страниц из 10.** Чего нет: раздел «Работа с макетом»
(на него ссылаются цены флаеров и буклетов), и, судя по сайту,
визитки, грамоты, баннеры, наклейки, ламинирование, брошюровка.

**Ждём от Гульнары:**
- [ ] **Прислать файл `pricegranat.html`** из папки «Загрузки» —
      закроет страницы 6–10 точными цифрами и снимет риск ошибки
      при чтении с фото.
- [ ] Сверить две расшифрованные цифры глазами: «печать готового
      сегмента 150 ₽» и «минимальный заказ по разделу открыток 700 ₽».

**Пришли остальные пять страниц — прайс расшифрован целиком.**
Добавлены разделы: материал и отделка (наценки за картон и дизайнерскую
бумагу, скругление, биговка, упаковка), печать на плоттере (чертежи
и плакаты до А0, Canon TM-340), ламинация (три толщины плёнки),
работа с макетом (проверка 150 ₽, адаптация 300–500 ₽, сложный файл
от 600 ₽, дизайн с нуля от 1 000 ₽) и раздел «как считаем»: срок
3–5 рабочих дней, срочно +30 %, предоплата 50 %, минимальные заказы
50/300/700 ₽, работа с юрлицами.

**Главная находка — не цены, а дыры.** Прайс на 10 страниц, и в нём
НЕТ визиток, грамот и сертификатов, наклеек, баннеров, брошюровки —
при том что визитки, сертификаты и стикеры обещаны на сайте с ценами.

**Грамоты — самая срочная.** Через две недели школы и садики берут
грамоты к 1 сентября, а цены на них нет нигде, кроме строчки
на сайте «300–1500 ₽».
## 15 августа — прайс типографии «Гранат» принят в репозиторий

**Цель дня:** владелица прислала фотографии печатного прайс-листа студии
«Гранат» — принять его в репозиторий так, чтобы цена лежала в одном месте
и не разъехалась, как разъезжалась цена студии четыре раза за неделю.

**Что было прислано.** Пять фотографий страниц печатного прайса
от 09.08.2026 (в подвале страниц виден исходный файл `pricegranat.html`
из её папки «Загрузки»). На страницах стоит нумерация «2/10», «3/10» —
то есть **прислано 5 страниц из 10**.

**Сделано (в репозитории `granat-kmv`, не в этом):**
- [x] `9680b8e` **`data/prices.ts` — единый прайс типографии.** Перенесены
      все шесть разделов с пяти страниц: фото на документы (7 позиций),
      печать фотографий (6 форматов × 4 тиража), ксерокопия и распечатка
      (5 позиций × 3 тиража), флаеры и листовки (4 формата × 4 тиража
      плюс шкала наценки за плотность бумаги), евробуклет А4 (6 тиражей
      с ценой за штуку и за тираж), открытки и приглашения (по 3 формата
      × 4 тиража) плюс индивидуальное приглашение от 200 ₽.
      Примечания из прайса перенесены дословно — они и есть условия:
      минимальный заказ 50 ₽ на распечатку, 700 ₽ по разделу приглашений,
      двусторонняя печать как два листа, бигование от 170 г/м².
- [x] `9680b8e` **`npm run ceny`** — печатает весь прайс списком, чтобы
      смотреть цену, не открывая код. Сделано так же, как в студии.
- [x] `9680b8e` **`NE_PERENESENO`** — список того, чего в прайсе нет.
      Заведён нарочно: без него следующий чат решит, что перенесён весь
      прайс, и начнёт выдумывать цены на визитки.

**Найдено по дороге, и это важнее самого переноса.** Страница
`app/pechat/page.tsx` на сайте Граната показывает цены, которых
в печатном прайсе нет вообще: «Визитки 200–500 ₽», «Приглашения
500–2000 ₽», «Сертификаты 300–1500 ₽», «Буклеты 1000–5000 ₽»,
«Стикеры 100–500 ₽», «Фирменный стиль 5000–15000 ₽». Совпадений
с настоящим прайсом — ноль. Настоящее приглашение А6 при тираже 100 стоит
38 ₽ за штуку, то есть 3 800 ₽ за сотню, а на сайте написано «500–2000 ₽»
без указания, за что. Человек приходит с одной цифрой в голове,
слышит другую — и это разговор, который проигран до начала.

**Остановились на:** прайс лежит в `granat-kmv`, ветка
`claude/granat-studio-price-c3bdej`, запушен. Страницу `pechat` НЕ трогали:
из пяти страниц её не пересобрать — визиток, сертификатов и стикеров нет
в перенесённой части, и убрать их с сайта, не зная настоящей цены, значит
потерять услуги. Следующий шаг — получить страницы 6–10 и пересобрать
страницу из `data/prices.ts`.

**Ждём от Гульнары:**
- [ ] **Сфотографировать страницы 6–10** печатного прайса (или прислать
      сам файл `pricegranat.html` из папки «Загрузки» — так точнее).
- [ ] **Решить по странице `pechat`:** цены на ней выдуманы. Либо
      пересобрать её из настоящего прайса, либо снять цифры совсем
      до того, как прайс перенесён целиком.

**Не доделано в коде:**
- [ ] Страница `app/pechat/page.tsx` не связана с `data/prices.ts`.
- [ ] В `granat-kmv` нет корней по правилу «ствол и корни»: ни `CLAUDE.md`,
      ни `STATUS.md`, ни `TASKS.md`, ни `docs/`. Знание о Гранате
      по-прежнему живёт в этом репозитории, в `docs/granat-kmv/`.

**Заодно: журналы снова разъехались по веткам.** 13.08 их свели к двум,
а на 15.08 в `origin` четырнадцать веток, и `TASKS.md` в них разный:
самый длинный — 4329 строк (`claude/sales-funnels-aj5au4`), самый свежий
по дате — 15.08 (`claude/marketer-web-projects-fpsmk9` и
`claude/connect-marketer-yfwc8q`), а в этой ветке и в `main` — 2814 строк
от 14.08. То есть работа за 15 августа записана как минимум в трёх местах
и нигде целиком. Сведение — отдельная работа, походя её делать нельзя:
ветки расходятся не только журналом.

### 15 августа, продолжение — прайс доехал до сайта, Telegram снят

**Цель:** владелица прислала оставшиеся пять страниц прайса и сказала:
«Чтобы на сайте granat-kmv.ru были те же прайсы. И у бота то же самое.
Телеграм как трафик для заявок снимаем — у меня телеграм не хочет
работать, я устала с ним бороться».

**Сделано (репозиторий `granat-kmv`, ветка `claude/granat-studio-price-c3bdej`):**
- [ ] `8d0e5e0` **Прайс перенесён целиком, все десять страниц.**
      Добавились: материал и отделка (картон 250/300/350, скругление
      углов, биговка, упаковка), чертежи на плоттере до А0 в четырёх
      градациях заливки, плакаты и постеры, ламинация (8 строк × 3
      тиража), работа с макетом (проверка 150 ₽, адаптация 300–500 ₽,
      сложный файл от 600 ₽, дизайн с нуля от 1 000 ₽) и условия
      расчёта: срок 3–5 рабочих дней, срочно +30%, предоплата 50%.
- [ ] `8d0e5e0` **Страница «Печать и полиграфия» пересобрана
      из `data/prices.ts`.** Руками в разметке цен больше нет.
      На главной — четыре цены, посчитанные из тех же таблиц через
      `najti()`: строку переименуют, и сборка упадёт, а не покажет
      старую цифру.
- [ ] `8d0e5e0` **Выдуманные позиции убраны с сайта:** визитки,
      сертификаты, стикеры, фирменный стиль, тиснение золотом.
      В печатном прайсе их нет вообще. Список — `NET_V_PRAJSE`.
- [ ] `8d0e5e0` **`/api/price` — прайс для бота.** Текстом и в JSON.
      Бот берёт цену по адресу, а не хранит у себя: правим прайс,
      выкатываем — у бота новая цена без правки бота.
- [ ] `8d0e5e0` **Telegram снят с сайта:** ссылки на `@GranatJarvis_bot`
      убраны из подвала и со страницы контактов. Разработка ботов как
      УСЛУГА осталась — это выручка, а не канал заявок.

**Три вещи, которые нашлись по дороге, и каждая дороже самой задачи.**

1. **Главной страницы на сайте не было.** `layout.tsx`, `page.tsx`
   и `globals.css` лежали в корне репозитория, а Next читает страницы
   только из `app/`. Собирались `/pechat`, `/cifra`, `/kontakty` —
   и всё: корень отдавал 404, меню и подвала не было ни на одной
   странице. Файлы перенесены в `app/`, `npm run build` теперь печатает
   `/` в списке адресов. Статика (robots, sitemap, картинки) уехала
   в `public/` — оттуда её отдаёт сервер, из корня не отдавал никто.
2. **Заявки с формы уходили в никуда.** `app/api/lead/route.ts` писал
   заявку в `console.log` и отвечал «Заявка получена», а человек видел
   «свяжемся в течение часа». Пока на сайте висел Telegram, часть людей
   писала туда — теперь его нет, и форма осталась единственным каналом.
   Переписано: заявка уходит письмом через SMTP Яндекса, а если почта
   не настроена — форма честно пишет «заявка не ушла, позвоните»
   и даёт телефон. Молча потерянная заявка хуже честной ошибки.
3. **Контакты на сайте расходились с печатным прайсом по трём
   пунктам:** адрес был «Лермонтово, Ставропольский край» вместо
   «г. Лермонтов, ул. Нагорная 2/1, этаж 2», часы «Пн–Пт 10:00–19:00»
   вместо «Пн–Сб 09:00–20:00, Вс 10:00–19:00», почта `info@granat-kmv.ru`
   (такого ящика нет) вместо рабочего. Всё выровнено по прайсу: адрес
   и часы теперь берутся из `STUDIYA` в `data/prices.ts`.

**Остановились на:** всё запушено в GitHub, на живом сайте пока старое.
Как granat-kmv.ru обновляется — в репозитории написано «Amvera
подключена к ветке main», но это не проверено, а у студии выкатка
устроена иначе (пуш в отдельный remote). Уточнить у владелицы, прежде
чем что-то сливать в `main`.

**Ждём от Гульнары:**
- [ ] **Сказать, как обновляется granat-kmv.ru** — Amvera сама берёт
      из GitHub или нужен push в отдельный remote, как у студии.
- [ ] **Завести в Amvera три переменные:** `MAIL_TO`, `SMTP_USER`,
      `SMTP_PASS` (пароль ПРИЛОЖЕНИЯ из Яндекса, не пароль от почты)
      и нажать «Перезапустить». До этого форма заявок работать не будет
      и будет честно просить позвонить.
- [ ] **Проверить заявку живьём** после выкатки: заполнить форму
      на сайте и убедиться, что письмо пришло.

**Не доделано в коде:**
- [ ] **Страниц `/privacy` и `/consent` не существует**, а форма на них
      ссылается и собирает персональные данные. Это 152-ФЗ, закрыть
      до любой рекламы. Ссылки из `sitemap.xml` убраны, но сами
      страницы нужны.
- [ ] **Бот пока не подключён к `/api/price`.** Адрес готов, но сам
      бот живёт отдельным приложением `jarvis-bot` на Amvera, его
      файлов нет ни в одном репозитории. Чтобы прайс доехал до бота,
      нужны эти файлы.
- [ ] **Прайс на сайты и ботов у Граната остался свой** — «от 25 000 ₽»
      на `/cifra` против «от 35 000 ₽» у студии. Расхождение известно
      с 08.08, решение не принято (см. `docs/granat-kmv/dva-sajta.md`).
- [ ] Страниц `/portfolio` и `/o-nas` нет — ссылки на них убраны
      из меню и sitemap, чтобы не отдавать поисковикам 404.

### 15 августа, вечер — живой сайт Граната оказался ДРУГИМ, Telegram с него снят

**Главное открытие дня, и оно дорогое.** Живой `granat-kmv.ru` — это
Flask-приложение `granat-site` в панели Amvera, файлы туда загружаются
руками. Репозиторий `granat-kmv` на GitHub — это ДРУГОЙ, недоделанный
сайт на Next.js. Полдня прайс переносили в него, и на живой сайт эта
работа не попала бы никогда.

**Как выяснилось.** Владелица прислала скриншот вкладки «Репозиторий»
в Amvera: там `app.py`, `templates/`, `static/`, `bots.py` — Python.
До этого я работал по GitHub и считал, что это и есть сайт.

**Правило на будущее:** прежде чем править сайт клиента или свой,
СНАЧАЛА выяснить, откуда он выкатывается. Совпадение имени репозитория
с именем домена ничего не доказывает.

**Поправка к записи выше по этому же дню.** Там сказано «заявки с формы
уходили в никуда». К живому сайту это НЕ относится: там всё сделано
хорошо — заявка пишется на диск до всякой отправки, потом уходит в три
канала сразу, есть сторож тишины и проверка `/api/health`. Потерянные
заявки были в Next.js-репозитории, который на живом сайте не стоит.

**Сделано (репозиторий `granat-kmv`, папка `zhivoj-sajt/`):**
- [x] `34b34ee` **Telegram убран из живого сайта, выкачено и проверено.**
      Решение владелицы: «Я вижу, когда в телеграм приходит информация,
      но открыть не могу». Канал, до которого нельзя дотянуться, хуже
      отсутствующего: заявка числится доставленной, счётчик молчит,
      а прочитать её нельзя.
      Что изменено в `app.py`: заявка уходит во ВКонтакте и на почту;
      после заявки клиента зовём в ВК или WhatsApp, а не в бота;
      сторож тишины докладывает по тем же двум каналам, а не в Telegram;
      приглашение Джарвиса убрано; ник в Telegram больше не принимается
      как единственный контакт — просим номер; `/api/health` считает
      `ok` по живым каналам.
      **Проверено на живом сайте:** `/api/health` отвечает
      `"telegram":"снят с сайта 15.08.2026"`, `vk: ok`, `mail: ok`.
- [x] `34b34ee` **Старая версия `app.py` сохранена рядом** —
      `app.py.do-15-08-2026`, откат за минуту.
- [x] `10ae5d1` **`pricegranat.html`** — исходный печатный прайс
      от 09.08.2026 положен в репозиторий. Сверен с `data/prices.ts`:
      **все цифры совпали**, перенос верный.

**Порядок выкатки живого сайта (записать и не забыть):** файл грузится
во вкладке «Репозиторий» приложения в Amvera, а потом ОБЯЗАТЕЛЬНО
нажимается **кубик** в правом верхнем углу — «Собрать». Круговая
стрелка рядом — это перезапуск, он поднимает СТАРУЮ сборку, и правка
не доезжает. На этом потеряли полчаса: файл лежал новый, сайт работал
на старом.

**Проверка после выкатки:** `granat-kmv.ru/api/health` с Ctrl+F5.
Обычное обновление отдаёт кэш браузера и показывает старое.

**Что ещё выяснилось из `/api/health`:** у Джарвиса `"нет токена"` —
клиентский бот не работает вовсе. Кнопка «Продолжить в Telegram» вела
в никуда ещё до того, как Telegram перестал открываться у владелицы.

**Владелица сказала:** «В ВК в сообществе у нас бот очень хорошо умеет
принимать заявки». Значит рабочий бот — ВКонтакте, и прайс подключать
надо к нему. Как он устроен — пока не смотрели.

**Остановились на:** Telegram снят и выкачен. Прайса на живом сайте
пока нет — он живёт в `templates/pechat.html`, файла у нас нет.

**Ждём от Гульнары:**
- [ ] **Проверить заявку живьём:** заполнить форму на `granat-kmv.ru`
      и убедиться, что она пришла во ВКонтакте и на почту.
- [ ] **Скачать из Amvera папку `templates`** и прислать — без неё
      не пересобрать страницу печати на настоящий прайс.

**Не доделано в коде:**
- [ ] **Шаблоны ещё предлагают Telegram.** В форме заявки он, скорее
      всего, остался в выборе канала. Сервер такой выбор больше
      не принимает и ведёт человека в ВК — но текст на странице врёт.
- [ ] **Прайса на живом сайте нет.** Готовая заготовка есть —
      `zhivoj-sajt/pricegranat.html`, уже в цветах сайта.
- [ ] **Живой сайт существует в одном экземпляре.** В GitHub лежит только
      `app.py`. `templates/`, `static/`, `bots.py`, `leads.py`,
      `contact.py`, `amvera.yaml` — только в Amvera, без истории.

### 15 августа — где клиенты: запросы и каналы

**Вопросы владелицы:** «давайте наладим, где клиенты», «что ищут
в интернете: ксерокопия, печать», «неужели никому не надо печатать».

**Сделано:**
- [x] `bfe7a1a` **`docs/granat-kmv/zaprosy-i-kanaly.md`** — четыре
      группы запросов (срочное бытовое, событийное, для бизнеса,
      профессиональное), пять каналов по важности, таблица «на что
      цена есть, а на что нет», порядок работ и что смотреть
      в Метрике.

**Главное, что отвечено на «неужели никому не надо».** Спрос
не измерен, а не отсутствует: 94 % показов ушло в сети, на поиске
Граната почти не показывали, а карточки в справочниках стоят
без услуг. Дверь была закрыта — это не то же самое, что за дверью
никого нет.

**Замечено про плоттер.** Canon TM-340 до А0 — самая недооценённая
позиция: чертежи из Лермонтова возят печатать в Пятигорск, а один
проектировщик носит заказы еженедельно. В карточках и на сайте
эта услуга сейчас не заявлена вовсе.

**Проверено перед работой, чтобы не переделывать чужое:** прайс
на живой сайт и в бота уже доехал сегодня утром — ветка
`claude/granat-studio-price-c3bdej`, коммиты `9680b8e`, `8d0e5e0`,
`34b34ee`, `10ae5d1`. Свою работу по переносу прайса не начинал.
Живой сайт Граната — Flask-приложение `granat-site` в Amvera,
а не репозиторий `granat-kmv` на Next.js.

### 15 августа — дополнение к прайсу: грамоты, визитки, наклейки

**Владелица принесла готовый разбор цен** на то, чего в прайсе не было:
грамоты, визитки, наклейки, брошюровка, баннеры. Ровно те дыры,
что нашлись утром при расшифровке.

**Сделано:**
- [x] `197cb04` **`docs/granat-kmv/ceny-dopolnenie.md`** — предложение
      записано целиком, со статусом «не утверждено, в прайс не внесено».

**Сверил с существующим прайсом — две цены сходятся, две развилки нет.**

Сходится:
- Грамота А4 за 150 ₽ ложится рядом с открыткой А5 на картоне за 90 ₽:
  больше площадь, плотнее бумага.
- Визитка 7,5 ₽ за штуку (100 шт. = 750 ₽) — ровно половина листовки
  А6 за 15 ₽ при вдвое меньшей площади.

**Развилка 1 — срочность.** В прайсе стоит «+30 %», в предложении
«+30–50 %», а строка для 2ГИС посчитана как 150 × 1,5 = 225 ₽,
то есть по +50 %. Клиент увидит на сайте одно, услышит по телефону
другое. Нужна одна цифра.

**Развилка 2 — минимальный заказ.** В прайсе «полиграфия — от 300 ₽»,
а грамота предложена по 150 ₽ за штуку. Либо грамота считается печатью
на плотной бумаге (минимум 50 ₽, продаётся поштучно), либо полиграфией
(минимум 300 ₽, то есть от двух штук).

**Развилка 3 — баннеры.** Печатает сама или через подрядчика?
Широкоформатной печати в прайсе нет, Canon TM-340 печатает по бумаге,
не по баннерной ткани. Если перепродажа — меняются и цена, и срок.
До ответа цену на баннеры не публиковать.

**Цифры конкурентов из разбора не проверены** — источники не названы,
сеть из беседы на них не пускает. На наши цены это не влияет, но
повторять чужие цифры в рекламе нельзя.

**Ждём от Гульнары:**
- [ ] Ответ по трём развилкам — после этого цены уедут в прайс,
      на сайт, в бота и в карточки одним заходом.

**Вечер 15.08 — три развилки закрыты владелицей.**

1. **Срочность +30 %**, как в прайсе. Грамота срочно — 195 ₽,
   с персонализацией срочно — 234 ₽. Цифра 225 ₽ из разбора снята:
   она считалась по +50 %.
2. **Грамота продаётся от одной штуки** за 150 ₽ как розничная услуга.
   Минимум 300 ₽ остаётся для прочей полиграфии и позиций без своей
   цены за штуку.
3. **Баннеры печатаются свои — Canon TM-340 её собственный.**
   Моя версия про подрядчика снята как ошибочная: я предположил
   перепродажу, не проверив оборудование. Плоттер печатает до 914 мм
   шириной и до 18 м длиной.

**Разведено, чтобы не путать клиента:** широкоформат на бумаге —
это плакаты, афиши и растяжки, печатаем сами; уличный ПВХ-баннер
с люверсами — «после согласования материала и обработки», потому что
нужны совместимая ткань, профиль печати и люверсатор.

**Найдено при сверке — цена за м² спорит с ценой за лист.** Лист А0
это ровно 1 м²: по прайсу 500 ₽ на бумаге и 750 ₽ на фотобумаге,
а в разборе предложено 700 и 900 ₽/м². Предложил считать лист и рулон
разными услугами: лист до А0 — по таблице, рулонная печать длиннее
А0 — от 700 ₽/м². Ждёт подтверждения.

**Сделано:**
- [x] `50fe829` Цены утверждены и записаны, десять новых строк
      добавлены в `kartochki-zapolnit.md`. **В карточке станет
      30 позиций** против одной рубрики сейчас.

**Ждём от Гульнары:**
- [ ] Подтвердить разведение «лист до А0 / рулон длиннее А0».
- [ ] Проверить ПВХ: встаёт ли ткань в TM-340, есть ли люверсатор.
- [ ] Заполнить 2ГИС тридцатью строками.

**«ДА» на разведение «лист / рулон» — 15.08.2026.** Спорных цен
не осталось, дополнение утверждено целиком:

| Услуга | Цена |
|---|---|
| Лист до А0, бумага | по прайсу, А0 — 500 ₽ |
| Лист до А0, фотобумага | по прайсу, А0 — 750 ₽ |
| Рулон длиннее А0, бумага | от 700 ₽/м² |
| Рулон длиннее А0, фотобумага | от 900 ₽/м² |
| Самоклеящаяся плёнка | от 1 200 ₽/м² |
| Уличный ПВХ-баннер | после согласования материала |

**Сделано:**
- [x] `259b4a2` Итоговая таблица широкоформата и одиннадцатая
      строка (плёнка) в карточку. **Итого 31 позиция.**

**Остановились на:** цены утверждены и лежат в
`docs/granat-kmv/ceny-dopolnenie.md`. Дальше их надо развезти —
и это работа для следующей беседы или для той, что ведёт живой сайт:

1. **Живой сайт** — Flask-приложение `granat-site` в Amvera, папка
   `zhivoj-sajt/` репозитория `granat-kmv`. Туда добавить грамоты,
   визитки, наклейки, брошюровку и широкоформат.
2. **Бот ВК Граната** — сейчас на любое слово отвечает «Не понял
   задачу», кнопки не приходят. Прайс ему не поможет, пока не починен
   сам сценарий.
3. **Карточки 2ГИС и Яндекс Бизнеса** — 31 строка готова
   к копированию в `kartochki-zapolnit.md`.

**Ждём от Гульнары:**
- [ ] Заполнить 2ГИС и Яндекс Бизнес 31 строкой.
- [ ] Снять сети в рекламе — висит с утра, не сделано.
- [ ] Проверить ПВХ: встаёт ли ткань в TM-340, есть ли люверсатор.

### 15 августа, вечер — НАЙДЕНО: реклама Граната продаёт разработку сайтов

**Открыли активную кампанию в Яндекс Бизнесе** (владелица прошла
по шагам, прислала статистику и примеры объявлений).

**Что крутится на деньги типографии:**

> «Разработаем сайт онлайн-школы под ваш проект»
> «Разработаем лендинг под ваш проект от 15000 ₽!»
> «Специалист по иллюстрациям для вашего бизнеса»

**Ни одного объявления про печать.** Ни ксерокопии, ни фото
на документы, ни грамот. Человек в Лермонтове ищет «распечатать
документы» — ему показывают разработку лендинга.

**Причина в рубриках.** В карточке стоит:
«Полиграфические услуги, **Программное обеспечение, Студия
веб-дизайна**». Две последние — это Vibe Digital, а не Гранат.
Подписка генерирует объявления по рубрикам, поэтому и пишет
про сайты.

**Деньги за 37 дней (06.07–15.08):**

| Показатель | Значение |
|---|---|
| Бюджет | 12 600 ₽ в месяц |
| Потрачено | 15 120 ₽ |
| Просмотров | 24 467 |
| Переходов | 644, по 23 ₽ |
| «Целевых клиентов» | 19, по 796 ₽ |
| «Целевых действий» | 26, по 582 ₽ |
| Реальных звонков и заявок по печати | **0** |

**Третье расхождение, найденное там же:** объявление обещает
«лендинг от 15 000 ₽», а в прайсе Граната лендинг стоит **от
25 000 ₽**. Человек звонит по одной цене, слышит другую.

**Вывод, который меняет весь разбор «почему нет заявок».** Раньше
мы считали причиной сети (94 % показов). Сети — половина беды.
Вторая половина: **даже правильно показанное объявление продавало
не то**. Типография 37 дней рекламировала веб-разработку.

**Порядок исправления:**
1. Убрать из карточки рубрики «Программное обеспечение» и «Студия
   веб-дизайна», оставить полиграфию, добавить копировальный центр
   и фотоуслуги.
2. Переоценить рекламные материалы: веб-объявлениям поставить
   «не нравится», дождаться новых — про печать.
3. Проверить, что в новых объявлениях цены совпадают с прайсом.
4. Площадками в подписке управлять нельзя — переключателей нет.
   Хотим отключать сети — это Директ, отдельным кабинетом.

**Ждём от Гульнары:**
- [ ] Вкладка «Рекламные материалы» — посмотреть все объявления
      и оценить их.
- [ ] Убрать две лишние рубрики из карточки Граната.

**Первое своё объявление создано владелицей 15.08 в 17:30** —
«Грамоты и дипломы А4 от 150 ₽ — Лермонтов», описание «Печатаем
с ФИО и номинацией. Готово в день обращения. Нагорная 2/1»,
цена 150 ₽, ссылка на главную. Блок «Свои объявления» в подписке
позволяет до 20 штук — это единственный рычаг, которым в Яндекс
Бизнесе можно управлять содержанием рекламы: автогенератор берёт
карточки товаров, а там семь веб-услуг против четырёх печатных.

**Дальше по одному:** фото на документы, ксерокопия, визитки.

**Владелица прислала макет грамоты** (золото на белом, «Награждается,
Фамилия, Имя ученика, Класс») и сказала: «на сайте пусть будет».
Задача записана, но **выполнить её из этой беседы нельзя**: живой
сайт — Flask-приложение в Amvera, файлы загружаются вручную.

**Проверить до публикации:** откуда макет. Если это шаблон из поиска —
ставить его как пример своей работы нельзя. Годится либо свой,
либо шаблон с правом коммерческого использования.

**Ждём от Гульнары:**
- [ ] Ещё три объявления: фото на документы, ксерокопия, визитки.
- [ ] Сказать, откуда макет грамоты — свой или скачанный.
